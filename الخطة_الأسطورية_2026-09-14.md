# الخطة الأسطورية الشاملة — RED Ultimate — 2026-09-14

> **القاعدة الذهبية:** كل شيء محلي/ذاتي الاستضافة (Self-Hosted). لا SaaS خارجي، لا Agora/Twilio/Vonage.
> سيرفرك + قاعدة بياناتك + SFU خاص بك + مشروع Firebase خاص بك (للنقل فقط).

## 0) تعديلات معتمدة من المالك — 2026-09-14 (تعلو على كل ما تحتها عند التعارض)

1. **صفر Firebase نهائياً:** لا FCM ولا `google-services` ولا GMS ولا أي SDK خارجي. الإيقاظ والدفع
   عبر **UnifiedPush ذاتي الاستضافة** (المعيار المفتوح الذي تستخدمه Element/Molly بدل FCM) + سوكت مباشر.
   يُحذف كل أثر Firebase من `red-app` والباك-إند (القائمة في §1).
2. **صفر APIs خارجية:** كل الـ Endpoints من سيرفرنا فقط (REST/WebSocket الخاصة بنا) — عبر الإنترنت
   أو الشبكة المحلية. أي اعتماد خارجي يُكتشف = يُستبدل ببديل ذاتي أو يُحذف.
3. **استخراج ثم حذف `app/`:** تُفرز ملفات `com/red` الـ 54 (القائمة في §1)، يُنقل المفيد منها فقط
   إلى `red-app` (نغمات، صوت، أمان كمي، شاشات فريدة)، ثم **يُحذف مجلد `app/` كاملاً** من البناء والمستودع.
4. **`red-app` هو التطبيق الأسطوري الوحيد:** أي تطوير جديد فيه حصراً، وبأحدث التقنيات والخوارزميات
   المناسبة لكل مسار (القائمة في §3).

---

## 1) ماذا فحصت وفهمت (الجرد الحقيقي)

| المكوّن | الحالة | الملاحظة |
|---|---|---|
| التطبيق القانوني | `RED_Ultimate_V1-main/RED_Ultimate/red-app/` (يُبنى كـ `:app`) — 440 ملف Kotlin | المصدر الوحيد للحقيقة |
| `app/` (Signal fork) | **خارج البناء عمداً** — مصدر استخراج فقط | لا تلمسه إلا للنقل منه |
| `ChatsScreen.kt` (2275 سطر) | **ميت 100% — صفر استخدام** | الواجهة الحية هي `ChatHubScreen` داخل `RedDashboard.kt` |
| الباك-إند | Spring Boot + Mongo + Postgres + Redis — 289 ملف | مسار الرسائل ناضج، مسار المكالمات يعمل عبر `/ws/calls` |
| media-sfu | **mediasoup ذاتي الاستضافة** (Opus FEC+DTX + JWT) | القرار الصحيح — يُحافظ عليه ويُصلَّب |
| `LiveKitBridge.kt` | مسار SFU ثانٍ في التطبيق | **تعارض** — يُحسم لصالح mediasoup (أو LiveKit، واحد فقط) |
| `/ws/calls` (JSON) | يوجّه OFFER/ANSWER/ICE + غرف جماعية + صندوق بريد 60s + Push | المسار الحي للمكالمات |
| `CALL_SIGNAL` في `/ws/master` (Proto) | **لا يفعل شيئاً — Log فقط** ولا عميل يستخدمه | مسار ميت — يُحذف من الـ Proto أو يُفعَّل، لا يُترك معلقاً |
| FCM | `PstnFcmListenerService` (خاص بـ PSTN فقط!) + `DevicePushTokenService` + مفتاح قديم اختياري | لا يوجد مستمع FCM موحد للمكالمات/الرسائل — **سبب عدم الرنين من تطبيق مقتول** |
| آثار Firebase (ستُحذف كلها) | `red-app/build.gradle.kts` + `google-services.json.example` + `AndroidManifest` + ملفات: `PstnFcmListenerService`، `VoipPushRegistrar`، إشارات FCM في `CallDeliveryEngine/CallNotificationManager/CallSystemIntegration/YounesCallService/CallBootReceiver/ScheduledCalls` + باك-إند: `NotificationService` و`ScheduledCallController` | الحصر الكامل أعلاه — الحذف في المرحلة 2 |
| ملفات `app/` الحمراء (54 ملفاً) | 11 مكررة بالاسم في `red-app` (تُقارن وتُهمل) + 43 فريدة: 3 PSTN (حذف) + ~10 مرشحة للنقل (رنين/صوت/شبكة/كمي/أمان/صلاحيات) + ~30 نموذجاً أولياً مُستبدَل بـ ChatHubScreen (تُهمل) | التفصيل في المرحلة 11 |
| PSTN/Dinstar/الSIP/اليمني | ~40 ملف في التطبيق + ~25 في الباك-إند + `kamailio/` + `pstn-asterisk/` + `voicebridge/` + `dinstar-config/` | **سيُحذف بالكامل** (قرار المستخدم) — على مراحل مع فحص البناء |
| CI | `.github/workflows` (مسارات ميتة `project/**`) + `workflow-ready/` (4 ملفات) + `ci-proposed/` | دمج في CI واحد قانوني — سبب "العمليات القديمة تتكرر" |

---

## 2) تشخيص "الرسائل لا تظهر لا لي ولا للطرف الآخر" — تم الإصلاح ✅

### السبب الجذري (واجهة، وليس تشفيراً ولا سيرفراً)

في `red-app/.../ui/RedDashboard.kt` (الشاشة الحية `ChatHubScreen`):

```kotlin
val conversationMessages = remember(decrypted, conversation) { ... }   // ❌ سطر 1673
val groupMessages = remember(decrypted, openGroup.id) { ... }          // ❌ سطر 2235
```

`remember` لا يعيد الحساب إلا عند تغيّر **مثيل** المفتاح. `decrypted` هو `SnapshotStateList` —
إضافة رسائل تغيّر **المحتوى** لا المثيل → القائمة **تتجمد على أول حساب (فارغة عادةً)** إلى الأبد.
الرسائل تصل وتُفك وتُحفظ في Room، لكن الشاشة لا تعرضها أبداً — **للطرفين** (نفس الـ APK).

### الإصلاح المطبق (2026-09-14)

```kotlin
val conversationMessages by remember(conversation) {
    derivedStateOf { resolveRichMessages(decrypted.filter { it.conversationId == conversation }) }
}
```

`derivedStateOf` يتتبع قراءات الـ SnapshotStateList ويعيد الحساب عند كل إضافة/تعديل —
نفس الأداء المقصود من التعليق الأصلي ("إعادة الحساب عند تغيّر الرسائل فقط") لكن **بشكل صحيح**.

### تدقيق إضافي تم

- `remember(messages)` في `MediaGallery` و`ChatThreadScreen`: المفتاح `List` غير قابل للتغيّر يُمرَّر من الأب
  (مثيل جديد عند كل تغيّر) → **سليم، لا يمسّ**.
- `ChatsScreen.kt:491` يستخدم `remember(decrypted.size, ...)` (مفتاح Int) → سليم، والملف ميت أصلاً.
- `DecryptedMessageBus` بلا Replay: مغطى باستعادة Room عند فتح المحادثة → يُترك، مع مهمة لاحقة
  لإضافة Placeholder للرسائل غير القابلة للفك ("بانتظار الرسالة" مثل واتساب) بدل الإسقاط الصامت.

### كيف تتحقق بنفسك (دقيقتان)

1. ابنِ التطبيق وثبّته على جهازين. 2. افتح محادثة خاصة وأرسل من الأول.
3. **قبل الإصلاح:** لا شيء يظهر عند الطرفين. **بعد الإصلاح:** تظهر فوراً (عرض متفائل) ثم ✓✓.

---

## 3) بحث المنافسين — التقنيات المختارة (كلها ذاتية الاستضافة)

### 3.1 واتساب (المرجع الأول للمكالمات 1:1)

- مكالمات P2P بتقنية WebRTC، صوت **Opus** (~48kbps مع الترويسات ≈ 720KB/دقيقة)، فيديو **H.264**
  (~1Mbps، 480p–720p تكيفي 24–30fps)، جودة تكيفية عدوانية تحفظ الاتصال قبل الدقة [1](https://www.clrn.org/how-to-make-international-calls-with-whatsapp/) [2](https://3roam.com/whatsapp-data-and-bandwidth-usage-calculator/) [3](https://axis-intelligence.com/video-call-apps-quality-comparison-2025/).
- إشارات خفيفة مخصصة للجوال (FunXMPP)، TURN كاحتياط خلف الجدران النارية، تشفير طرف-لطرف [4](https://dev.to/nube_colectiva_nc/the-architecture-of-ubiquity-what-is-whatsapp-made-of-e3).
- **ما نأخذه:** P2P أولاً لـ 1:1 + Opus بتكوين واتساب + H.264 + تكيّف عدواني + TURN ذاتي (coturn).

### 3.2 تيليجرام/سيجنال/زانجي

- Opus للصوت، MTProto/إشارات مشفرة، مكالمات P2P مع ترحيل عند الحاجة، إشعارات Push للإيقاظ.
- **ما نأخذه:** إشارات المكالمات عبر قناة مشفرة موثوقة + إيقاظ Push + مفاتيح Sender Key للمجموعات (موجودة لديك — تُستكمل).

### 3.3 زووم/إيمو/Meet (المكالمات الجماعية للأصدقاء — منفصلة عن مكالمات المحادثات)

- SFU هو المعيار 2026 فوق ~4 مشاركين (P2P ينهار O(N²))؛ Simulcast/SVC لتكييف الجودة لكل مستقبل [5](https://www.forasoft.com/blog/article/agora-io-alternative) [6](https://www.forasoft.com/learn/webrtc-architecture-production-systems).
- **القرار:** تثبيت **mediasoup** (موجود لديك) كـ SFU وحيد + Simulcast ثلاثي الطبقات + إزالة `LiveKitBridge`
  أو تجميده — **SFU واحد فقط** لمنع التعارضات.

### 3.4 البث المباشر (تيك توك/يوتيوب) — بدون هدايا، بتفاعلات

- التفاعلي الحقيقي (<500ms) = **WebRTC عبر SFU**؛ البث الواسع = LL-HLS (2–6s)؛ النمط القياسي 2026:
  WebRTC للمضيف/الضيوف + LL-HLS للجمهور الكبير [7](https://techbuzzonline.com/low-latency-live-streaming-implementation-guide/) [8](https://www.zype.com/blog/choosing-streaming-protocol).
- **ما نأخذه:** المذيع WebRTC→mediasoup؛ أول 200 مشاهد WebRTC؛ الباقي LL-HLS Egress من نفس الغرفة؛
  التفاعلات (إيموجي/تصويت/أسئلة) عبر قناة بيانات WebRTC + WebSocket — **لا هدايا إطلاقاً** (تُحذف `LiveGiftSystem`).

### 3.5 المؤتمرات والمساحات الصوتية (X Spaces)

- صوت Opus أحادي منخفض البت (24–32kbps كافٍ للصوت [9](https://cloudinary.com/guides/video-formats/opus-codec))،
  أدوار (مضيف/متحدث/مستمع)، رفع يد، كتم جماعي، تقارير جودة (MOS/RTT/Loss).
- **ما نأخذه:** غرف صوتية على mediasoup (/plain RTP) + أدوار + إيقاظ Push + تسجيل اختياري بموافقة.

### 3.6 الرنين من تطبيق مقتول — **بدون Firebase** (الوصفة السيادية)

- **UnifiedPush** هو المعيار المفتوح للدفع بدون FCM: التطبيق يسجل عبر `unifiedpush-android-connector`
  لدى **موزّع (Distributor)**، وسيرفرك يرسل POST لنقطة التوزيع عند الحاجة — الموزع عملية منفصلة
  تبقى حية حتى لو قُتل تطبيقك. تستخدمه Element وMolly وTusky في 2026 [13](https://www.bigiron.cc/guides/unifiedpush-self-hosted-push-for-android-explained) [14](https://grokipedia.com/page/UnifiedPush).
- **الموزع ذاتي الاستضافة:** سيرفر **ntfy خاص بك** (MQTT/WebSocket دائم، ~1% بطارية/يوم) + تطبيق ntfy
  كموزع، أو لاحقاً **موزع RED مضمّن** داخل تطبيقك (مكتبة `embedded_distributor`) فلا يحتاج المستخدم تطبيقاً ثانياً.
- سلسلة الرنين بعد الإيقاظ تبقى أندرويد خالصاً: Foreground Service (نوع `phoneCall`) + إشعار `CallStyle`
  + Full-Screen Intent + Telecom `ConnectionService` + أذونات 13/14 — وهي نفس وصفة واتساب/تيليجرام
  بعد استبدال قناة الإيقاظ فقط [10](https://www.forasoft.com/blog/article/android-calling-app-must-have-features-404) [12](https://www.forasoft.com/blog/article/how-to-make-a-custom-android-call-notification-455).
- مشروعك يملك `TelecomBridge` و`CallNotificationManager` و`IncomingCallActivity` — **ينقصها مستمع الدفع
  الموحد `RedPushListener` (UnifiedPush) والربط**.

---

## 4) الدفع السيادي الكامل — كيف نوقظ تطبيقاً مقتولاً بدون أي خدمة خارجية؟

1. **الحقيقة التقنية بصراحة:** تطبيق مقتول 100% لا يوقظه إلا **عملية أخرى حية** ترسل له Broadcast.
   مع FCM هذه العملية هي خدمات جوجل. **بدون جوجل هذه العملية هي الموزع (Distributor)** — تطبيق خفيف
   منفصل (ntfy أو موزع RED) يحافظ على اتصال واحد دائم بسيرفرك. لا سحر ولا API خارجي — فقط عملية ثانية.
2. **لماذا يقبل المستخدم هذا؟** لأنه موثق ومجرب: Molly (نسخة سيجنال السيادية) وElement يعملان هكذا
   على أجهزة بلا جوجل أصلاً (GrapheneOS). الفارق عن FCM: يحتاج المستخدم تثبيت الموزع مرة واحدة +
   إضافته للقائمة البيضاء للبطارية (شاشة إرشاد داخل تطبيقك).
3. **التصميم المعتمد (محلي 100%):**
   - التطبيق حي/خلفية → سوكت `/ws/calls` و`/ws/master` المباشر (الأسرع).
   - التطبيق مقتول → سيرفرك يرسل POST لسيرفر ntfy **الخاص بك** → الموزع يوقظ `RedPushListener` →
     يبدأ `YounesCallService`/`RedConnectionService` ويرنّ خلال <2 ثانية.
   - الحمولة **إشارات فقط** (callId/caller/mode) — لا محتوى — والوسائط E2EE/P2P دائماً.
   - لاحقاً: موزع RED مضمّن (صفر تطبيقات إضافية) + قناة SMS كاحتياط قصوى (مرفوضة حالياً مع إلغاء PSTN).

---

## 5) الخطة المرحلية (التنفيذ)

### المرحلة 0 — تثبيت (تمت ✅ 2026-09-14)

- [x] إصلاح تجمّد قوائم الرسائل (خاص + مجموعات) — `derivedStateOf`.
- [x] جرد كامل + بحث منافسين + هذه الخطة.

### المرحلة 1 — موثوقية المحادثات (P0) — ✅ منفذة 2026-09-14 (تحتاج بناءً وتجربة على جهازين)

- [x] Placeholder للرسائل غير القابلة للفك (`PendingDecryptPlaceholder` + `DecryptedMessageUpdateBus`) —
  بلا ACK فيعيد السيرفر التسليم تلقائياً (retry عبر المضخة + إعادة الاتصال)؛ حارس يمنع READ للنوائب.
- [x] إظهار أخطاء الإرسال (`MessageSendErrorBus` + `reportSendFailure`): الصف المتفائل → FAILED (⚠ أحمر)
  + Toast عربي فوري + ترجمة الرموز (`NO_APPROVED_REMOTE_DEVICE`…).
- [x] أرشفة `ChatsScreen.kt` الميت → `الأرشيف/dead-code-2026-09-14/` + توجيه `HomeScreen` للنسخة القانونية.
- [ ] اختبار قبول (على جهاز المالك): إرسال 1:1 ومجموعة بين جهازين + قتل التطبيق + إعادة فتح →
  كل الرسائل ظاهرة ومرتبة + النوائب تُستبدل تلقائياً + فشل الإرسال يظهر ⚠ وToast.

### المرحلة 2 — الرنين السيادي بدون Firebase (P1) "الرنين من تطبيق مقتول"

1. **حذف Firebase كاملاً:** إزالة الاعتماد من `red-app/build.gradle.kts` + حذف `google-services.json.example`
   + تنظيف `AndroidManifest` + حذف `PstnFcmListenerService` + إزالة إشارات FCM من
   (`VoipPushRegistrar` يُعاد كتابته، `CallDeliveryEngine`، `CallNotificationManager`،
   `CallSystemIntegration`، `YounesCallService`، `CallBootReceiver`، `ScheduledCalls`) +
   تنظيف `NotificationService` و`ScheduledCallController` في الباك-إند. ثم `grep firebase` = صفر.
2. **باك-إند:** `UnifiedPushSender` جديد + جدول `push_endpoints` (user_id → endpoint) +
   `POST /api/push/unified/register` — يرسل إشارات (مكالمة/رسالة/بث/مساحة) لسيرفر ntfy الخاص بك.
3. **تطبيق:** `RedPushListener` موحد عبر `unifiedpush-android-connector` → يبدأ `YounesCallService`/
   `RedConnectionService` + `CallStyle` + Full-Screen Intent + Telecom (نفس سلسلة الرنين الاحترافية).
4. **بنية:** حاوية `ntfy` ذاتية في Docker Compose + شاشة إعداد الموزع + إرشاد القائمة البيضاء
   (Xiaomi/Samsung/Huawei) + اختبار قتل فعلي. لاحقاً: موزع RED مضمّن (بلا تطبيق ثانٍ).

### المرحلة 3 — مكالمات المحادثات الخاصة 1:1 (صوت منفصل / فيديو منفصل)

- صوت: P2P + Opus (mono، FEC، DTX، 24–32kbps) + AEC/NS/AGC + بلوتوث HFP + تبديل سماعة.
- فيديو: P2P + H.264 (توافق) مع VP9 اختياري + تكيّف عدواني (واتساب: 480p→720p) + PiP + تبديل كاميرا.
- رنين: نغمة + اهتزاز + CallStyle + مهلة 45s + "غير متاح" + سجل + إعادة اتصال بضغطة.
- إشارات `/ws/calls` موجودة — تُضاف: `RINGING` ACK (يؤكد `CallDeliveryEngine`)، `BUSY`، `HOLD/RESUME` كاملة.
- قبول: جودة ≥ واتساب على 3G ضعيف (اختبار Throttling) + استهلاك بيانات مماثل.

### المرحلة 4 — مكالمات محادثات المجموعات (صوت/فيديو داخل المجموعة)

- صوت المجموعة: Mesh حتى 8 + SFU فوقها؛ فيديو المجموعة: SFU دائماً + Simulcast + Spotlight للمتحدث.
- دعوة بزر واحد ترنّ للكل (`GROUP_CALL_INVITE` موجودة — تُستكمل: حالة كل عضو، انضمام متأخر، طرد/كتم).
- رسالة نظام "بدأت مكالمة — انضم" (موجودة جزئياً — تُوحَّد).

### المرحلة 5 — المكالمات الجماعية للأصدقاء (Zoom/IMO — منفصلة تماماً)

- كيان مستقل: غرفة برابط/رمز + جدولة + غرف فرعية (Breakout موجودة — تُفعَّل) + مشاركة شاشة +
  سبورة؟ (لاحقاً) + تسجيل بموافقة + مضيف/مشارك + قفل الغرفة.
- SFU mediasoup + PIN + انتظار (Lobby). لا تخلط منطقها مع مكالمات المحادثات (حالة/شاشات/سوكت منفصلة).

### المرحلة 6 — البث المباشر (أفضل من تيك توك/يوتيوب، تفاعلات لا هدايا)

- إصلاح المسار الحالي (`LiveStreamService` + `LiveStreamWebSocketHandler` + SFU): مذيع WebRTC،
  مشاهدون WebRTC (تفاعلي) + LL-HLS Egress (واسع)، مزامنة صوت/فيديو، إعادة اتصال تلقائي.
- **حذف `LiveGiftSystem` والهدايا** من التطبيق والباك-إند والداشبورد؛ استبدالها: إيموجي طائر،
  تصويت، أسئلة Q&A، حضور، إحصاء لحظي.
- قبول: تأخير تفاعلي <1s، بث 720p ثابت على 4G، 1000 مشاهد HLS بلا كسر.

### المرحلة 7 — المؤتمرات والمساحات الصوتية (أفضل من X)

- مساحة صوتية: آلاف المستمعين (SFU صوتي + HLS صوتي للجمهور الكبير)، أدوار، رفع يد،
  كتم الكل، دعوات، جدولة، تسجيل، طرد/إبلاغ، إحصاء.
- مؤتمر فيديو: حتى 100 بالصوت + 25 فيديو + مشاركة شاشة + تخطيطات.
- قبول: مساحة 500 مستمع مستقرة + انضمام <3s.

### المرحلة 8 — إلغاء PSTN/Dinstar/الهاتف اليمني (قرار نهائي)

- الترتيب الآمن: (1) إخفاء نقاط الدخول في الواجهة (Dialer/Dinstar screens) خلف `PSTN_ENABLED=false`؛
  (2) تعطيل Controllers/Services في الباك-إند؛ (3) حذف الملفات + `kamailio/` + `pstn-asterisk/` +
  `voicebridge/` + `dinstar-config/` + جداول/هجرات PSTN + وثائق Dinstar؛ (4) بناء أخضر كامل.
- يُحفظ `EmergencyCallManager`؟ **لا** — اتصال الطوارئ عبر شبكة الهاتف يُترك لنظام أندرويد نفسه.

### المرحلة 9 — القواعد والمزامنة والصلاحيات والأجهزة

- تثبيت العقود: Mongo (رسائل/وسائط ساخنة) + Postgres (هوية/علاقات/فوترة) + Redis (حضور/مؤقتات) —
  توثيق "من يكتب أين" لكل كيان + فهارس + TTL.
- مزامنة: Catch-up + Redelivery (موجودة) + مزامنة أجهزة المرسل + حل تعارض Sequence.
- أجهزة: اعتماد الأجهزة + مسح عن بُعد + جلسات + صلاحيات دقيقة (أدوار المجموعات/القنوات/البث).

### المرحلة 11 — توحيد التطبيق: استخراج المفيد من `app/` ثم حذفه نهائياً

**الفرز المعتمد (54 ملفاً تحت `com/red` — فحص 2026-09-14):**

| القرار | الملفات |
|---|---|
| **نقل إلى `red-app`** (بعد مراجعة المحتوى، تُدمج في البنية الحالية لا كملفات موازية) | `RedRingtoneManager` (النغمات) + `RedAudioManager` (الصوت) + `NetworkMonitor` (مقارنة مع `NetworkChangeWatcher` والأفضل يبقى) + `QuantumGuard` (مقارنة مع `PostQuantumCipher`) + `SecurityVisualizerScreen` (عرض الأمان) + `GroupIDManager` + `RedPermissionManager` + `RedMediaTransporter` + أفكار `ConferenceScreens`/`GoLiveScreen`/`LiveDiscoverScreen` (تُدمج في الشاشات الحية) + `CallRepository` (النمط فقط) |
| **إهمال (مكرر أو مستبدل)** | 11 مكررة بالاسم (`RedWebSocketClient`، `CallSignalingClient`، `CallScreens`، `SettingsScreen`، `StoryViewModel`، `LuxuryChatBubble`، `LiveStreamScreen`، `RedTheme` + PSTN) + نماذج الشات الأولية (`ChatListScreen`، `ChatDetailScreen`، `RedChatScreen`، `RedChatDetail`، `RedMessageInput`، `ChatViewModel`، `GroupCreateScreen`، `GroupDetailScreen`) + (`MasterFeatureSet`، `MasterDao`، `RedMasterDatabase`، `MasterDeliveryEngine`، `RedDeliveryEngine`، `SyncEngine`، `RedMasterModule`، `IdentityManager`، `RedIdentityManager`، `RedSovereignApp`، `RedMainHost`، `SovereignAuthScreens`، `PermissionRequestScreen`، `CallOrchestrator`، `CallViewModel`، `RedVoipMaster`، `CallForegroundService`، `SfuClient`، `NotificationBridge`، `RedPushService`) — تُراجع سريعاً بحثاً عن أي منطق فريد قبل الإهمال |
| **حذف مع PSTN** | `DinstarDashboardUI`، `DinstarLiveMonitor`، `PstnViewModel` (+ نسخ `red-app` المماثلة في المرحلة 8) |

**خطوات التنفيذ:** (1) نقل الدفعة الأولى (رنين/صوت/شبكة) + بناء أخضر؛ (2) نقل الدفعة الثانية
(أمان/صلاحيات/وسائط) + بناء أخضر؛ (3) مراجعة سريعة لقائمة الإهمال؛ (4) **حذف مجلد `app/` كاملاً**
+ تنظيف أي إشارة له في `settings/build-logic/CI` + بناء أخضر نهائي. (`app/` خارج مخطط البناء أصلاً،
فالحذف آمن بعد النقل — يُستثنى من الفحص أي مهمة QA قديمة تشير إليه فتُحدَّث أو تُحذف.)

### المرحلة 10 — GitHub/CI/Docker (إنهاء الفوضى)

- CI واحد قانوني: `.github/workflows/red-ultimate-ci.yml` (بناء تطبيق + باك-إند + SFU + اختبارات)؛
  أرشفة البقية في `الأرشيف/`؛ إصلاح المسارات الميتة (`project/**`)؛ منع التكرار بـ `concurrency` و`paths`.
- مراجعة طلبات السحب المفتوحة وإغلاق/تحديث القديم؛ حماية `main` (مراجعة + CI أخضر).
- Docker: ملف واحد لكل بيئة +8479 Compose؛ فحص `docker-compose.*.yml` وحذف الزائد.

---

## 6) بروتوكول عمل الـ 20 وكيلاً (منع التعارضات)

1. **ملكية ملفات:** كل وكيل يملك مساراً واحداً فقط في كل مهمة (يُسجَّل في PR الوصف) — ممنوع لمس مسار وكيل آخر.
2. **برانش لكل مهمة** من `main` + PR صغير (<400 سطر) + CI أخضر إجباري قبل الدمج.
3. **ممنوع:** ملفات جديدة مكررة لنفس الشاشة (`ChatsScreen` الثانية لن تتكرر)؛ أي شاشة جديدة تُبنى
   داخل هيكل `ui/` الموجود وتُحذف القديمة في نفس الـ PR.
4. **العقود أولاً:** تغيير Proto/API يتطلب تحديث الطرفين + اختبار تعاقد في نفس الـ PR.
5. **PSTN مجمّد:** لا تطوير عليه إطلاقاً — حذف فقط حسب المرحلة 8.
6. **صفر خارجي:** ممنوع إضافة أي SDK/API/خدمة خارجية (Firebase، تحليلات، خطوط/خرائط سحابية) —
   أي حاجة تُحل محلياً أو تُرفع للمالك.
7. **البناء مسؤولية المنفذ:** بيئة الوكلاء بلا Android SDK — كل PR كود يجب أن يبنيه صاحبه
   (أو المالك) محلياً ويذكر نتيجة البناء في الوصف قبل طلب الدمج.

---

## 7) معايير القبول النهائية (Definition of Done)

- رسائل 1:1 ومجموعات تظهر لحظياً للطرفين + بعد القتل وإعادة الفتح.
- رنين صوت/فيديو من تطبيق مقتول خلال <2s على Pixel + Samsung + Xiaomi.
- مكالمة صوت 1:1 تعمل على 100kbps؛ فيديو يتكيف 480p→720p دون انقطاع.
- مجموعة 8 فيديو مستقرة 10 دقائق؛ بث 1000 مشاهد؛ مساحة 500 مستمع.
- صفر إشارة لـ PSTN/Dinstar في الكود والواجهة والـ CI.
- صفر إشارة لـ Firebase/FCM/GMS/`google-services` في أي سطر (`grep` نظيف) + الدفع يعمل على جهاز بلا جوجل.
- مجلد `app/` محذوف نهائياً + `red-app` هو التطبيق الوحيد الذي يُبنى.
- CI واحد أخضر + صفر PRs قديمة مفتوحة.

---

*وُثّقت من فحص مباشر للكود + بحث ويب (سبتمبر 2026). المرحلة 0 منفذة ومدفوعة على `arena/01a0a14b-pro-v1`.*
