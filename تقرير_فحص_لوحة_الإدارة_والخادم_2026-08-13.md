# فحص عميق — لوحة الإدارة + الخادم

بعد عطل `createdAt` في صفحة المستخدمين فُحصت بقية المسارات بنفس العدسة: أسماء أعمدة Postgres، ترتيب Spring Data، وإخفاء الأخطاء في الواجهة.

## ما أُصلح في هذه الجولة

| مسار | العطل | الإصلاح |
|---|---|---|
| `GET /api/admin/users` | `ORDER BY createdAt` → عمود `createdat` غير موجود | `AdminUserSort` → `created_at` |
| لوحة الرئيسية (أرقام 0) | `calculateCurrentAnalytics` كان يستدعي `findAllByStatusOrderByCreatedAtAsc` ثم يبتلع الخطأ ويعيد أصفاراً | `countByStatus` / `countCreatedAfter` بلا ORDER BY خاطئ |
| الموافقات المعلقة | نفس ترتيب `createdAt` المشتق | JPQL `ORDER BY u.createdAt` (يُترجم إلى `created_at`) |
| أجهزة المستخدم | `findAllByUserIdOrderByCreatedAtAsc` | JPQL صريح |
| سجل التدقيق العام | `ORDER BY createdAt DESC LIMIT 200` مشتق | JPQL + LIMIT |
| صفحة سجل التدقيق في اللوحة | `PageRequest` بلا عمود فعلي | `JpaSort.unsafe(..., "created_at")` |

**يجب إعادة بناء وتشغيل حاوية/عملية `backend-server`.** الواجهة وحدها لا تكفي.

---

## مشكلات مؤكدة ما زالت موجودة

### 1) نفس عائلة أسماء الأعمدة (خطر متكرر)

عشرات الدوال المشتقة ما زالت `OrderByCreatedAtDesc` على كيانات JPA:

- `AdminAuditLogRepository` (6 دوال)
- `UserReportRepository` (5 دوال)
- `PollRepository` / `StickerPackRepository`
- أي `PageRequest` + `Sort.by("createdAt")` جديد

إن مرّت عبر الـ metamodel فهي آمنة. إن أُلحِق الترتيب كمعرّف SQL خام (كما حدث في قائمة المستخدمين) ستنكسر صفحات: **سجل التدقيق، البلاغات، المحتوى، الملصقات**.

استراتيجية `CamelCaseToUnderscores` أُضيفت كشبكة أمان عامة، لكنها لا تغطي `JpaSort`/`Sort` غير الممرَّر من المسار.

### 2) أخطاء تُبتلع فتظهر أصفاراً

`AdminV2Controller.getDashboardSummary` و`getRealtimeMetrics` يستخدمان `runCatching { ... }.getOrElse { أصفار }`.

أي JDBC exception يتحوّل إلى «إجمالي 0 / في الانتظار 0» بلا رسالة حمراء. هذا ما رأيته مع المستخدمين.

### 3) أزرار الإدارة معلنة لكنها مرفوضة في الخادم

| زر اللوحة | رد الخادم الفعلي |
|---|---|
| إلغاء الحظر | `UNBAN_REQUIRES_DEVICE_REENROLLMENT` دائماً |
| ترقية إلى ADMIN | `ADMIN_PROMOTION_REQUIRES_SUPER_ADMIN` — لا يوجد SUPER_ADMIN |
| نسخة احتياطية / استعادة | `BACKUP_OPERATOR_WORKFLOW_REQUIRED` — يجب سكربت المضيف |
| حذف نهائي | `HARD_DELETE_DISABLED` |

الواجهة تعرض هذه كفشل عام إن لم تُترجم.

### 4) SSE يضع JWT في عنوان URL

`subscribeToEvents` يفتح:

`/api/admin/events/stream?access=...`

الرمز يظهر في سجلات Nginx والبروكسي وتاريخ المتصفح.

### 5) كلمة مرور مؤقتة ومسح عن بُعد

اللوحة تستدعي:

- `POST /api/admin/users/{id}/temporary-password`
- `POST /api/admin/users/{id}/remote-app-wipe`

هذه موجودة في طبقة الذكاء التشغيلي. إن فشل العمود `password_reset_required` (هجرة V17) ستنكسر صفحة الملف التشغيلي. لم يُتحقق من قاعدة حية هنا.

### 6) جرد بيانات المنصة

`getOperationsOverview` يرمي إن لم يوجد مفتاح `users` في الرد. أي عطل جزئي في Mongo/Redis يُظهر «رد الجرد غير مكتمل» رغم أن Postgres قد يكون سليماً.

### 7) Prompts/Feature flags

`target_user_ids` كان `UUID[]` ثم حُوِّل في V22 إلى `TEXT`. الكيان ما زال `String` مع `columnDefinition = UUID[]` في التعليق القديم على الإعلانات. قراءة الأعلام قد تفشل على قواعد لم تُطبَّق عليها V22.

### 8) أمان وتشغيل

- توكن الإدارة في `sessionStorage` (XSS يسرّب الجلسة).
- `RED_ADMIN_PASSWORD` و`DINSTAR_PASSWORD` الافتراضي `admin` في `application.yml`.
- لا دفع FCM لتطبيق الأندرويد (من الفحص السابق).
- `findAll()` كان يُستخدم لعدّ مستخدمي 24 ساعة — أُزيل؛ لا تُعد إضافته.

### 9) تكرار مسارات الإدارة

`/api/admin/users` موجود في `AdminV2Controller` و`getPstnUsers` يستدعي نفس المسار بدون فلاتر.  
`/api/admin/users/pending` في `AdminController` و`AdminMasterController` — مساران قديمان بجانب V2.

---

## أولوية بعد إعادة التشغيل

1. تحديث صفحة المستخدمين — يجب أن تختفي رسالة JDBC.
2. الرئيسية — الأرقام يجب ألا تكون أصفاراً إن وُجد مستخدمون.
3. الموافقات المعلقة.
4. سجل التدقيق والبلاغات (إن ظهر `createdat` أبلغ فوراً).
5. لا تعتمد على أزرار فك الحظر / ترقية أدمن / نسخ احتياطي من الواجهة حتى تُنفَّذ سياساتها.
