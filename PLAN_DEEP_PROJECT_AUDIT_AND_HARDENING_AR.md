# خطة التدقيق العميق والتطوير التحسيني لمشروع pro_v1

## الهدف

تنفيذ تدقيق منهجي وعميق لكل مكوّن يدخل في المنتج فعليًا، لاكتشاف الأخطاء والعيوب المنطقية والثغرات والمزايا الناقصة وفرص التحسين، ثم تحويل النتائج إلى دفعات إصلاح قابلة للاختبار والمراجعة على GitHub. يشمل ذلك Android والخادم وقواعد البيانات والوسائط ولوحة الإدارة والبنية التشغيلية والهاتف اليمني وDINSTAR، من دون ادعاء أن أي تكامل عتادي نجح قبل اختباره على بيئة مصرح بها.

ينطلق العمل من تقرير التدقيق السابق `PRO_V1_COMPREHENSIVE_AUDIT_2026-08-19_AR.md`، لكنه يوسعه بمراجعة دقيقة وفق خرائط اعتماد وعقود لكل مكوّن، بدل الاكتفاء بمسح الأنماط أو باختبارات البناء.

## نطاق المراجعة الفعلي

| المجال | ما ستراجعه الخطة | معيار الإكمال |
|---|---|---|
| Android | كل package منتج تحت `red-app/src/main`، حالات Compose، ViewModels، Room، الشبكة، التشفير، الأذونات، الحياة الخلفية، الإشعارات، وإدارة الجلسة. | خريطة ملف–مسؤولية–عقد–اختبار، مع نتائج مصنفة وخطة فصل الملفات المتضخمة. |
| الخادم | Controllers وServices وRepositories وSecurity وWebSocket وعمليات background وعقود REST وProtobuf ومعالجة الأخطاء. | مصفوفة endpoint–authz–input–side effect–test، بلا مسار إداري أو حساس غير مراجع. |
| البيانات | Flyway وJPA وJDBC وMongo وRedis وMinIO، retention والنسخ الاحتياطي والاستعادة والاتساق بين المخازن. | تحليل migration، فهارس، TTL، معاملات، idempotency، وخطة restore قابلة للاختبار. |
| الاتصالات والوسائط | WebRTC وSFU وTURN وAsterisk وDINSTAR وSMS وUSSD وCDR وإدارة المنافذ. | مخطط دورة حياة كامل لكل اتصال، وخطة testbed حية وفحوص failure/reconnect/failover. |
| لوحة الإدارة | RBAC، API client، شاشات الإشراف، الحالات المدمرة، الأمان، الأداء، والوصولية. | اختبارات مكونات وE2E محددة للمسارات عالية الأثر. |
| البنية والتشغيل | Compose وNginx وTLS والأسرار والمراقبة وCI/CD والموارد والشبكات وخطط الاستجابة للحوادث. | قائمة جاهزية إنتاجية واضحة، مع عناصر blocking ومالك كل عنصر. |

## مراحل التنفيذ المقترحة

### المرحلة 1: تثبيت خط الأساس وخريطة المكونات

سيتم تحديث الفرع المحلي إلى `main`، وحصر رسم البناء الحقيقي، والوحدات التي تدخل في المنتج، والوحدات المرجعية أو القديمة الخارجة عنه. سأنتج جردًا للملفات حسب المجال، وحجمها، واعتماداتها، ومالكي العقود، وأحدد الأسطر والمسارات ذات التأثير العالي. لن تُعامل أمثلة Signal أو الملفات المولدة أو `node_modules` كمكونات منتج إلا إذا دخلت البناء الفعلي.

**المخرجات:** خريطة هندسية، مصفوفة مكوّنات، وخط أساس قابل لإعادة التشغيل.

### المرحلة 2: مراجعة Android تفصيليًا

سيتم تقسيم Android إلى حزم قابلة للمراجعة: المصادقة والجلسة، الشبكة، التشفير، Room، المحادثات، المجموعات، الحالة والمنشورات، الوسائط، المكالمات، الإعدادات، وواجهة Compose. ستتبع المراجعة تدفق البيانات من حدث المستخدم إلى ViewModel ثم API ثم التخزين المحلي ثم إعادة العرض. وسيفحص كل تدفق التعامل مع الانقطاع وإعادة التشغيل والتكرار والأذونات والوصولية والتعريب.

سيتم إعطاء أولوية خاصة لبناء Outbox دائم للرسائل والوسائط، وإزالة أي كلمة مرور مخزنة، وتفكيك `RedDashboard.kt` تدريجيًا إلى وجهات وحالات مستقلة، وإضافة اختبارات سلوك للواجهات الأكثر حساسية.

**اختبارات القبول:** اختبار offline/restart، تبديل الشبكة، رفض الأذونات، تعطل التطبيق، تدوير refresh token، واستعادة واجهة صحيحة.

### المرحلة 3: مراجعة الخادم والعقود والأمان

سيتم فحص كل endpoint فعلًا من حيث المصادقة والتفويض ودور المستخدم والتحقق من المدخلات والـrate limiting والتأثير الجانبي وidempotency والاستجابة والأخطاء. ستُراجع قنوات WebSocket مسارًا مسارًا؛ وبخاصة DINSTAR وadmin logs والمكالمات والبث والمؤتمرات. كما ستراجع سياسة CSRF وCORS والرموز والتدوير والسجلات وخصوصية الرسائل.

سينفذ الإصلاح على دفعات صغيرة تبدأ بإغلاق عناصر P0: حصر قناة DINSTAR في ADMIN، توحيد error codes، وحذف إعدادات DINSTAR الافتراضية الخطرة. بعد ذلك تُضاف اختبارات تفويض سلبية وإيجابية لكل مسار حساس.

**اختبارات القبول:** USER لا يحصل على أي بث إداري، ADMIN يحصل على البيانات المصرح بها فقط، ولا تستجيب الأخطاء بتفاصيل مزود أو عتاد أو أسرار.

### المرحلة 4: مراجعة البيانات والاتساق والاستعادة

ستحلل هجرات Flyway، توافق نماذج JPA مع PostgreSQL، SQL النصي، فهارس Mongo، مفاتيح Redis وTTL، صلاحيات MinIO، وعمليات cleanup وretention. وسيُراجع كل تدفق يكتب في أكثر من مخزن لتحديد partial failure والتعافي وidempotency.

سيُبنى اختبار restore واقعي على بيانات تجريبية، وتحدد RPO وRTO المقترحتان، وتُوثق الاستثناءات القانونية والخصوصية قبل فرض retention نهائي.

**اختبارات القبول:** migration من قاعدة فارغة ومن نسخة أقدم، استعادة ناجحة، وفشل مصدر بيانات يظهر كـ"غير متاح" ولا يتحول إلى عداد صفر أو نجاح زائف.

### المرحلة 5: الوسائط، الهاتف اليمني، وDINSTAR

سيجري تحليل دورة حياة كل مكالمة من Android إلى الإشارة والخادم وSFU أو Asterisk ثم الحفظ والتنظيف. ستعالج حالة PSTN المؤقتة المخزنة داخل JVM بنقل تخصيص call–gateway–port إلى Redis أو PostgreSQL بTTL ومعرّف ارتباط، ثم ربطها بأحداث AMI.

أما اختبار DINSTAR وSMS وUSSD والصوت الحقيقي فيتطلب بيئة اختبار مصرح بها. عند توافرها، ستنفذ مصفوفة اختبارات للطراز والـfirmware وإرسال واستقبال SMS العربية وتقارير التسليم والـCDR والفشل وتحرير المنفذ وإعادة تشغيل الخادم وتعدد البوابات. لن تحفظ عناوين البوابات أو كلمات المرور أو مفاتيح AMI في Git.

**اختبارات القبول:** لا تسرب بيانات DINSTAR، لا منافذ عالقة بعد restart، واستدعاءات العتاد تتناسب مع عدد البوابات لا عدد جلسات WebSocket.

### المرحلة 6: لوحة الإدارة والتشغيل والإطلاق

ستراجع لوحة الإدارة من منظور RBAC وتجربة المشرف ومنع التكرار والتأكيدات وسجل التدقيق. ستضاف اختبارات E2E للعمليات المدمرة والحالات الفاشلة. وعلى مستوى التشغيل ستراجع Compose وNginx وTLS وsecrets وhealthchecks والمراقبة والمقاييس وحدود الموارد والشبكات، ثم سينشأ runbook للحوادث والنسخ والاستعادة وتدوير الأسرار.

**اختبارات القبول:** سيناريو E2E لكل دور إداري، وإثبات أن عمليات الحظر/المسح/الاستعادة لا تنفذ مرتين وأنها تسجل في audit log.

### المرحلة 7: بوابات الجودة والتسليم

بعد كل دفعة برمجية سيُشغّل بناء Android واختباراته، واختبارات الخادم، وفحص لوحة الإدارة، وفحص SFU، ثم اختبارات تكامل container/device حينما تتوفر البيئة. ستوثق النتائج وقيودها بوضوح، وتفتح طلبات دمج صغيرة قابلة للمراجعة بدل دمج تغيرات ضخمة. لن يدمج أي طلب إلا بتأكيد المستخدم عند الوصول إلى خطوة الدمج.

## ترتيب الإصلاحات المقترح

| الترتيب | الدفعة | السبب |
|---:|---|---|
| 1 | فرض ADMIN على WebSocket DINSTAR | تسرب محتمل لبيانات SMS وCDR وحالة البوابات. |
| 2 | إلغاء حفظ كلمة المرور المؤجلة | تقليل سطح هجوم بيانات الاعتماد فورًا. |
| 3 | لقطة DINSTAR مركزية بدل استطلاع لكل جلسة | منع إنهاك البوابات وتحسين موثوقية لوحة الإدارة. |
| 4 | Outbox دائم للرسائل والمرفقات | منع فقد المحتوى عند انقطاع الشبكة أو قتل التطبيق. |
| 5 | نقل حالة PSTN إلى تخزين مشترك | حماية دورة المكالمة من restart والتوسع الأفقي. |
| 6 | توحيد error codes وhealth states | عقود ثابتة ومراقبة صادقة دون تسريب تفاصيل. |
| 7 | اختبار SFU وDINSTAR وSMS حيًا | إثبات أن المزايا تعمل خارج الاختبارات الساكنة. |
| 8 | فصل Dashboard وإضافة E2E للإدارة | تقليل دين الصيانة وحماية العمليات الإدارية. |

## الافتراضات والمخاطر المفتوحة

يفترض هذا العمل أن `main` هو مصدر الحقيقة بعد دمج #47 و#48، وأن المستودع يحتوي كل شفرة المنتج المطلوبة. لا يمكن تنفيذ اختبار هاتف يمني أو SMS أو مكالمة حيّة دون طراز DINSTAR وfirmware وبيانات اتصال بيئة اختبار وAMI وSIMs مصرح بها. كما أن نجاح اختبارات الوحدة لا يغني عن اختبارات جهاز Android أو شبكة أو Docker متعدد الخدمات.

العمل التفصيلي سيصنف كل ملاحظة إلى: خطأ مثبت، خطر تصميم، نقص اختبار، تحسين مقترح، أو قيد بيئة؛ ولن تُعامل ملاحظة ثابتة وحدها على أنها فشل تشغيلي دون دليل.

## تعريف الإنجاز

يعد التدقيق مكتملًا عند وجود تقرير محدث لكل مجال، وقائمة P0/P1/P2، وخارطة تنفيذ ومالك/اختبار قبول لكل بند، وإغلاق عناصر P0 في طلبات مراجعة منفصلة، وتوثيق صريح لما ما زال يتطلب بيئة اختبار حية.
