# نسخ Android غير الموصولة بالبناء الفعلي — 2026-09-24

أُخرجت الملفات الأربعة أدناه من `red-app/src/main` بعد فحص مراجعها في `red-app/src/main` و`src/test`: لا يوجد مستدعٍ لها خارج ملفاتها نفسها. تُستعاد **فقط بعد** عقد API/اعتماديات/اختبارات فعلية؛ يمكن الاطلاع على نسختها الكاملة في Git عند الالتزام `1b43cc1c`، لا بإعادة دمج عمياء:

- `features/chat/MessageTranslation.kt`: نموذج ML Kit بلا اعتماد Gradle، واستدعاءات SDK غير صحيحة؛ شاشة الترجمة لم تُستدعَ قط من الدردشة. لا تدّعِ ترجمة على الجهاز قبل اختبار نماذج حقيقية وسياسة خصوصية وتنزيل.
- `features/chat/DisappearingMessages.kt`: معالج/Worker/Picker غير موصولين؛ أساليب الحذف التي يناديها غير موجودة في `LocalRepository`، وجدولة التنظيف مجرد تعليق. الحذف المحلي غير الموثوق قد يضر بيانات مستخدم غير مُزامنة.
- `settings/ModernNetworkSettingsScreen.kt`: شاشة غير موصولة؛ تطلب `NetworkState` و`DiscoveredServer` وتقدّم الفحص والقياس كأنه مُنفذ بينما `UnifiedNetworkManager` لا يوفّر تلك العقود. تبقى شاشة الإعدادات المستخدمة فعلياً في المشروع.
- `sms/SmsViewModel.kt`: اسم مستعار مهجور يشير إلى حزمة `features.sms` الملغاة مع نطاق PSTN، بلا أي مستدعٍ.

هذه إزالة لمسارات *غير مستخدمة فقط* لتحرير بناء التطبيق، وليست إعلاناً عن وجود بديل كامل للترجمة أو الرسائل الزائلة. الاختبارات على جهازين لازمة قبل تقديم أي من هذه الميزات للمستخدمين.
