# تنفيذ تحسينات الأمان والاكتمال — 2026-08-11

تم تنفيذ دفعة إصلاحات مباشرة بناءً على التدقيق السابق، في الجزء القانوني من المشروع `RED_Ultimate_V1-main/RED_Ultimate`.

## ما تم إصلاحه

1. **توحيد انتقالات حالة الحساب**
   - صفحة الإدارة والمسارات `approve/reject/ban` تستخدم الآن `RedApprovalService` بدلاً من تغيير حالة user مباشرة.
   - بذلك تعتمد الأجهزة المعلّقة وتصدر شهاداتها عند الموافقة، وتبطل refresh sessions والأجهزة عند الحظر أو الرفض.
   - أوقف الحذف الصلب من API العامة؛ صار soft delete يمر بالحظر المركزي.
   - إلغاء الحظر لا يعيد جهازاً تم إبطاله أمنياً؛ يعود خطأ صريح يطلب تسجيل جهاز جديد ثم موافقة صريحة.

2. **تقليل تصعيد الامتياز**
   - لا يمكن عبر API الإدارة الحالية ترقية مستخدم إلى ADMIN أو تعديل مدير؛ تحتاج هذه الخطوة طبقة `SUPER_ADMIN` مصممة ومراجعة لاحقاً.
   - منع تعديل الحساب الذاتي في انتقالات الحالة.

3. **خصوصية القصص والوسائط**
   - أضيفت audiences للقصص: `CONTACTS` (الافتراضي الآمن)، `EVERYONE`، و`SELECTED`.
   - فحص العرض، المشاهدة، التفاعل، والتنزيل المباشر للوسائط يطبق قاعدة audience، علاقة الاتصال المتبادلة، والحظر ثنائي الاتجاه.
   - لم تعد القصة النشطة وحدها تصريحاً لتنزيل الوسائط.

4. **تصحيح الوسائط والحساب**
   - أصلح validator مفاتيح thumbnail ليدعم `thumbs/users/...` الناتج من generator.
   - تحديث avatar لا يقبل رابطاً حراً أو كائن مستخدم آخر: يجب أن يكون object key يملكه الحساب، موجوداً، ومن نوع صورة.

5. **إعدادات وجلسات**
   - مدة access JWT تقرأ الآن `red.jwt.access-expiration-minutes` (15 دقيقة افتراضياً) بدلاً من مفتاح خاطئ يؤدي إلى ساعة.
   - مدة refresh تقرأ `red.jwt.refresh-expiration-days` من مصدر الإعداد الصحيح.
   - أصلحت Controller الحالة/الخصوصية ليستخدم `Authentication` الحقيقي؛ كان يعتمد `@AuthenticationPrincipal UserAccount` رغم أن فلتر JWT يضع principal كـUUID string.

6. **حماية API الإدارة**
   - حد أعلى `100` لصفحة users، منع page سالب، allowlist لأعمدة sort، وfallback آمن لاتجاه sort.
   - عُدّل type واجهة الإدارة لإزالة `MODERATOR` غير الموجود في enum الخادم.

## التحقق المنفذ

- `npm run check` للوحة الإدارة: ناجح.
- `npm run build` للوحة الإدارة: ناجح.
- `python3 scripts/check-schema-consistency.py`: ناجح.
- `python3 scripts/check-kotlin-static.py`: ناجح.
- `python3 scripts/check-infrastructure.py`: ناجح (74/74).
- `git diff --check`: ناجح.

## قيد بيئي معروف

تعذر تشغيل اختبارات Gradle/Backend الحية لأن البيئة لا تحتوي Java أو `JAVA_HOME`، كما لا تحتوي Docker لتشغيل PostgreSQL/Mongo/Redis/MinIO. قبل production يلزم تنفيذ اختبارات تكامل حقيقية تشمل: register → approve → device login، ban → revoke، وقواعد story/media لكل audience.
