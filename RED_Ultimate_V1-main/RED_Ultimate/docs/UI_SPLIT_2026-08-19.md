# تفكيك `ui/RedDashboard.kt` — 2026-08-19

## لماذا

بلغ الملف **3,604 سطر** و**51 تعريفًا عُلويًّا**. حجمٌ كهذا يجعل المراجعة
شبه مستحيلة، ويُبطئ الترجمة التزايدية عند كل تعديل مهما صَغُر، ويُكثر
تعارضات الدمج لأن كل عمل على الواجهة يلمس الملف نفسه.

## ما أُلغي قبل البدء

كانت في المستودع خطة سابقة: `ui/screens/README_SPLIT.md` ومعها خمسة
ملفات `{Home,Chats,Groups,Calls,More}Screen.kt`.

**حُذفت كلها**، للأسباب الآتية بعد قراءتها سطرًا سطرًا:

1. **الخطة مبنيّة على رقم خاطئ**: تقول إن الملف 1,787 سطرًا و29 دالة،
   والواقع 3,604 و51 — أي أن الخريطة تصف ملفًا لم يعد موجودًا.
2. **الملفات الخمسة كانت فارغة**: كلٌّ منها ~506 بايت لا تحوي سوى
   `XxxScreenPlaceholder()` بجسمٍ فارغ. صفر منطق منقول.
3. **لا أحد يستدعيها**: تأكّد بالبحث في الوحدة كلها؛ كانت شيفرة ميتة.
4. **التقسيم المقترح غير متوازن**: يضع `ChatHubScreen` (1,491 سطرًا)
   في ملف واحد، فلا يحلّ المشكلة أصلًا.

## المبدأ المتّبع بدلًا منها

اشتُقّت الخريطة **آليًّا من الملف الفعلي** (حدود كل تعريف، ورسم
الاستدعاءات الداخلي، وجرد مستهلكي كل رمز داخل الملف وخارجه)، ثم
اختيرت المجموعات حسب **أنظف حدٍّ فاصل** — أقل عدد من الحواف بين
المجموعة وبقيّة الملف — لا حسب أكبر دالة.

## النتيجة

| الملف | الأسطر | المحتوى | ما يحتاجه من الخارج | ما يُصدِّره |
|---|---:|---|---|---|
| `RedDashboard.kt` | **2,200** | اللوحة، التبويبات، `ChatHubScreen`، المزيد، الإنشاء، مساعدات الوقت | — | `MainSection`, `RedDashboard` |
| `MessageContent.kt` | 795 | عرض الرسائل: نص غني، وسائط، صوت، تفاعلات، منتقي الرموز | لا شيء | 14 رمزًا `internal` |
| `CallsScreens.kt` | 545 | السجلّ الموحَّد + لوحة اتصال DINSTAR | `RED_ID_PATTERN` | `UnifiedCallsScreen`, `DinstarPhoneScreen` |
| `FeedScreens.kt` | 395 | الحالات والمنشورات | `conversationId` | `FeedScreen` |

انخفض أكبر ملف من **3,604 إلى 2,200 سطر (‑39%)**.

## ضمانات السلامة

النقل **بنيوي بحت**: لم يتغيّر سطر منطق واحد. المتحقَّق منه آليًّا
بمقارنة الناتج مع `git show HEAD:` للأصل:

- **51/51** تعريفًا عُلويًّا موجودة، **صفر تعريف مكرّر** بين الملفات.
- **أسطر المنطق**: الفرق الوحيد عن الأصل هو **19 سطر تصريح** غُيّرت
  فيها كلمة `private` إلى `internal` — لا غير. كل سطر آخر منقول حرفيًّا.
- **توازن الأقواس** `{}` `()` `[]` صفر في الملفات الأربعة، كما الأصل.
- **الرؤية بأضيق نطاق**: رُقّي إلى `internal` فقط ما تستدعيه ملفات
  أخرى فعلًا (14 + 2 + 1)؛ وما يُستعمل داخل ملفه فقط بقي `private`
  (`InlinePollCard`, `VoiceWaveform`, `formatBytes`, `formatDuration`,
  `shouldAutoDownload`, `ImageMessage`, `VideoMessage`, `AudioMessage`,
  `FileMessage`, وغيرها) حتى لا يتسرّب إلى بقيّة الوحدة.
- **لا تصادم أسماء** على مستوى حزمة `com.red.sovereign.ui` (12 ملفًا).
- **لا استيراد زائد** في الملفات الثلاثة الجديدة، ولا استيراد فُقد من
  `RedDashboard.kt` (الـ258 استيرادًا ما زالت كلها مستعملة فيه).
- لا اختبار كان يشير إلى `RedDashboard`، فلا انكسار في مجموعة الاختبارات.

## دَينٌ باقٍ

`ChatHubScreen` ما زال **1,491 سطرًا** داخل `RedDashboard.kt`. تفكيكه
أصعب: حالته الداخلية متشابكة (مُركِّب الرسائل، الردود، التحرير،
الاقتباس، البحث)، وفصله يستلزم استخراج حاملِ حالة أولًا لا مجرّد نقل
دوال. يُترك لجولة قادمة مستقلّة، لأن خلطه بنقلٍ بنيويّ آمن كهذا يُفقد
القدرة على إثبات أن شيئًا لم يتغيّر.

## قاعدة مستخلَصة

> خطة مكتوبة داخل المستودع قد تكون بائدة. لا تُنفَّذ قبل إعادة اشتقاق
> الخريطة آليًّا من الملف الفعلي والتحقق من أن أرقامها ما زالت صادقة.
