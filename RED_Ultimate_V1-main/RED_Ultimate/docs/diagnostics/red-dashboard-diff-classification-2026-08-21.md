# تصنيف فرق `RedDashboard.kt` — 21 أغسطس 2026

## ملخص المراجعة

تمت مراجعة الفرق المحلية في `red-app/src/main/java/com/red/sovereign/ui/RedDashboard.kt` من دون إعادة ضبطها. الفحص النصي للفروق نظيف. يحتوي الملف على مجموعة غير متجانسة من تحسينات الواجهة والحالات والمنشورات والمجموعات والرسائل الصوتية، إلى جانب توسعة صريحة لمسارات DINSTAR/PSTN/SMS. لذلك لا ينبغي دمجه كمجموعة غير مفصّلة عند إعداد release ضمن النطاق الحالي.

| المؤشر | القيمة |
|---|---:|
| الفروق المسجلة | 46 سطراً مضافاً و224 سطراً محذوفاً تقريباً |
| فحص المسافات `git diff --check` | نظيف |
| قرار المراجعة | **إبقاء الفروق المحلية كما هي؛ فصل تصنيفي قبل الدمج أو الإصدار** |

## التغييرات المصنفة ضمن النطاق المسموح

| المجال | التغيير | التقييم |
|---|---|---|
| المنشورات | تحويل تبويب الخلاصة من `FOLLOWING` إلى `FRIENDS`، واستدعاء `feed::requestFriend` بدلاً من المتابعة، وتعديل النص إلى «أضف صديقاً» | متوافق مع قرار المنتج: الأصدقاء أو العام |
| التنقل الأساسي | استبدال `NavigationBar` المحلي بـ `SovereignBottomBar`، مع الإبقاء على تحميل سجل المكالمات وتحديث presence عند فتح قسم المكالمات | تحسين واجهة قابل للاحتفاظ بعد مراجعة مرئية |
| صلاحيات الإدارة | تقييد الوصول إلى `DINSTAR_ADMIN` عند `account.isAdmin` | تحصين صلاحيات مناسب، لكن لا يمثل تفعيل DINSTAR |
| المحادثات الجماعية | استعادة مدة اختفاء رسائل المجموعة من التخزين المحلي وحفظها عند تغيير الإعداد | متوافق مع تحسينات المجموعات والرسائل |
| وسائط المجموعات | عدم قصر قائمة رسائل المجموعة على نوعين فقط، لدعم الوسائط والملفات والملصقات الموجودة | متوافق مع توسيع تجربة المجموعات |
| واجهة المحادثة | استخدام `SovereignEmptyConversationState` للحالة الفارغة | تحسين تجربة استخدام آمن |
| الرسائل الصوتية | توجيه تنزيل الصوت إلى `attachments.downloadVoice` بدلاً من التنزيل العام | تغيير متخصص منطقي بشرط اجتياز اختبارات الوسائط |

## التغييرات المؤجلة أو الخارجة عن النطاق

| المجال | التغيير | سبب الاستبعاد | قرار الإصدار الحالي |
|---|---|---|---|
| إعدادات PSTN | إضافة `PSTN_CONFIG` إلى `SovereignScreen` وربطه بـ `PstnConfigScreen` | PSTN وDINSTAR خارج النطاق صراحةً | لا يُدمج ضمن release هذه الجولة |
| قائمة «المزيد» | إضافة خيار «إعدادات PSTN / DINSTAR» | مرتبط مباشرةً بـ PSTN وDINSTAR | مؤجل |
| SMS/DINSTAR القديم | حذف 198 سطراً تقريباً من واجهة SMS المضمّنة لصالح مسار إعدادات آخر | SMS/DINSTAR مستبعدان؛ الحذف جزء من إعادة تشكيل مسار مؤجل | لا يدمج مستقلاً من دون مراجعة نطاق منفصلة |
| استيرادات مرتبطة | `NetworkCheck`، `PstnConfigScreen`، `SnackbarHost` و`SnackbarHostState` في سياق مسار PSTN | تبعية لمسار مؤجل | تُفصل مع كتلة PSTN |

## تبعيات الدمج ومخاطرها

توجد تغييرات ضمن النطاق وتغييرات مؤجلة في الملف نفسه، خصوصاً حول `SovereignScreen` و`MoreScreen`. لهذا فإن استبعاد PSTN/DINSTAR من release لا يعني حذف `RedDashboard.kt` أو إعادة ضبطه. الإجراء الصحيح هو إنشاء commit/patch فصل لاحق يحافظ على التحسينات التالية: تبويب الأصدقاء، طلب الصداقة، تحصين دخول الإدارة، شريط التنقل، حالات المحادثة الفارغة، إعداد اختفاء رسائل المجموعات، وتوجيه تنزيل الصوت.

> لا يتم حالياً نقل أو حذف أو إعادة ضبط أي سطر من هذا الملف. التصنيف فقط يحمي الفروق المحلية ويمنع خلط نطاق PSTN المؤجل بتحسينات التطبيق المعتمدة.

## نتيجة المراجعة

أصبح الفرق الكبير مفهوماً ومصنفاً. لا يوجد عيب شكلي من `git diff --check`. مع ذلك، يجب أن يسبق أي merge أو staging من هذا الملف فصل كتلة PSTN/DINSTAR/SMS في تغيير مستقل أو تأجيلها كاملةً، ثم إعادة اختبار Android على النسخة الناتجة.
