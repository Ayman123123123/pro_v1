# Debug signing (committed — safe for team)
debug.storeFile=signing/red-debug.p12
debug.storePassword=red-debug-only
debug.keyAlias=reddebug
debug.keyPassword=red-debug-only
debug.storeType=PKCS12

# Release signing (DO NOT COMMIT — use env vars or CI secrets)
# release.storeFile=path/to/release.keystore
# release.storePassword=
# release.keyAlias=
# release.keyPassword=
# release.storeType=PKCS12
