2026-08-21: My Browser reached https://192.168.11.2/enFrame.htm (UC2000-VE Business) after user takeover. This confirms an authenticated management session is available for read-only configuration and API investigation. No gateway configuration was changed.
Read-only browser navigation to https://192.168.11.2/api/get_port_info returned HTTP Forbidden / Access Denied Prohibited User despite the authenticated web-console session. The web console session therefore does not authorize this API route; API credentials/roles or the documented endpoint policy must be corrected separately. No SMS was sent and no gateway configuration was changed.
The authenticated UC2000 console exposes the SMS and USSD section with SMS Send Overview, SMS Send Limit Settings, SMS Routing, SMSC Switch Setting, Send SMS, SMS Outbox, SMS Inbox, USSD, and SMS Settings. This confirms the configured device supports managed inbound/outbound SMS and provides read-only diagnostic pages for subsequent validation.
Read-only SMS Send Overview inspection: ports 0–7 are listed, but current minutely/day/month counts and frequency/daily/monthly limits are displayed as '--' for every port. No counters or configured limits are visible on this overview page. The Clear controls were not used.
The SMS and USSD menu was re-opened successfully in the authenticated UC2000 session, confirming access to SMS Routing, Send SMS, SMS Outbox, SMS Inbox, and SMS Settings pages for read-only diagnosis.
Read-only SMS Settings inspection: the page exposes SMS resend-after-failure and email integration switches (bind mail to port, SMS-to-email, email-to-SMS). These are ancillary email features rather than the GSM SMS send path. No field was changed and Save was not used.
The authenticated UC2000 SMS menu was re-opened and exposes SMS Routing as a distinct page. No routing rule or delivery setting was changed at this step.
Read-only SMS Routing Rule inspection: the visible routing rows have unchecked enable boxes, blank destination-number/prefix fields, Source defaults to SMPP, and Destination Port defaults to Any. No explicit API-source routing rule is visibly enabled. This is a likely configuration gap for programmatic SMS routing, but no routing rule was changed because the required port/SIM selection must be confirmed against device configuration and a controlled test.
The UC2000 Advanced menu exposes port-operation features (overview, basic configuration, phone number learning, balance, billing, call limit, exception handling, forwarding, schedule) but no visible HTTP API account or permission page. No advanced setting was changed.
The authenticated UC2000 Mobile Configuration menu exposes Basic Configuration, which is the official location documented for enabling the new-version HTTP API. No mobile configuration setting was changed before opening that page.
Critical read-only finding: Mobile Configuration > Basic Configuration shows the API radio set to New Version. This satisfies the official API prerequisite, so HTTP API failure is not caused by the feature being disabled. The remaining gateway blockers are the HTTP API credential mismatch/authorization and the absence of a confirmed active API routing path. No Save action was used.
