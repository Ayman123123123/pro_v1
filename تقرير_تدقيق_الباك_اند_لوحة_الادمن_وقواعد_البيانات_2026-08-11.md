# تقرير تدقيق فعلي — Backend + لوحة الإدارة + قواعد البيانات
**التاريخ:** 2026-08-11
**النطاق المفحوص:** `RED_Ultimate_V1-main/RED_Ultimate/`، وبالتحديد `backend-server` و`admin_dashboard` وMigrations PostgreSQL وMongo/Redis/MinIO والتشغيل عبر Compose.
**منهجية التدقيق:** قراءة الكود ومسارات الصلاحيات وتدفقات التسجيل/الجلسات/الوسائط والإدارة، مراجعة 26 migration، بناء وفحوص ساكنة وتشغيل فحوص عقد API. لم أعتمد على التقارير الموجودة مسبقاً كمصدر للحكم.

> **الحكم التنفيذي:** البنية طموحة ومكوّناتها كثيرة، لكن النسخة ليست جاهزة للإنتاج أو للاعتماد الأمني بعد. توجد عيوب مؤكدة في مسار موافقة الحسابات من لوحة الإدارة، الخصوصية، وإبطال الجلسات. هذه ليست تحسينات تجميلية؛ بعضها يمنع مستخدماً تمت الموافقة عليه من تسجيل الدخول، وبعضها يتيح وصولاً لمحتوى قصص، أو يبقي جلسة مستخدم محظور صالحة مؤقتاً.

---

## 1) ما تم فحصه بالفعل

| الجزء | ما تمت مراجعته |
|---|---|
| Backend | 142 ملف Kotlin، SecurityConfig، JWT، Auth، التسجيل والموافقات والأجهزة، refresh tokens، WebSocket، الرسائل والمجموعات، الوسائط، القصص، PSTN/Dinstar، الإدارة والتدقيق |
| لوحة الإدارة | 35 ملف TypeScript/TSX، `api.ts`، الحراسة، التخزين، تسجيل الدخول، كل المسارات التي تستدعيها الصفحات، خادم التطوير الوهمي |
| PostgreSQL | 26 ملف Flyway migrations من `V1` إلى `V26`، تطابق Entities/Schema، القيود والفهارس ومسار users/devices/sessions/grants |
| بقية التخزين | وثائق Mongo، Redis rate limit/presence، MinIO/object keys، Docker Compose وملف البيئة |
| عقد التكامل | مقارنة استدعاءات اللوحة مع مسارات الخادم (100 استدعاء مقابل 288 مساراً حسب الفاحص) |

---

## 2) النتائج الحرجة والعالية — يجب إصلاحها قبل الإطلاق

### C-01 — زر موافقة المستخدم في لوحة الإدارة يتجاوز خدمة الموافقة الصحيحة
**الخطورة: حرجة وظيفياً وأمنياً**

- **الدليل:** `admin_dashboard/src/api.ts` يستدعي `POST /api/admin/users/{userId}/approve`.
- هذا المسار ينفذه `admin/controller/AdminV2Controller.kt` بتغيير `user.status` فقط.
- بينما المنطق الصحيح والمركزي موجود في `auth/RedApprovalService.kt::processAction()`؛ وهو يصدر شهادة الجهاز ويحوّل الأجهزة المعلّقة إلى `APPROVED` ويسجل audit موحداً.
- بعد التسجيل يكون الجهاز `PENDING` في `DeviceEnrollmentService.kt`، و`RegistrationService.login()` لا يقبل جهازاً غير معتمد.

**الأثر:** حساب تمت الموافقة عليه من واجهة الإدارة قد يظل غير قادر على تسجيل الدخول، لأن جهازه لم يُعتمد. كما أن الرفض والحظر من نفس واجهة الإدارة يتجاوزان إبطال refresh sessions وإلغاء الأجهزة الذي تنفذه الخدمة المركزية.

**الإصلاح المطلوب:** احذف من `AdminV2Controller` تعديلات الحالة المباشرة، واجعل approve/reject/ban/unban/delete تستدعي خدمة حالة موحدة داخل transaction. يجب أن تكون الخدمة الوحيدة التي تغيّر `AccountStatus`، وتقوم بـ:
1. اعتماد الأجهزة وإصدار certificate عند approval؛
2. revoke لكل refresh tokens والأجهزة عند الحظر/الرفض/الحذف؛
3. منع العملية على حساب الإداري الأخير؛
4. تسجيل حدث تدقيق واحد غير قابل للتلاعب.

**اختبارات قبول لازمة:** register → approve من صفحة Approvals → login من الجهاز نفسه؛ ثم ban → رفض access token/refresh token/WebSocket مباشرة.

---

### C-02 — القصص ووسائطها مكشوفة لكل مستخدم مصادق ولا توجد خصوصية للقصص
**الخطورة: حرجة للخصوصية**

- `stories/StoryService.kt::active()` يعيد كل story نشط لكل مستخدم، بلا owner/audience/contacts/block checks.
- `media/MediaAccessService.kt::requireDownloadAllowed()` يسمح بتنزيل أي كائن إذا وجده في `StoryDocument` فعال، بلا فحص أن الطالب يحق له رؤية القصة.
- `StoryDocument` لا يحتوي أصلاً جمهوراً أو visibility أو قائمة استثناءات.

**الأثر:** أي حساب معتمد يستطيع قراءة قائمة القصص وتنزيل وسائط كل القصص النشطة، حتى لو كان مالك القصة يتوقع نموذج جهات الاتصال أو الحظر. هذا يتعارض مع وجود إعدادات الخصوصية في `UserStatusService`.

**الإصلاح المطلوب:**
- أضف `visibility` (EVERYONE/CONTACTS/SELECTED/NOBODY) وallow/deny lists إلى story؛
- اجعل `active(viewerId)` و`viewed/react` تتحقق من audience ومن الحظر؛
- انقل نفس القرار إلى `MediaAccessService`، ولا تجعل مجرد «القصة نشطة» تصريح تنزيل؛
- أضف اختبارات: مستخدم غريب، جهة اتصال، مستخدم محظور، وقصة منتهية.

---

### H-03 — الحظر/الرفض من لوحة الإدارة لا يبطل الوصول القائم
**الخطورة: عالية**

- المسارات `/api/admin/users/{id}/ban` و`reject` وsoft delete في `AdminV2Controller.kt` تغيّر status فقط.
- `RefreshTokenService.revokeAll()` وdevice revocation لا تُستدعى هنا؛ وهما موجودان في `RedApprovalService` فقط.
- فلتر JWT يعيد فحص status في كل HTTP request، وهذا يحد الأثر على HTTP، لكن refresh tokens تبقى محفوظة ويمكن أن تعود عند تغيير الحالة، وWebSocket قائم قد يستمر وفق handler/الحراسة الخاصة به؛ كما يبقى النظام غير موحد السلوك.

**الإصلاح:** جزء من إصلاح C-01: خدمة انتقالات حالة واحدة، revoke atomic للجلسات والأجهزة، وإغلاق/broadcast لسحب جلسات WebSocket عند الحظر.

---

### H-04 — أي ADMIN يستطيع ترقية أي حساب إلى ADMIN أو تخفيض/تعديل حسابات الإدارة
**الخطورة: عالية (تصعيد صلاحيات داخلية)**

- `AdminV2Controller.kt::promoteUser()` يقبل `role` مباشرة عبر `AccountRole.valueOf`.
- لا توجد طبقات مثل SUPER_ADMIN، ولا تحقق من أن الفاعل لا يعدّل نفسه أو مديراً آخر، ولا مبدأ two-person approval، ولا حماية «آخر مدير».
- الواجهة تعلن `MODERATOR` في TypeScript مع أن enum الخادم لا يملك إلا USER وADMIN، ما يعطي خطأ 400 بدلاً من عقد صلاحيات واضح.

**الإصلاح:** RBAC حقيقي (`SUPER_ADMIN`, `ADMIN`, `MODERATOR`, `SUPPORT` بحسب الحاجة) وصلاحية دقيقة لكل فعل. لا تسمح بترقية/تخفيض حساب إداري إلا لمستوى أعلى، مع re-authentication، تدقيق تفصيلي، وربما موافقة ثانية.

---

### H-05 — صورة الملف الشخصي يمكن ربطها بكائن وسائط لا يملكه المستخدم ولا يوجد تنفيذ لإعداد خصوصيتها
**الخطورة: عالية/متوسطة بحسب نموذج الخصوصية**

- `AuthController.kt::updateProfile()` يتحقق من طول `avatarUrl` فقط؛ لا يتحقق أنه object key صالح، موجود، أو يبدأ بـ`users/{caller}/`.
- `PublicDirectoryController` و`ContactService` يعيدان `avatarUrl`، لكن `MediaAccessService` لا يقرر الوصول لصورة الملف الشخصي وفق إعداد `profilePhoto`.
- إعداد `profilePhoto` موجود في Redis في `UserStatusService` لكنه لا يُستهلك في هذه المسارات.

**الأثر:** مراجع معطلة أو إحالات إلى وسائط مستخدم آخر، وتسريب/تعطل صورة حسب المفاتيح المستخدمة.

**الإصلاح:** لا تقبل إلا object key يملكه caller وموجود ونوعه صورة؛ خزّن مرجعاً نوعياً (avatarMediaKey لا URL)، وطبّق سياسة الخصوصية في directory وmedia authorization.

---

## 3) عيوب مؤكدة متوسطة

### M-06 — مسار thumbnail لا يعمل للكائنات المصغرة التي ينشئها بنفسه
- `MediaService.generateThumbnail()` ينتج مفتاحاً بصيغة `thumbs/users/...`.
- `MediaService.metadata()` و`exists()` و`stream()` تستدعي `validateKey()` التي تقبل فقط `^users/...`، وترفض `thumbs/...`.
- `MediaController.thumbnail()` ينفذ `metadata(thumbKey)` بعد التوليد، وبالتالي سيفشل عندما يكون thumb موجوداً فعلاً.

**الإصلاح:** نموذج مفاتيح واحد آمن يدعم `users/...` و`thumbs/users/...` صراحة، مع منع traversal. احذف thumbnail عندما يحذف الأصل، واختبر الصورة والفيديو.

### M-07 — صفحة users تطبق البحث/الفلاتر بعد pagination وتعيد totals غير صحيحة
- `AdminV2Controller.kt::getUsers()` يستدعي `users.findAll(pageable)` ثم يفلتر `allUsers.content` في الذاكرة.
- `totalElements` و`totalPages` يخصان كل المستخدمين لا المجموعة المفلترة؛ وقد تظهر صفحة فارغة رغم وجود نتائج في صفحات أخرى.
- `size` غير محدود، و`sortBy` غير محصور في allowlist؛ القيم الخاطئة تؤدي إلى 500 بدلاً من 400.

**الإصلاح:** Specification/QueryDSL أو repository query ينفذ filter/search/sort في PostgreSQL، مع `page >= 0` و`size 1..100` وallowlist للأعمدة.

### M-08 — pagination غير مقيد في معظم واجهات الإدارة والمحتوى
- أمثلة: audit، reports، backups، polls، events في `AdminV2Controller` و`ContentController` تستخدم `PageRequest.of(page,size)` بلا حد أعلى.
- إدخال `size` ضخم يسبب تحميل ذاكرة/DB غير لازم، وsize/page سالب ينتج استثناءات 500.

**الإصلاح:** DTO/query validator مركزي للصفحات؛ الحد المقترح 100 (أو 200 للمدير فقط) وعودة 400 منظمة.

### M-09 — إعدادات JWT لا تطابق مفاتيح `application.yml`
- `JwtService` يقرأ `red.security.jwt-expiration-ms` غير الموجود، فيستعمل default ساعة.
- `application.yml` يعرّف `red.jwt.access-expiration-minutes: 15` لكنه غير مستخدم.
- `RefreshTokenService` يقرأ `red.security.refresh-expiration-days` بينما الملف يعرّف `red.jwt.refresh-expiration-days`.

**الأثر:** مدة access token الفعلية تختلف عن الموثق والمتوقع (ساعة بدلاً من 15 دقيقة).

**الإصلاح:** مصدر إعداد واحد typed بـ`@ConfigurationProperties` واختبار يثبت 15 دقيقة و30 يوماً حسب البيئة.

### M-10 — refresh token محفوظ في `localStorage`
- `admin_dashboard/src/api.ts`: access token في `sessionStorage` وrefresh token في `localStorage`.
- أي XSS في origin اللوحة يستطيع أخذ refresh token طويل العمر، رغم أن CSP تساعد في تخفيف الخطر.

**الإصلاح:** refresh token في cookie: `HttpOnly; Secure; SameSite=Strict`، مع CSRF protection مناسب لمسار refresh/logout. لا يكفي التعليق الموجود في `SecurityConfig`; يلزم تنفيذ فعلي وخطة انتقال واختبارات.

### M-11 — حضور/جهات الاتصال وإعدادات الخصوصية ليست موصولة كما توحي التعليقات
- `ContactService` لا يعتمد `UserStatusService` ولا يحدث Redis contact sets التي يستخدمها الأخير (`contacts:*`).
- `presenceDetailed()` يعيد `lastSeen` لكل contact ولا يطبق إعداد `lastSeen` للمستهدف.
- لا يظهر في مسار login تحديث موثوق لـ`UserAccount.lastSeen` أيضاً.

**الإصلاح:** خدمة presence/privacy وحيدة، تحديثها transactionally/بأحداث عند accept/remove/block، وتطبيق كل scope قبل إرجاع online/lastSeen/avatar/about.

### M-12 — حد الطلبات العام محلي لكل instance وليس موزعاً، ويفرض على كل `/api/**`
- `SecurityEnhancer` يستخدم `ConcurrentHashMap` لكل عملية؛ في عدة نسخ backend يمكن تجاوز الحد بالتبديل بينها، والذاكرة تنمو مع عناوين IP بلا cleanup.
- يوجد `RateLimitService` على Redis لكنه مستعمل فقط في register/login/recover.

**الإصلاح:** استخدم Redis/Lua أو gateway rate limiting لكل المسارات الحساسة، وحافظ على اختلاف الحدود حسب المسار والهوية؛ لا تعتمد على map محلي في الإنتاج.

### M-13 — `application/octet-stream` يمر من scanner بلا تحقق حقيقي
- `MediaSecurityScanner` يقبل `application/octet-stream` مباشرة، و`MediaService.ALLOWED` يسمح به.
- هو مناسب لحمولات E2EE المشفرة فقط إذا كان مرتبطاً بسياق تشفير واضح، لكنه حالياً يتيح رفع binary اعتباطي حتى 100MB.

**الإصلاح:** افصل مسار encrypted attachment عن upload العام، افرض metadata/protocol، فحص مضاد للبرمجيات الخبيثة/حجر صحي للمحتوى غير المشفر، وسياسات download مثل `Content-Disposition: attachment` للملفات.

---

## 4) ملاحظات منطقية/معمارية وتشغيلية

1. **حذف مستخدم hard delete:** `users.delete(user)` قد يفشل بسبب foreign keys عديدة أو يترك بيانات Mongo/MinIO غير متناسقة. اعتمد سياسة retention/anonymization أو purge workflow متدرج ومؤكد، لا delete مباشر من لوحة.
2. **النسخ الاحتياطي في اللوحة ليس backup engine:** صفحة Backups تعرض سجلات/إرشادات، لكن لا توجد في تدقيق الكود worker واضح ينفذ snapshot متسقاً PostgreSQL + Mongo + MinIO + مفاتيح identity، ولا restore drill موثق ومؤتمت.
3. **الحسابات والتحليلات تعتمد تحميل كل users:** `calculateCurrentAnalytics()` يجلب قوائم كاملة لحالات مستخدمين ويحسب في الذاكرة؛ سيتدهور مع النمو. استخدم `COUNT` وqueries مجمعة وفهارس.
4. **سجلات الإدارة:** `AdminSession` منفصل عن JWT/refresh sessions وفق المسارات المفحوصة؛ إنهاء session إداري قد يغيّر سجل session فقط ولا يضمن إبطال refresh token المقابل. وحّد مفهوم الجلسة أو اربطه بـ refresh-session id/jti.
5. **الـ WebSocket:** المصافحة محمية جيداً بـJWT/ticket، لكن `SecurityConfig` يضع `/ws/**` permitAll ويعتمد كلياً على interceptor. هذا مقبول فقط مع اختبارات تغطي كل endpoint جديد؛ الأفضل policy صريحة وعدم إضافة WS جديد بلا interceptor.
6. **رسائل E2EE:** حفظ server metadata لمحاولة parse `VOICE` من payload في `MessageService.extractVoiceMetadata()` يتعارض مع فرضية payload المشفر في التعليقات. لا تحاول تفسير ciphertext؛ اجعل metadata غير حساسة ومنفصلة ومحددة العقد.
7. **Mongo/SQL ليست transaction واحدة:** تغييرات membership/وسائط/قصص تتوزع بين Mongo وPostgreSQL وMinIO بلا saga/outbox/reconciliation كامل. يلزم jobs إصلاح idempotent وقياس orphan keys.
8. **خادم التطوير الوهمي:** `admin_dashboard/dev-server` يفيد التطوير فقط (SQLite، أرقام وhealth محاكية ومسارات محاكاة). يجب منعه صراحة في أي image/host إنتاجي، وإظهار banner واضح في الواجهة؛ لا يعتبر اختباراً للتفويض أو قاعدة الإنتاج.
9. **NotificationConfig:** قيم SMTP مكتوبة كثوابت (`smtp.gmail.com` إلخ) رغم التعليق أنها Environment. لا ينبغي وجود مزود أو defaults إنتاجية في الكود؛ استخدم properties واستخدم disabled-by-default.
10. **أسرار وملفات توقيع:** `red-app/signing/red-debug.p12` tracked. حتى لو كان debug، لا ينبغي خلط أي signing material قابل للاستعمال مع المستودع؛ استبدله بمفتاح توليدي محلي/CI secret، وتحقق من history إن كان استعمل خارج التطوير.
11. **الأصل وCORS:** `allowedOriginPatterns` مع `allowCredentials=true` يحتاج allowlist إنتاجي دقيق بلا wildcards. وثّق origins منفصلة للـ admin والموبايل وpreview؛ لا تنشر إعداد localhost.

---

## 5) قاعدة البيانات: التقييم

### نقاط سليمة
- Flyway مفعل مع `validate-on-migrate: true` وJPA `ddl-auto: validate`.
- فاحص المخطط المرفق أكد تطابق 25 entities/جداول مفحوصة مع migrations.
- توجد قيود/فهارس جيدة للمفاتيح لمرة واحدة، refresh sessions، media grants، audit، groups، gateway fleet.
- تخزين refresh token hash بدلاً من raw token جيد، واستهلاك one-time prekeys يعتمد SQL ذرياً مع `FOR UPDATE SKIP LOCKED`.

### نقاط تحتاج عملاً
- V1 تاريخي يبدأ بـ`email NOT NULL` ثم تعدله V3؛ الترحيل يعمل لسجل جديد لكن baseline متشعب ويصعّب استعادة/تدقيق schema. أنشئ migration أو schema snapshot نظيفاً موثقاً لبيئة جديدة، مع عدم تعديل migrations المنشورة.
- Migrations كبيرة ومتراكمة (`V26` يجمع عدة features وإصلاح build). ينبغي تجزئة التغييرات المستقبلية حسب bounded context، مع migration test على قاعدة فارغة وقاعدة قديمة.
- لا توجد سياسة retention مثبتة لجداول audit، CDR، health samples، analytics، prekeys المستهلكة، backups history، ولا partitioning للبيانات الزمنية.
- لا يوجد دليل استعادة مثبت باختبار دوري يشمل اتساق مراجع Mongo/SQL/MinIO والمفاتيح.

---

## 6) نتائج التحقق المنفذة

| الأمر/الفحص | النتيجة |
|---|---|
| `npm ci && npm run check && npm run build` في `admin_dashboard` | **نجح**؛ TypeScript وbuild وعقد API والحراس والأدوار نجحت |
| فاحص عقد API | **نجح**: 100 استدعاء لوحة مقابل 288 مسار خادم وفق الفاحص |
| `python3 scripts/check-schema-consistency.py` | **نجح**: 25 تطابق entity/table معلن |
| `python3 scripts/check-kotlin-static.py` | **نجح**: 255 فحصاً ساكناً |
| `python3 scripts/check-android-integrity.py` | **نجح**: 89 فحصاً |
| `python3 scripts/check-infrastructure.py` | **نجح**: 74/74 |
| `./gradlew :backend-server:test` | **لم يمكن تشغيله**: البيئة لا تحتوي Java ولا `JAVA_HOME` |
| Docker Compose / migration runtime / integration live | **لم يمكن تشغيله**: Docker غير متاح في البيئة |

**مهم:** نجاح الفحوص الساكنة لا ينفي العيوب أعلاه؛ مثلاً فاحص عقد API يثبت وجود المسار، لا يثبت أن مسار approve يستخدم المنطق الصحيح أو أن قصة لا تتسرب.

---

## 7) ترتيب التنفيذ المقترح

### P0 — قبل أي اختبار مستخدمين موسع أو نشر
1. دمج كل انتقالات user/device status في `RedApprovalService` أو service جديدة واحدة (C-01/H-03).
2. تصميم وتنفيذ ACL للقصص والوسائط وصور الملف الشخصي (C-02/H-05).
3. إصلاح thumbnail contract وإضافة اختبارات integration له (M-06).
4. إلغاء صلاحية الجلسات والأجهزة وWebSocket عند ban/reject/delete.
5. تقييد privilege management؛ على الأقل امنع تعديل ADMIN والحساب الذاتي حتى يكتمل RBAC (H-04).

### P1 — قبل production
6. نقل refresh token إلى HttpOnly cookie مع CSRF model مُختبر.
7. pagination/filtering SQL مع limits وallowlists.
8. توحيد إعدادات JWT وكتابة tests للمدة الحقيقة.
9. خدمة privacy/presence موحدة ومختبرة.
10. rate-limit موزع، validation موحد، وعقود أخطاء API ثابتة.
11. backup/restore حقيقي واختبار restore دوري.

### P2 — قابلية التوسع والجودة
12. إزالة/حماية dev mock تماماً من الإنتاج.
13. retention/partitioning/data lifecycle.
14. قياس ومراقبة ومؤشرات أمنية قابلة للتنبيه.
15. فصل المكونات والـ migrations الكبيرة وإضافة Testcontainers/CI integration.

---

## الخلاصة
المنصة تحتوي عملاً هندسياً حقيقياً: مصادقة بأجهزة، Flyway، Redis، MinIO، مفاتيح لمرة واحدة، WebSocket tickets، وواجهة إدارة مبنية وقابلة للبناء. لكن وجود المسارات والخدمات لا يكفي؛ هناك **مساران متوازيان لإدارة حالة المستخدم**، والمسار الذي تستخدمه لوحة الإدارة هو المسار الناقص. كما أن خصوصية القصة/الوسائط لم تتحول إلى قرار تفويض فعلي.

لا أنصح بالإطلاق قبل إغلاق بنود P0 وإعادة تشغيل اختبارات backend الفعلية وDocker integration على بيئة تحتوي Java وDocker وقواعد بيانات نظيفة.
