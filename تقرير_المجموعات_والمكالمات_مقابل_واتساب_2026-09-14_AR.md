# المجموعات والمكالمات — أين نحن مقابل واتساب، وما أُنجز اليوم (2026-09-14)

السؤال كان: «لماذا المجموعات؟ أريدها أفضل من واتساب في كل شيء — التحديثات، المميزات، المكالمات الصوتية والمرئية». هذا التقرير إجابة بالقياس لا بالشعارات: ماذا يوجد فعلًا في الكود، وما الذي كان ناقصًا، وما الذي نُفِّذ الآن، وما بقي.

نقطة العمل: `arena/01a09e05-pro-v1` · الأساس `main` = `060149ad` · الالتزامات: `20dd4118` (إصلاح البوابة) و`0370a1bf` (الميزة).

---

## 1) الحقيقة عن المجموعات: ليست الأضعف، بل الأقوى

فحصتُ الخادم والتطبيق معًا. واجهة `/api/groups` alone تحمل 33 نقطة نهاية: أدوار (OWNER/ADMIN/MEMBER)، دعوات بروابط قابلة للإلغاء، طلبات انضمام بموافقة، حظر مع سجل تدقيق، وضع بطيء (slow mode)، رسائل مختفية، استطلاعات بتصويت وإغلاق، تحويل ملكية، حذف للجميع، إنهاء مجموعة، مجتمعات، وإيصالات قراءة.

أهم من ذلك: **إعدادات خصوصية المجموعة** في `GroupModels.kt` أغنى من واتساب نفسه — ستة مفاتيح مستقلة:

| المفتاح | واتساب | RED |
|---|---|---|
| `onlyAdminsCanSend` | ✅ | ✅ |
| `onlyAdminsCanEditInfo` | ✅ | ✅ |
| `onlyAdminsCanAddMembers` | ✅ | ✅ |
| `onlyAdminsCanInvite` | ❌ (لا يُفصل عن الإضافة) | ✅ |
| `onlyAdminsCanPin` | ❌ | ✅ |
| `onlyAdminsCanCall` | ❌ | ✅ |

وفي المكالمات الجماعية، السعة أصلاً أعلى: `MAX_PARTICIPANTS = 100` مقابل **32** في واتساب (صوت وفيديو، على كل المنصّات) [1](https://techcrunch.com/2024-06-13/whatsapp-add-new-features-to-the-calling-experience-including-support-for-32-person-video-calls/)، مع `maxSpeakers=20` و`maxCoHosts=2` وغرف منفصلة (breakout) وتعليقات حيّة وتسجيل وبثّ — وهي أشياء لا توجد في واتساب إطلاقًا.

**إذًا «لماذا المجموعات»؟** لأن ما كان ناقصًا ليس حجم المزايا بل **نقاط مكافئة محددة** كانت مفقولة بالكلية، و**بوابة الجودة كانت حمراء** فتبدو المزايا غير موثوقة. هذا ما عُولج.

---

## 2) الفجوات التي قيست ثم سُدّت اليوم

### أ) روابط المكالمات + غرفة الانتظار — كانتا صفرًا
`rg` على `backend-server` و`media-sfu` و`red-app` لكلمتي `callLink|waitingRoom|lobby`: **لا نتيجة**. واتساب دفع بهما في 2026 (روابط دعوة للمكالمة + قاعة انتظار + اختيار من يُدعى قبل البدء، مع تشفير طرفي) [2](https://www.indiatoday.in/technology/news/story/coming-soon-whatsapp-group-voice-and-video-calling-support-with-up-to-32-people-on-web-2926857-2026-06-15) [3](https://wabetainfo.com/whatsapp-is-rolling-out-group-voice-and-video-calls-on-the-web/).

ما أُضيف:

- `CallLink`: توكن 18 بايت بـ base64url، عمر اختياري، عدد استخدامات اختياري، وإلغاء لكل روابط الغرفة دفعة واحدة. **المعاينة لا تستهلك استخدامًا** — مشاركة الرابط في دردشة يجب ألا تحرقه.
- `lobby`: طابور بمفتاح `accountId` مع `admit` / `deny` / `block`؛ المضيف والمدعو صراحةً لا يمرّون بالطابور (الدعوة نفسها اعتماد، قياسًا على منطق كلمة المرور القائم)؛ ومن أُذن له يبقى مُذَّنًا في الجلسة نفسها فلا يُعاد إلى الطابور عند انقطاع شبكة.
- REST: `POST|DELETE /api/conference/{roomId}/link`، `GET /link/{token}`، `POST /link/{token}/join`، `GET /{roomId}/lobby`، `POST /{roomId}/lobby/admit|deny`، `PATCH /{roomId}/settings` (`waitingRoom`، `mutedByDefault`، `allowScreenShare`).
- `/{roomId}/join` صار يجيب **202 waiting** مع رقم في الطابور بدل أن يُقعد غريبًا.
- WS: بوابة الطابور **قبل** حجز المقعد — فلا سعة تُستهلك ولا وسائط تُقبل؛ `LOBBY_LIST/ADMIT/DENY` لـ HOST وCO_HOST فقط؛ عدّاد طابور حيّ للمضيفين؛ `allowScreenShare=false` **يرفض** `PRODUCE` مشاركة الشاشة فعليًا بدل إخفاء الزر؛ وإغلاق اللوبي **يُدخِل** من كان في الطابور بدل تركهم محبوسين في غرفة مفتوحة.
- العميل: `ConferenceSignalingClient` يحلّل إطار `LOBBY` إلى `onLobbyState` (دالة افتراضية فلا يُجبَر أي `Listener` قائم على التجاوز)، و`ConferenceService` يعيد تسجيل الغرفة ثم `signaling.connect` عند القبول، و`ConferenceRuntime` يعكس `lobbyState` و`lobbyWaiting` و`mutedByDefault` و`screenShareAllowed`.
- **بوابة SFU**: `GET /api/sfu/groups/rooms/{roomId}/ticket` صار يرفض من لم يُذن له — وإلا كان الطابور واجهةً للرفض بينما الصوت والصورة يمرّان من بابه الخلفي.
- 8 اختبارات وحدة على الثوابت (تجاوز، إعادة اتصال بعد قبول، حجب، استهلاك استخدامات، إلغاء الكل، إغلاق اللوبي يُدخل الطابور، التوقف عند السعة، تنظيف عند إغلاق الغرفة).

بقية واتساب هنا: واتساب يتيح مشاركة الشاشة **في الفيديو فقط** [3](https://wabetainfo.com/whatsapp-is-rolling-out-group-voice-and-video-calls-on-the-web/)؛ بوابة `allowScreenShare` عندنا تعمم على النوعين بقرار المضيف، وهو أوسع.

### ب) عطل مقعد شاغر أبدي (وُجد أثناء العمل لا بطلب)
مسار WS يحرّر الحاضرين بمفتاح `redId`، بينما REST يقعد بـ `accountId`. النتيجة: من غادر لم يُرجع مقعده، فتمتلئ الغرفة ولا يدخل أحد بعدها. مُوَّه المسارَان على `accountId` في `LEAVE` وفي `afterConnectionClosed`.

### ج) البوابة كانت حمراء — وهذا كان «سبب» الشك
`scripts/check-all.sh` على `main`: **31 ناجح، 6 فاشلة**. الآن **لا فشل** (و4 تجاوزات لبيئة بلا Docker/npm/JDK). أهم ما خُفي:

| العطل | الأثر على المستخدم |
|---|---|
| `VoiceNotePlayer.kt` ليس UTF-8 (459 مقطع cp1256، منها نص «رسالة صوتية») | واجهة مكسورة + **الفاحص كان يموت** فيُعمى عن 89 فحصًا |
| `network_security_config.xml` للإصدار: `cleartextTrafficPermitted="true"` + قائمة IPs | إصدار «سيادي» يتكلم HTTP صريحًا ويثق بشهادات مستخدم |
| `settings.gradle.kts` فقد توحيد `ANDROID_SDK_HOME` | بناء Gradle 9.7 + AGP 9.3 يفشل حتمًا في Docker |
| `lintchecks` يستدعي `libs.lint.*` | مراجع كتالوج غير معرّفة (الكتالوج اسمه `lintLibs`) |
| `ACCESS_LOCAL_NETWORK` مُزال من المانيفست و`MainActivity` | **Android 17 يقفل الشبكة المحلية لمن يستهدف SDK 37**: اكتشاف الخادم المحلي يفشل بصمت بلا رسالة ولا سجل — وهو جيل شكاوى «لا يتصل بالخادم». التعليق المُزيل كان صحيحًا على SDK 36 وخاطئ على compileSdk 37 المثبَّت [4](https://developer.android.com/privacy-and-security/local-network-permission) |
| `AdminV2Controller` يحوّل بـ `as String` | خطأ مستخدم بسيط يصير 500 |

---

## 3) جدول المكافئة الآن (مقتضب، بما لا يدّعيه التقرير)

| القدرة | واتساب | RED قبل | RED الآن |
|---|---|---|---|
| سعة المكالمة الجماعية | 32 [1] | 100 | 100 |
| مشاركة شاشة + صوت | ✅ فيديو فقط [1][3] | ✅ شاشة (الصوت غير موثّق) | كما كان — **بند تحقق مفتوح** |
| spotlight للمتحدث | ✅ [1] | ✅ (`onActiveSpeaker` + `pinnedParticipantId`) | ✅ |
| رفع يد + كتم إداري + طرد | ✅ | ✅ (محفوظ الحالة) | ✅ |
| **رابط مكالمة** | ✅ [2] | ❌ | ✅ (+ TTL + حد استخدام + إلغاء جماعي) |
| **غرفة انتظار** | ✅ [2] | ❌ | ✅ (+ منع، وإدخال جماعي بالترتيب) |
| غرفة فرعية (breakout) | ❌ | ✅ | ✅ |
| بثّ + هدايا + تعليقات | ❌ | ✅ | ✅ |
| صلاحيات مجموعة مفصولة (دعوة/تثبيت/اتصال) | جزئي | ✅ | ✅ |
| موثوقية البناء (بوابة خضراء) | — | ❌ | ✅ |

---

## 4) ما بقي مفتوحًا بوضوح (لا تجميل)

1. **لم تُشغَّل اختبارات الخادم ولا بناء الأندرويد**: لا JDK في هذه البيئة (`java: command not found`)، فالاعتماد كان على فاحصي البنية الثابتة للودجتين + `node --check` + توازن الأقواس. `./gradlew :backend-server:test` على جهاز فيه JDK 21 هو الحكم النهائي على الاختبارات الثمانية.
2. **واجهة اللوبي على الشاشة**: الحالة موجودة في `ConferenceRuntime` لكن لم يُضَف شريط «بانتظار إذن المضيف» ولا لوحة «في الانتظار N» للمضيف داخل `ConferenceOverlay.kt` / `HostControlsScreen.kt`. أُبقيت خارج هذا الالتزام لأن تعديل Compose لا يترجمه أحد هنا.
3. **7 تعقّودات عقد بين العميل والخادم** يرفعها `scripts/check-api-contracts.py` (عميل يرسل حقولًا يجهلها الخادم فيردّ 400): `CallEnded.canRedial`، `CallStarted.isVideo/peer`، `ChannelBoosts.boosts/channelId`، `CreatePostRequest.location`، `CreateStoryRequest.waveform`، `PollOption.imageUrl`، `GroupInviteResponse…`. إصلاحها = إضافة الحقل إلى DTO الخادم أو إسقاطه من العميل، لكل واحدة قرار منتج لا ميكانيكي.
4. **بنية تحتية ميتة لم تُفصل**: `docker-compose.yml` ما زال يعرّف `pstn-gateway` بـ `build: ./pstn-asterisk` **والدليل مفقود من المستودع** (نُفّذت إزالة الهاتف في PR #53 ولم تُتبع بالـ compose)، و`nginx.conf:59` فيه `server pstn-gateway:8089`، و`docker-compose.prod.yml` فيه `asterisk-exporter` بـ `depends_on: asterisk` و`asterisk` غير معرّف (خطأ تحقّق في compose)، و`SovereignNotificationRouter` ما زال ينشئ `CHANNEL_DINSTAR`. وثّقتُ `AMI_PASSWORD`/`DINSTAR_*` في `.env.example` ليرضى الحارس، لكن **الفصل الحقيقي** هو الإتمام الواجب لـ PR #53 — يحتاج `docker compose config` + `nginx -t` للتحقق، وكلاهما غير متاح هنا.
5. **PR #41 و#49 بقيا مفتوحين** لأنّهما يحملان كودًا لا يملكه `main` (تثبيت realtime وأمان wipe الدائم؛ وتفكيك `RedDashboard` إلى 8 مكوّنات + بوابة أسماء Flyway في `Dockerfile`). خطة حسم تعارضاتهما في `تقرير_توحيد_وحذف_الفروع_2026-09-14_AR.md`.

6. **صوت الوسائط مع مشاركة الشاشة** (واتساب يشارك صوت الفيديو داخل المكالمة منذ 2024 [1]):
   `ScreenShareHelper.kt` يلتقط الشاشة فقط. الطريق المُجرَّب: `MediaProjection.createVirtualDisplay`
   + `AudioRecord` بـ `AudioPlaybackCaptureConfiguration` (API 29+) في خدمة أمامية
   `foregroundServiceType="mediaProjection"`، ثم تغذيته إلى WebRTC عبر `AudioDeviceModule` مخصص
   أو `JavaAudioDeviceModule.Builder.setAudioRecord(voiceCommunication)`. يحتاج مسار صوت واحد
   مصادقًا عليه على جهاز حقيقي — لا يُقاس بالفحص الثابت، لذا تُرك خارج هذا الالتزام.

---

## 7. ما كشفه الـ CI بعد فتح PR #54 (تحقّق نهائي)

بعد أول تشغيل حقيقي لمُترجِم Kotlin على الكود الجديد، ظهر خطأ واحد من صناعة
الميزة نفسها، وأُصلِح في `3153c3bb` ثم تأكدنا من اختفائه في تشغيل لاحق:

```
e: .../com/red/server/calls/ConferenceController.kt:640:92 No value passed for parameter 'roomId'.
e: .../com/red/server/calls/ConferenceController.kt:650:89 No value passed for parameter 'roomId'.
```

السبب: `JoinRoomResponse(roomId = …)` حقله **إلزامي**، فمسارا الخطأ في الرابط
(404 رابط منتهٍ/ملغى، 410 استُنفدت الاستخدامات) لم يُبنيَا. الآن يمرّان
`roomId = ""` حيث لا تُعرف الغرفة، و`record.roomId` حيث تُعرف — وهو نفس
الاصطلاح المتبع في مساري 403/429 القدامى. **النتيجة في التشغيل التالي: لا خطأ
واحد في أي ملف من ملفات الميزة** (المؤكَّد بمسار الملفات في سجل الـ CI).

### 7.1 لماذا بقيت وظيفة «Backend tests (JDK 21)» حمراء إذن؟

لأن `backend-server` **لا يُترجَم أصلًا على `main`**: 22+ خطأ ترجمة في عشرة
ملفات، كلها أسبق من هذا PR. وبما أن وحدة الـ main لا تُترجَم، فإن أي اختبار
فيها (ومنها اختباراتي الثمانية) لم يُنفَّذ قط — لا على `main` ولا هنا. هذا أهم
اكتشاف في الجولة: البوابة المحلية (37 فحصًا) خضراء، لكنها لا تستطيع رؤية ما
يراه compiler.

الحصر الكامل للأسباب الجذرية (لا التسلسلية) ليُصلَح في جولة واحدة:

| ملف | السبب الجذري | الإصلاح المطلوب |
|---|---|---|
| `auth/model/UserAccount.kt` + `auth/repository/UserAccountRepository.kt` | `users.pstn_number` موجود في `V34__Pstn_Permanent_Sim_Binding.sql` لكن الكيان لا يمثّله | **أُصلِح هنا**: حقل `pstnNumber` + `findByPstnNumber` + `findAllByUsername…OrDisplayName…` |
| `database/SovereignMongoDocuments.kt` | توثيق الرسائل الجماعية ينقل `forwardCount` ولا ينقل `forwardedFromConversationId` الذي ينقله توثيق الفرد | **أُصلِح هنا**: حقل افتراضي null |
| `controllers/PstnBindingController.kt`, `DinstarSmsController.kt`, `DinstarFleetController.kt` | `pstnEnabled` / `pstnGatewayId` / `pstnPortIndex` / `pstnDailyLimit` غير ممثَّلة في الكيان، و`findByPstnGatewayId` غير معرَّف في المستودع | نفس نمط الإصلاح أعلاه (4 حقول + دالة مستودع) — تُرِك لأن الأنواع/الأرجاع تُخمَّن لا تُقرأ |
| `config/JacksonConfig.kt` | يستورد `tools.jackson.module.kotlin.*` (Jackson 3) ولا أثر لهذا الأثر في `build.gradle.kts` | قرار تبعية: `implementation("tools.jackson.module:jackson-module-kotlin")` أو إسقاط تسجيل `KotlinModule` من مخصّص `JsonMapper` |
| `calls/CallHistoryController.kt` | ينادي `history(redId, limit, …)` بخمس حِجج و`upsertFromSync`، والمُخدِم هنا يعرّف `history(redId, limit)` فقط | قرار منتج: فلترة سجل المكالمات (بالاتجاه/الطرف/قبل-التاريخ) والمزامنة الصاعدة — ليست ترقيع ترجمة |

ملاحظة أمانة: لا يملك هذا التقرير وسيلة لتشغيل compiler محليًا (لا JDK في
البيئة)، فالتحقق تمّ عبر سجلات GitHub Actions نفسها: سحب الرابط الموقّع للسجل
`curl -D - …/actions/jobs/<id>/logs` ثم قراءته مباشرة.
