# تقرير التدقيق التقني الشامل لمنصة YOUNES / pro_v1

**تاريخ التدقيق:** 19 أغسطس 2026  
**حالة المستودع:** المراجعة على `main` بعد دمج طلبي الدمج #47 و#48.  
**نطاق التدقيق:** تطبيق Android، خادم Spring Boot، قواعد PostgreSQL وMongoDB وRedis، طبقة WebSocket، SFU وWebRTC، Asterisk وDINSTAR، لوحة الإدارة، ملفات Compose، والعقود والاختبارات.

> **مهم:** هذا تدقيق ثابت مدعوم ببناء واختبارات محلية. لا يثبت تشغيل DINSTAR أو Asterisk أو SMS أو WebRTC على شبكة حية، لأن بيئة التدقيق لا تحتوي أجهزة البوابة أو بيانات اعتماد اختبارية مصرحًا بها.

## الملخص التنفيذي

المشروع يمتلك قاعدة تقنية واسعة ومتكاملة نسبيًا؛ يوجد تطبيق Android حديث قائم على Kotlin وCompose، وخادم Spring Boot مع PostgreSQL وMongoDB وRedis، وتشفير محلي للجلسات، ومسارات للوسائط والحالات والمجموعات والمكالمات والبث والهاتف اليمني ولوحة الإدارة. كما أن بوابات البناء الحالية سليمة محليًا.

### تحديث حالة المعالجة — 19 أغسطس 2026

أغلقت دفعات التحصين على فرع `chore/platform-rebuild-baseline` جميع عناصر **P0** و**P1** الواردة في هذا التقرير: قنوات DINSTAR الإدارية أصبحت مقيدة بدور ADMIN، واستطلاعها تحول إلى لقطة مركزية، وحذف تخزين كلمة المرور المؤجلة، وبُني Outbox دائم ومشفّر، ونُقلت حجوزات ومكالمات PSTN النشطة إلى PostgreSQL، وأزيلت بيانات DINSTAR الافتراضية، ووحد عقد الأخطاء، وصححت لوحة الإدارة التمييز بين الصفر وتعذر المصدر. أضيفت كذلك حماية سجل الشبكة، وتحقق CSRF لمسارات الكوكيز، وفصل بيانات الحساب عند الخروج، وتدفق طلبات الصداقة بالـRED ID أو username.

هذا لا يثبت جاهزية إنتاج كاملة: ما زالت اختبارات تكامل WebRTC/SFU والعتاد PSTN/DINSTAR/SMS والنسخ الاحتياطي تحتاج بيئة حية مصرحًا بها، كما أن Compose الكامل لم يُشغل في بيئة التدقيق لغياب Docker وذاكرة كافية.

| مؤشر تدقيق | النتيجة |
|---|---:|
| ملفات Kotlin الإنتاجية في الخادم | 164 |
| ملفات Kotlin الإنتاجية في Android | 157 |
| ملفات اختبار الخادم | 45 |
| ملفات اختبار Android | 32 |
| اختبارات الخادم المنفذة | 262، بلا إخفاقات |
| اختبارات Android المنفذة | 205، بلا إخفاقات |
| فحص لوحة الإدارة | ناجح: العقد، الحراس، الأدوار وTypeScript |
| فحص SFU | ناجح نحويًا فقط (`node --check`) |

## ما تم فحصه

تمت مراجعة خط البناء الفعلي، وسياسة Spring Security، معترض JWT لمصافحة WebSocket، معالج DINSTAR الحي، دورة PSTN، إعدادات التطبيقات وقواعد البيانات، تخزين Android المحلي، عميل الشبكة، منطق الإرسال غير المتصل، أحجام ملفات الواجهة، ملفات Compose، وتعريفات SFU ولوحة الإدارة. كما تم تشغيل بوابات الجودة المحلية وتفحّص سجلات JUnit. لا يدّعي التقرير فحصًا يدويًا حرفيًا لكل سطر من آلاف ملفات legacy والأمثلة خارج رسم المنتج؛ المنتج الفعلي في البناء يتكون من `red-app` و`shared-proto` في Android، إضافة إلى مكونات الخادم والخدمات المساندة المحددة في Compose.

## النتائج ذات الأولوية القصوى

| المعرّف | الخطورة | الدليل | الأثر | المعالجة المطلوبة | اختبار القبول |
|---|---|---|---|---|---|
| **P0-01** | حرجة | `DinstarWebSocketHandler.kt` يقبل كل جلسة مصادقة ويبث SMS وUSSD وCDR وحالة المنافذ، بينما معترض المصافحة لا يفرض دور ADMIN لهذه القناة. | يمكن لمستخدم عادي مصادق قراءة بيانات بوابة إدارية ورسائل قصيرة وسجلات مكالمات. | فرض ADMIN في `JwtHandshakeInterceptor` لمسار `/ws/dinstar` أو في `afterConnectionEstablished` قبل إضافة الجلسة؛ ثم توحيد سياسة الدور مع REST. | مستخدم USER يحصل على 403/إغلاق 4403؛ مستخدم ADMIN يتلقى الحالة؛ لا تخرج أي رسالة أو CDR إلى USER. |
| **P0-02** | حرجة | كل اتصال إلى `/ws/dinstar` ينشئ جدولة كل 5 ثوانٍ تستعلم كل البوابات؛ و`DinstarHeartbeatService` يستدعي بثًا يعيد الاستعلام عن جميع البوابات داخل حلقة البوابات. | حمل استعلامات متضاعف، احتمال إنهاك البوابة أو الشبكة، وتأخر واجهة الإدارة مع تعدد المشرفين أو البوابات. | إنشاء `DinstarStatusSnapshotService` مركزي يجلب لقطة واحدة لكل interval، يخزنها بوقت ومصدر، ويبث اللقطة لكل المشتركين؛ إضافة backoff وtimeout وعدادات. | مع 10 مشتركين و3 بوابات يبقى عدد طلبات العتاد متناسبًا مع البوابات لا مع الجلسات؛ قياس p95 وزمن stale. |
| **P0-03** | حرجة | `TokenStore` يحفظ `pending_password` على القرص لاستعادة دخول مؤجل. | كلمة مرور أصلية محفوظة محليًا رغم التشفير، ما يوسع سطح الهجوم بلا حاجة. | حذف حفظ كلمة المرور؛ حفظ اسم المستخدم فقط أو challenge قصير العمر؛ طلب كلمة المرور مجددًا بعد انقطاع الاتصال. | لا توجد مفاتيح `pending_password` في SharedPreferences؛ تعطل واستئناف التطبيق لا يعيد كلمة المرور. |

## النتائج عالية الأولوية

| المعرّف | الخطورة | الدليل | الأثر | المعالجة المطلوبة | اختبار القبول |
|---|---|---|---|---|---|
| **P1-01** | عالية | `RedConnectionService` يحتفظ بالرسائل غير المرسلة في `ConcurrentLinkedQueue` داخل الذاكرة؛ WorkManager موجود كاعتماد لكن لا يظهر Worker أو Outbox دائم. | قد تضيع الرسائل والمرفقات عند قتل العملية أو إعادة التشغيل أو انقطاع الشبكة الطويل. | Outbox مشفر في Room بحالات `QUEUED/SENDING/ACKED/FAILED`، idempotency/client ID، وWorker فريد بقيود شبكة وbackoff. | أرسل دون شبكة ثم اقتل التطبيق وأعد تشغيله؛ تُرسل مرة واحدة عند عودة الشبكة ويظهر ACK. |
| **P1-02** | عالية | تخصيص PSTN وheartbeat يحتفظان بحالة المكالمة في خرائط JVM محلية. | فقد علاقة `callId → gateway/port` بعد restart أو مع التوسع الأفقي؛ خطر منفذ عالق أو تحرير غير مكتمل. | تخزين تخصيص المكالمة في Redis أو PostgreSQL بTTL وgateway/port/owner/correlation ID؛ ربطه بأحداث AMI؛ سياسة إنهاء قابلة للضبط. | restart أثناء مكالمة ثم hangup/AMI event؛ يحرر المنفذ الصحيح مرة واحدة فقط. |
| **P1-03** | عالية | `application.yml` يحتوي default لـDINSTAR username/password بقيمة `admin`، مع عنوان افتراضي. | تمكين غير مقصود للبوابة ببيانات معروفة، وصعوبة تمييز الخطأ التشغيلي من الخطأ الأمني. | إزالة قيمة password الافتراضية؛ تحقق fail-fast عندما `DINSTAR_ENABLED=true` بلا أسرار صريحة قوية؛ منع تسجيل challenge أو العنوان في الاستجابة. | يفشل الإقلاع الآمن إن فُعل DINSTAR بلا credentials؛ لا تظهر الأسرار أو host في API/logs العامة. |
| **P1-04** | عالية | `AuthExceptionHandler` يعيد نصوص `IllegalArgumentException` و`IllegalStateException` و`NoSuchElementException` مباشرة. | تسريب بنية داخلية، رسائل Asterisk أو DINSTAR، مسارات أو تحديات HTTP، وتناقض عقود الأخطاء. | نموذج `ApiError(code, diagnosticId, retryAfter?)` ثابت؛ سجل داخلي مقترن بـdiagnosticId؛ قوائم رسائل آمنة لكل مجال. | أي خطأ عتاد/شبكة يعيد code عام فقط؛ السجل الداخلي يحمل السبب؛ Android يعرض ترجمة مستقرة. |
| **P1-05** | عالية | `AdminDataOverviewController` يمتص فشل PostgreSQL/Mongo/Redis ويعرض `0`. | لوحة الإدارة قد تظهر أصفارًا مضللة أثناء انهيار مصدر بيانات، ما يضعف الاستجابة للحوادث. | إرجاع قيمة مع `available/error/observedAt` لكل مصدر؛ سجل تحذير ومقياس صحة؛ عدم تحويل failure إلى صفر. | أوقف Redis أو Mongo؛ اللوحة تعرض "غير متاح" وليس صفرًا. |

## النتائج المتوسطة

| المعرّف | الخطورة | الدليل | الأثر | التحسين المقترح |
|---|---|---|---|---|
| **P2-01** | متوسطة | `RedDashboard.kt` يبلغ 3684 سطرًا ويجمع التنقل وحالات المحادثات والمجموعات والمنشورات والحالات والمكالمات. | صعوبة الاختبار، خطر تداخل الحالات، وارتفاع كلفة الصيانة وإعادة التركيب. | تقسيم الوجهات إلى modules أو ملفات feature مستقلة؛ state holders لكل route؛ أحداث UI أحادية الاتجاه. |
| **P2-02** | متوسطة | `LoggingInterceptor` يكتب الرؤوس والجسم كاملين عند debug، ويستهلك `ResponseBody` عند قراءته. | احتمال تسجيل Authorization/رسائل؛ واحتمال إتلاف جسم الاستجابة للعميل عند الاستعمال. | إزالة الأجسام افتراضيًا، حجب Authorization/Cookie/رموز الأجهزة، وإعادة بناء response body إن لزم التشخيص. |
| **P2-03** | متوسطة | migrations تسمي تفرعات مثل `V25_1` و`V26_1`. | قابلية قراءة أقل واحتمال أخطاء تسلسل في فرق متعددة. | قاعدة تسمية Flyway موحدة وإلزام فحص CI يمنع تفرعات غير معيارية أو ترتيبًا مكررًا. |
| **P2-04** | متوسطة | `media-sfu` يملك `node --check` فقط، بلا E2E أو اختبار mediasoup حقيقي. | لا إثبات لتبادل ICE/DTLS، الاستئناف، الصوت/الفيديو الجماعي أو تحمل الانقطاع. | مجموعة تكامل حاوية: SFU وbackend وTURN وعميلان headless وإحصاءات جودة. |
| **P2-05** | متوسطة | لوحة الإدارة تملك فحوص عقد ودور ممتازة، بلا اختبارات مكونات أو E2E ظاهرة. | لا تحقق من سلوك الأزرار المدمرة أو تكرار النقر أو حالات الشبكة الفعلية. | Playwright لسيناريوهات RBAC، الإشراف، الحظر، الاستعادة، الفشل وإعادة المحاولة. |
| **P2-06** | متوسطة | CSRF معطل عالميًا في Spring بينما يوجد مسار مستقبلي لرموز refresh في cookies. | خطر انتقال غير منضبط عند إدخال HttpOnly cookies أو مسار cookie جديد. | فصل سياسة Bearer عن cookie endpoints، وتفعيل CSRF أو double-submit للمسارات cookie، واختبارات origin/CSRF. |
| **P2-07** | متوسطة | أجزاء legacy وSignal والأمثلة كثيرة وخارج رسم بناء المنتج. | صعوبة فهم الملكية، تضخم المسح الأمني واحتمال تعديل مسار غير منتج. | وثيقة ownership واضحة، aliases للمسارات الفعلية، وعزل legacy في subtree أو مستودع مرجعي. |

## المزايا الموجودة ولكن يلزم إثباتها تشغيليًا

المنصة تحتوي مسارات برمجية للمكالمات الفردية والجماعية، المؤتمر، البث، TURN، PSTN، DINSTAR، الرسائل القصيرة، الإشراف، التشفير، الحالات والمجموعات. وجود هذه المسارات لا يعادل نجاحًا تشغيليًا. المزايا التالية تحتاج بيئة تكامل قبل اعتبارها "مفعلة بالكامل":

| المجال | ما يلزم إثباته |
|---|---|
| WebRTC وSFU | عميلان أو أكثر خلف شبكات مختلفة، TURN relay، صوت وفيديو، تبديل شبكة، استئناف، قياس packet loss وjitter وCPU. |
| البث والمؤتمرات | دخول/خروج متعدد المشاركين، تبديل ناشر، إعادة اتصال، صلاحيات المضيف، حد السعة، وتنظيف الموارد. |
| DINSTAR وPSTN | بوابة اختبار مصرح بها، طراز firmware محدد، اتصال AMI، SIP outbound، تحرير منفذ، CDR، وfailover. |
| SMS وUSSD | إرسال واستقبال، encoding عربي، delivery report، idempotency، حدود سرعة، وفشل الشريحة/الشبكة. |
| النسخ الاحتياطي والاستعادة | نسخة PostgreSQL/Mongo/MinIO، تجربة restore مع RPO/RTO، وتشفير مفاتيح النسخ. |
| الإشعارات | FCM على جهاز فعلي، خلفية/قتل عملية، إذن الإشعارات، وفتح deep link الصحيح. |

## خارطة التطوير المقترحة

### الموجة الأولى: إغلاق المخاطر الحرجة خلال دورة قصيرة

ابدأ بتقييد `/ws/dinstar` إلى ADMIN، وإعادة تصميم الاستطلاع إلى لقطة مركزية مشتركة، وحذف تخزين كلمة المرور المؤجلة. هذه تغييرات صغيرة نسبيًا وعالية العائد، ويجب إرفاقها باختبارات رفض/قبول دور، وقياس عدد طلبات البوابة، وفحص تخزين Android.

### الموجة الثانية: ضمان الاستمرارية والموثوقية

أنشئ Outbox مشفرًا للرسائل والمرفقات يعتمد على Room وWorkManager، ثم انقل تخصيص PSTN قصير الأجل من الذاكرة إلى Redis أو PostgreSQL. أضف correlation ID موحدًا بين Android وbackend وAsterisk وCDR، وأسس سياسة restart/failover قابلة للاختبار.

### الموجة الثالثة: توحيد العقود والرصد

استبدل رسائل الاستثناء المكشوفة بـerror codes ثابتة، وأضف request ID وdiagnostic ID، ثم حسّن لوحة العمليات كي تفرق بين `0` و`source unavailable`. أضف OpenAPI أو مصدر عقود مولدًا مشتركًا لمنع اختلاف Android ولوحة الإدارة والخادم.

### الموجة الرابعة: تشغيل الوسائط والهاتف اليمني على بيئة حية

جهز compose أو بيئة Kubernetes تجريبية فيها PostgreSQL وMongoDB وRedis وMinIO وSFU وTURN وAsterisk وDINSTAR اختبارية. ثبّت الأسرار في مدير أسرار، لا Git. نفذ اختبارات end-to-end لسيناريوهات المستخدم الحقيقي، ثم اختبارات ضغط وعزل شبكة وفشل بوابة.

### الموجة الخامسة: إعادة هيكلة الواجهة والجودة المستمرة

قسّم `RedDashboard` تدريجيًا دون إعادة كتابة شاملة، وأضف E2E للوحة الإدارة، ولقطات Compose للواجهات المهمة، وcoverage report في CI. ينبغي أن تفشل CI عند تراجع الاختبارات أو ظهور تسريب للأسرار أو فتح قناة إدارية لمستخدم عادي.

## خطة قبول قبل إطلاق إنتاجي

| البوابة | معيار القبول |
|---|---|
| الأمن | مراجعة مستقلة لـRBAC وWebSocket وCSRF والأسرار والسجلات؛ لا بيانات DINSTAR إلى USER. |
| المراسلة | Outbox دائم، ACK idempotent، test kill/restart/offline ونجاح مرة واحدة. |
| الوسائط | اتصال صوت وفيديو عبر شبكات مختلفة باستخدام TURN، اختبارات reconnect وجماعي وبث. |
| الهاتف اليمني | DINSTAR/Asterisk/SMS على جهاز اختبار، CDR وتسليم SMS وتحرير منفذ وفشل حتمي موثق. |
| البيانات | restore ناجح موثق للبيانات والوسائط، وفحوص migration من نسخة سابقة. |
| الإدارة | E2E للأدوار والعمليات المدمرة، وقيم health صحيحة عند توقف مصدر. |
| الأداء | حمل متزامن للرسائل والـWebSocket والوسائط، وSLIs واضحة للزمن والخطأ والموارد. |

## نتيجة التدقيق

**التوصية:** لا توسع نطاق الميزات حاليًا. عالج عناصر P0 وP1 أولًا، ثم نفذ التكامل الحي للوسائط والهاتف اليمني. بعدها يمكن متابعة تحسينات تجربة المستخدم، فصل الواجهة، والميزات التنافسية بثقة أعلى. البنية الحالية قادرة على التطور، لكن الوصول إلى منصة تنافسية وموثوقة يتطلب تحويل ميزات الواجهة والعقود الموجودة إلى تدفقات دائمة، قابلة للرصد، ومثبتة على أجهزة وشبكات حقيقية.

## المراجع

[1] [DINSTAR GSM Gateway HTTP API and tooling](https://www.dinstar.com/tools/)

[2] [DINSTAR SMS solution capabilities](https://www.dinstar.com/solutions/SMS-solution/)

[3] [DINSTAR UC2000-VG GSM/3G/4G VoIP Gateway](https://www.dinstar.com/GSM-3G-LTE-voip-gateway/32-ports/)

---

**ملفات الأدلة المحلية:** `.audit/inventory.md`، `.audit/backend_static.md`، `.audit/backend_reliability.md`، `.audit/data_layer.md`، `.audit/android_static.md`، `.audit/android_offline.md`، `.audit/android_security_combined.md`، `.audit/media_ops_admin.md`، `.audit/test_metrics.md`، و`.audit/quality_results.tsv`.
