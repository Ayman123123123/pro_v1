# تدقيق الاعتماديات الشامل — 2026-09-15

> النطاق: **كل مكتبة في كل مكون** — `red-app` (Android)، `backend-server` (Spring Boot)،
> `media-sfu` (Node)، `admin_dashboard` (React)، + كتالوجات Gradle الأربعة و`build-logic`.
> المنهج: جرد آلي (42 إدخالاً ميتاً اكتُشفت بمسح كل `*.gradle.kts`) + بحث ويب موثق + **CI هو الحَكَم**.

---

## 1) التعارض الحقيقي الكبير: حزمة Jackson مزدوجة في الخادم — أُصلح جذرياً

**قبل:** Spring Boot 4 يدير Jackson 3 (`tools.jackson`) لـ MVC، بينما 13 ملفاً من الكود
اليدوي استخدم Jackson 2 (`com.fasterxml`) بإصدارات مثبتة يدوياً 2.19.2 — **حزمتان
كاملتان على نفس الـ classpath** (تضخم + خطر سلوك متباين في التسلسل).

**بعد:** هجرة كاملة إلى Jackson 3 (12 ملفاً): `tools.jackson.databind.ObjectMapper` +
`jacksonObjectMapper()` في الاختبارات + `asString()` الجديدة (41 موقعاً بدل `asText()`
المهجورة) + `JacksonConfig` صار tools فقط + **حذف 3 تثبيتات fasterxml يدوية**.
ملاحظات الهجرة الموثقة من المصدر (cowtowncoder/javadoc): تعليقات `com.fasterxml.jackson.annotation`
**تبقى صالحة** في Jackson 3 عمداً (توافق 2↔3)، و`asText()` بقيت deprecated لا محذوفة.

## 2) رفع إصدارات الخادم (مفكوك القفل — تحقق CI مباشر)

| المكتبة | قبل | بعد | السبب (مصدر) |
|---|---|---|---|
| `bcprov-jdk18on` | 1.79 (أكتوبر 2024) | **1.86** (11 سبتمبر 2026) | إصلاح **CVE-2026-71887** (ربط مفتاح توقيع ثانوي بمفتاح مهاجم) + ~7 إصدارات إصلاحات [deps.dev/bc-java] |
| `jsoup` | 1.18.3 (ديسمبر 2024) | **1.22.2** (أبريل 2026) | يحلل محتوى Open Graph غير موثوق — 9 إصدارات تصحيحات تحليل [deps.dev] |
| `jjwt` (api/impl/jackson) | 0.12.6 | **0.13.0** | أحدث إصدار رسمي [javadoc.io] |
| `kotlinx-coroutines-core` | 1.10.2 | **1.11.0** (مايو 2026) | إصلاحات flowOn/ThreadContextElement + تحسينات جرة [libhunt/changelog] |

ملاحظة: `jjwt-jackson` يجلب fasterxml-Databind متعدية لاستخدام **الداخلي الخاص به فقط** —
معزولة بعد هجرتنا (كودنا لم يعد يلمس Jackson 2 إطلاقاً).

## 3) كتالوج Android: 38 إدخالاً ميتاً حُذفت (بقايا فورك Signal)

مسح آلي لكل الرموز ضد كل ملفات `*.gradle.kts`: هذه الإدخالات **صفر مراجع** في شجرة
البناء كلها — لم تكن تُحلّ أصلاً (لا أثر على APK ولا على verification-metadata):

`mobilecoin` • `signal-ringrtc` • `signal-aesgcmprovider` • `square-leakcanary` •
`leolin-shortcutbadger` • `materialish-progress` • `roundedimageview` •
`subsampling-scale-image-view` • `kotlinx-coroutines-rx3` • `androidx-compose-rxjava3` •
`rxdogtag` • `google-ez-vcard` • `google-flexbox` • `kotlinx-collections-immutable` •
`androidx-navigation-{fragment,ui}-ktx` • `androidx-navigation-safe-args-gradle-plugin` •
`androidx-room-ktx` (مدمج في runtime منذ 2.7) • `androidx-profileinstaller` •
`androidx-recyclerview` • `androidx-{autofill,sharetarget,gridlayout,preference}` •
`androidx-legacy-{support,preference}` • `androidx-asynclayoutinflater{,-appcompat}` •
`androidx-concurrent-futures` • `androidx-fragment-testing` • `androidx-window-java` •
`androidx-camera-extensions` • `androidx-lifecycle-{livedata-ktx,reactivestreams-ktx,viewmodel-savedstate}` •
`androidx-compose-runtime-livedata` • `androidx-benchmark-gradle-plugin` • `protobuf-gradle-plugin`
(المخطوط مباشرة في shared-proto).

> **حذف إدخالات غير محلولة لا يغيّر القفل** — `verification-metadata.xml` لم يحتج إعادة توليد،
> وCI أخضر يؤكد. (بقايا موجودة في ملفات وحدات خارج مخطط البناء مثل `lib/`, `demo/` تُركت كما هي عمداً.)

## 4) القفل الحديدي: `verification-metadata.xml` (2021 مدخلة SHA-256)

بناء Android **مقفول بالكامل بالبصمات** — أي رفع إصدار فيه = فشل حلّ فوري إلا بعد
إعادة التوليد. لذلك: **لم يُرفع أي إصدار Android**. خطة المالك الجاهزة:

```
# بعد تعديل gradle/libs.versions.toml حسب الجدول أدناه:
./gradlew --write-verification-metadata sha256 :app:assembleDebug --rerun-tasks
git add gradle/verification-metadata.xml && git commit
```

### مصفوفة الرفع المدروسة (سبتمبر 2026) — بانتظار قرار المالك

| المكتبة | الحالي | الأحدث الموثق بحثاً | ملاحظة |
|---|---|---|---|
| Kotlin | 2.3.21 | **2.4.0** (14 يوليو 2026) | يتطلب KSP 2.4.x + Compose plugin نفس الإصدار [kotlinlang.org] |
| kotlinx-coroutines | 1.10.2 | **1.11.0** | تحذير deprecation لتمرير Job للبنّاءات |
| androidx.core-ktx | 1.12.0 (2023!) | خط 1.17+ | أقدم مكتبة نشطة في الكتالوج — أولوية |
| security-crypto | 1.1.0-alpha06 | **1.1.0 مستقرة** | خروج من ألفا |
| biometric | 1.1.0 (2021) | خط 1.2 | قديم |
| Compose BOM | 2026.08.00 | تحقق دوري من صفحة BOM | حديث |
| OkHttp | 5.3.2 | حديث | ✓ |
| libsignal | 0.86.5 | 0.86.5 (الأحدث المنشور فعلاً) | 0.99.x غير منشور — التوثيق في الكتالوج نفسه ✓ |
| tink-android | 1.23.0 | تحقق عند الرفع | مثبّت يدوياً في build.gradle — انقله للكتالوج |

## 5) media-sfu و admin_dashboard

- **mediasoup 3.24.0 → 3.26.0 متاح** (18 أغسطس 2026) — **لا يُرفع أعمى**: `package-lock.json`
  لا يمكن إعادة توليده هنا (npm محجوب في السندبوكس)، و3.26.0 فيها **تغيير كسر**:
  "Simulcast/SVC: تقييد الطبقة الزمنية بالمفضَّلة" — يتفاعل مع `SET_QUALITY/setAllVideoLayers`
  لدينا ويحتاج اختبار جودة فعلي. **قرار مقترح**: رفعة منفصلة بعنوان مخصص + اختبار بث حقيقي.
- **admin_dashboard حديثة بالفعل**: React 19.2 / antd 6.1 / Vite 7.2 / TS 5.9 / Tailwind 4.3 —
  لا تعارضات، ولا يُنصح بمسّها.

## 6) الخلاصة

| البند | قبل | بعد |
|---|---|---|
| حزم Jackson في الخادم | 2 (fasterxml 2.19.2 + tools/Boot4) | **1** (tools.jackson موحّد) |
| تثبيتات إصدارات يدوية متعارضة | 3 | 0 |
| إدخالات كتالوج ميتة | 42 | 4 (أعضاء bundles فقط) |
| ثغرات معروفة في اعتماديات الخادم | bcprov 1.79 (CVE-2026-71887) | 1.86 ✓ |
| القفل الأمني للأندرويد | سليم | سليم (بلا أي كسر) |
