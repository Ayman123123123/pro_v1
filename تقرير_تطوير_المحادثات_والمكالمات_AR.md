# تقرير تطوير شامل — مطابقة واتساب، إصلاح الإرسال، والمكالمات

> التاريخ: 2026-08-14 — فحص حرفي للملفات الفعلية في `RED_Ultimate_V1-main/RED_Ultimate/`.

---

## 1) نتائج البحث عن شكل محادثات واتساب

من البحث في الويب (مصادر مخصصة لمواصفات واجهة واتساب [1][2])، خصائص شكل المحادثة المعتمدة التي يعرفها المستخدم فورًا:

| العنصر | مواصفة واتساب |
|---|---|
| فقاعة الصادر | **أخضر** (#005C4B داكن / #DCF8C6 فاتح) |
| فقاعة الوارد | **رمادي داكن** (#202C33) أو أبيض |
| علامات القراءة | علامة واحدة ✓ (أُرسل)، علامتان ✓✓ (وصل)، علامتان **زرقاوان** ✓✓ (قُرئ) |
| الوقت | **داخل الفقاعة** أسفل اليمين (مثل `4:20 م`) |
| اسم المرسل | **لا يظهر** في المحادثة الفردية 1:1 — يظهر **بلون ملوّن** في المجموعات فقط |
| ذيل الفقاعة | زاوية سفلية حادة (نحو المرسل) |
| خلفية | نقش/تدرّج خلف الفقاعات |
| رأس المحادثة | صورة + الاسم + «متصل/آخر ظهور» + أزرار اتصال/فيديو |

المشروع يملك معظم هذه العناصر (خلفيات، تواريخ، تفاعلات، ردود، صوت) لكن كان ينقصه التطابق الدقيق في الفقاعات.

---

## 2) الخلل الجذري في الإرسال (تم إصلاحه)

### 🐛 السبب: استخدام متغيّر قبل تعريفه
في `ChatHubScreen` داخل `RedDashboard.kt`، كان `LaunchedEffect(messageText)` (الخاص بإرسال حالة "يكتب…") يستخدم متغيّر `conversation` **قبل تعريفه** — فالمتغيّر يُعرَّف لاحقًا داخل نطاق `if (tab == 0)`:

```kotlin
LaunchedEffect(messageText) {
    ...
    putExtra(EXTRA_CONVERSATION, conversation)   // ❌ غير معرّف هنا بعد
    ...
}
// ... بعد عشرات الأسطر ...
val conversation = remember(account.redId, target) { conversationId(account.redId, target) }
```

هذا يمنع التجميع (Unresolved reference) ويكسر مسار الإرسال/مؤشر الكتابة.

### ✅ الإصلاح
استُبدل المرجع المبكر بحساب مباشر:
```kotlin
val typingConversation = conversationId(account.redId, target)
```
أصبح المسار سليمًا: الإرسال الفعلي للرسائل (`RedConnectionService.sendRichText`) كان سليمًا أصلًا، لكن هذا الخطأ كان يعطّل جزءًا من التدفق. تأكد أن مسار الإرسال يعتمد على:
1. `signal.encrypt()` — يتطلب **جلسة Signal مُنشأة** مع الطرف (تُنشأ عند قبول طلب الصداقة).
2. اتصال WebSocket حي عبر `RedConnectionService`.

> ملاحظة تشغيلية: إن استمر فشل الإرسال لشخص محدد، فالسبب غالبًا أن جلسة التشفير لم تُنشأ بعد (لم يتم قبول طلب الصداقة من الطرفين)، لا خلل في الزر.

---

## 3) تحسين مظهر الفقاعات (مطابقة واتساب — تم)

### المحادثة الفردية 1:1
- ❌ حُذف سطر «أنت» فوق كل رسالة صادرة (واتساب لا يعرضه).
- ❌ حُذف اسم المرسل فوق الرسالة الواردة في 1:1.
- ✅ أُضيف **الوقت داخل الفقاعة** (`formatClockTime`) أسفل اليمين.
- ✅ علامات القراءة بصيغة نظيفة: `✓` ثم `✓✓` ثم `✓✓` زرقاء عند القراءة (بدل «✓✓ (مقروء)»).

### المجموعات
- ✅ أُبقي **اسم المرسل الملون** (مطابق لواتساب).
- ✅ أُضيف الوقت + علامات القراءة داخل الفقاعة.

### الألوان
فقاعة الصادر `YounesEmerald` (أخضر الهوية) والوارد `AqyalSurfaceRaised` (رمادي داكن) — مطابقة لدلالة واتساب مع الحفاظ على هوية «يونس».

---

## 4) تشخيص المكالمات («لا تصل للطرف الآخر» + «تخرج»)

### كيف تعمل المكالمات في المشروع
1. المتصل يبدأ `YounesCallService` → `CallSignalingClient` يتصل بـ `/ws/calls` → يرسل `OFFER`.
2. الباك-اند (`CallWebSocketHandler`) يوجّه `OFFER` إلى **جلسات الطرف المستهدف الحية**.
3. إن لم توجد جلسة حية → يضع العرض في صندوق 60 ثانية + يرسل **VoIP Push (FCM)**.
4. الطرف الآخر يستقبل عبر `YounesCallService.listen()` (خدمة أمامية) ثم WebRTC عبر SFU/TURN.

### الأسباب الجذرية المرجّحة (حسب الكود)

**أ) لا يوجد FCM (إشعار سحابي):**
- `NotificationService.sendVoipPushNotification` يرسل FCM، لكن **لا تبعية Firebase في Gradle** (مؤكد في فحص 2026-08-13).
- النتيجة: عندما يكون تطبيق الطرف الآخر مغلقًا/مقتولًا من النظام (Android 12+ يقتل الخدمات الخلفية)، لا توجد جلسة `/ws/calls` حية → العرض يذهب للصندوق + FCM غير موجود → **الرنين لا يصل أبدًا**.

**ب) TURN مكوّن على شبكة LAN فقط:**
- `.env.example`: `TURN_PUBLIC_HOST=192.168.1.50` و `MEDIASOUP_ANNOUNCED_IP=192.168.1.50`.
- إن كان الطرفان على **شبكتين مختلفتين** (أو عبر الإنترنت) فلن يمرّ وسيط المكالمة عبر NAT → **تنقطع المكالمة فور القبول** أو لا تُنشأ أصلًا.

**ج) انقطاع أثناء المكالمة:**
- `onConnectionState(FAILED/CLOSED)` يستدعي `endCall` مباشرة. توجد آلية `CallReconnectManager` + `restartIce` لكنها تعتمد على بقاء الخدمة الأمامية حية ووجود TURN صالح.

### الحلول
| المشكلة | الحل |
|---|---|
| لا FCM | أضف تبعية Firebase + FCM، أو شغّل `YounesCallService` كخدمة أمامية `START_STICKY` مع `foregroundServiceType="phoneCall"` (موجود جزئيًا)، أو اعتمد صندوق الـ 60 ثانية مع بقاء التطبيق مفتوحًا |
| TURN على LAN | اربط `TURN_PUBLIC_HOST` و`MEDIASOUP_ANNOUNCED_IP` بـ **IP عام/نطاق عام** وافتح المنافذ 3478 (TURN) و40000-40100/udp (SFU) على جدار الحماية |
| انقطاع | تأكد أن `RED_HTTP_PORT=8088` و`8443` مفتوحان للطرفين، وأن الشهادة TLS صالحة لمسار `wss://` |

> هذه الإصلاحات **تشغيلية/بنية تحتية** ولا تُحل بالكود وحده — تحتاج بيانات نشر خادمك الفعلية.

---

## 5) المجموعات والمحادثات الخاصة (تطوير هذه الجولة)

### ✅ إصلاح: وصف المجموعة كان يُفقد
شاشة «إنشاء مجموعة» كانت تجمع حقل «الوصف» لكن لا تمرّره إطلاقًا (الـ callback يأخذ `name, privacy, memberRedIds` فقط). أُضيف `description` لمسار الإنشاء:
- `CreateGroupScreen.onCreate` أصبح `(name, description, privacy, memberRedIds)`.
- زر «تأسيس المجموعة» يمرّر الوصف الآن.
- `GroupViewModel.create` يستقبله ويحفظه في الخادم.

### ✅ إصلاح: مرجع `conversation` قبل تعريفه
في `ChatHubScreen`، كان `LaunchedEffect(messageText)` (مؤشر «يكتب…») يستخدم متغيّر `conversation` قبل تعريفه — خطأ تجميع يكسر مسار الإرسال. استُبدل بحساب مباشر `conversationId(account.redId, target)`.

### ✅ جديد: فتح محادثة خاصة من قائمة أعضاء المجموعة
لم يكن بالإمكان مراسلة عضو من داخل المجموعة. الآن:
- `SovereignGroupInfoScreen` يستقبل `ownRedId` و`onMessage`.
- كل عضو (عدا نفسك) أصبح **قابلًا للنقر** لفتح محادثة خاصة، مع أيقونة 💬 واضحة.
- `RedDashboard` يضيف حالة `pendingChatTarget` تُغذّي `deepLinkSender` في تبويب الدردشات، فتُفتح المحادثة الصحيحة تلقائيًا، ثم تُمسح الحالة لتفادي إعادة الفتح عند التبديل بين التبويبات.

### ✅ تدفق «الدخول لمحادثة خاصة والضغط عليها» (مراجعة وتأكيد)
- قائمة الدردشات (`getActiveConversations`) تعرض كل محادثة مع آخر رسالة ووقت وعدد غير المقروء.
- الضغط على أي محادثة يضبط `target = conv.peerId` → تُعرض الرسائل وتُفتح المحادثة.
- زر الرجوع يعيد لقائمة الدردشات.
- فتح محادثة من «الأصدقاء» أو من «البحث في الدليل» يعمل بنفس المسار.

---

## 6) ملخص التعديلات البرمجية في هذه الجلسة

| الملف | التغيير |
|---|---|
| `RedDashboard.kt` | إصلاح مرجع `conversation` المبكر (كسر الإرسال) + مطابقة فقاعات واتساب + ربط أعضاء المجموعة بمحادثة خاصة (`pendingChatTarget`) + تمرير وصف المجموعة |
| `SovereignGroupSystem.kt` | تمرير وصف المجموعة في الإنشاء + جعل أعضاء المجموعة قابلين للنقر لفتح محادثة خاصة |
| `AuthViewModel.kt` | ترجمة `INVALID_REQUEST`/`MALFORMED_JSON`/`INTERNAL_ERROR` + دالة `setServerUrl()` |
| `AuthScreens.kt` | زر إدخال عنوان الخادم يدويًا + تمرير رأسي |
| `AuthExceptionHandler.kt` | تسجيل الأسباب + معالجات JSON/تحقق + `INTERNAL_ERROR` |
| `AuthDtos.kt` + `AuthController.kt` | `@Valid`/`@NotBlank`/`@NotNull` لمنع NPE |

---

## 6) الخطوات التالية المطلوبة منك (لا تُنجز بالكود)

1. **لإصلاح المكالمات**: 
   - فعّل Firebase FCM في المشروع، أو أبقِ التطبيق مفتوحًا.
   - اجعل `TURN_PUBLIC_HOST`/`MEDIASOUP_ANNOUNCED_IP` عنوانًا عامًا (أو أبقِ الطرفين على نفس الشبكة المحلية).
2. **لبناء APK يعمل**:
   ```bash
   ./gradlew :app:assembleDebug -PRED_SERVER_URL=http://IP_الخادم:8088
   ```
3. أعد بناء الخادم بعد تعديلات `Auth*`: `docker compose up -d --build backend`.

> ⚠️ لا توجد Java/Android SDK في هذه البيئة، فلم أستطع تنفيذ البناء — لكن تحققت يدويًا من توازن الأقواس ومن عدم كسر الاختبارات الموجودة.
