# 📞 الفهم الشامل لنظام المكالمات في «يونس»

> **التاريخ:** 2026-08-11
> **النطاق:** كل أنواع المكالمات وفروعها وأشكالها وسير عملها الكامل في `red-app`
> **الملفات:** 24 ملفًا في `calls/` + `features/calls/`

---

## 1) أنواع المكالمات (5 أنواع رئيسية)

| النوع | التصنيف (`CallType`) | الحالة/التعريف | البنية |
|---|---|---|---|
| **صوتي يونس** | `VOIP_AUDIO` | مكالمة طرفية E2EE (صوت فقط) | `YounesCallService` (mode=VOICE) |
| **فيديو يونس** | `VOIP_VIDEO` | مكالمة فيديو E2EE (1080p) | `YounesCallService` (mode=VIDEO) |
| **مؤتمر** | `CONFERENCE` | حتى 32 مشارك (صوت/فيديو) | `ConferenceService` |
| **بث مباشر** | `LIVE_BROADCAST` | بث سيادي عام (مُبث/مشاهد) | `LiveStreamService` |
| **خطي يمني PSTN** | `PSTN_DINSTAR` | اتصال بالهاتف اليمني | `PstnApi` + `Dinstar*` |

---

## 2) حالات المكالمة (Call State Machine)

### 2.1 المكالمة الفردية (`CallUiState`)
```
Idle → Incoming / Connecting → Active → (ActiveWithIncoming) → Idle
                         ↘ Error
```
- **Idle** — لا مكالمة
- **Incoming(callId, peer, mode)** — مكالمة واردة (OFFER)
- **Connecting(callId, peer, mode)** — جارٍ التفاوض
- **Active(callId, peer, mode, startedAt, isHeld)** — نشطة (يدعم تعليق)
- **ActiveWithIncoming(active, waiting)** — **Call Waiting** (مكالمة نشطة + واردة ثانية)
- **Error** — فشل

### 2.2 المؤتمر (`ConferenceUiState`)
```
Idle → Connecting(roomId) → Active(roomId, startedAt) → Idle
                        ↘ Error
```

### 2.3 البث المباشر (`LiveStreamUiState`)
```
Idle → Connecting(streamId, isBroadcaster) → Active(streamId, isBroadcaster, startedAt) → Idle
                                        ↘ Error
```

---

## 3) سير عمل المكالمة الفردية (الأشمل)

### 3.1 المكالمة الصادرة (Outgoing)
```
start(target, video) → ACTION_START → prepareAudio() → signaling.connect()
  → onConnected() → createEngine(video) → engine.offer()
  → onLocalDescription → signaling.send(OFFER+sdp)
  → استقبال ANSWER → engine.setRemote → Active
```

### 3.2 المكالمة الواردة (Incoming)
```
onSignal(OFFER) → CallUiState.Incoming → startRingtone() → TelecomBridge.addCall()
  → incomingNotification (رنين + قبول/رفض)
قبول → ACTION_ACCEPT → acceptIncoming() → setRemote(OFFER) → engine.answer()
رفض → ACTION_REJECT → rejectIncoming() → send(REJECT) → endCall
```

### 3.3 Call Waiting (مكالمة ثانية واردة)
- مكالمة نشطة + OFFER جديد → `ActiveWithIncoming(active, waiting)`
- أزرار: `ACCEPT_SECOND` (إكمال الأولى والرد) / `REJECT_SECOND`

---

## 4) الوسائط والمحرّك (WebRtcEngine)

| المكوّن | الوظيفة |
|---|---|
| **WebRtcEngine** | PeerConnection + ICE + SDP + Simulcast + Adaptive Bitrate |
| **AudioTrack / VideoTrack** | كتم الميكروفون / إيقاف الكاميرا / تبديل الكاميرا |
| **RTCStats** | قياس RTT + فقدان الحزم + الجودة (Poor→Excellent) |
| **Adaptive Bitrate** | ضبط تلقائي حسب الشبكة (NetworkStats.BitrateProfile) |
| **Simulcast** | إرسال عدة درجات جودة للبث/المؤتمر |
| **EglBase** | تسريع الفيديو |

---

## 5) واجهة المكالمة (شكلها)

### 5.1 `YounesCallOverlay` (مكالمة 1:1)
- **فيديو**: `RemoteVideoRenderer` (ملء الشاشة) + `LocalVideoRenderer` (مصغّرة 120×170)
- **أزرار التحكم** (دائرة 52dp):
  - تسجيل (●) • ميكروفون (تشغيل/إيقاف) • مكبر صوت
  - تعليق/استئناف • Bluetooth • كاميرا (فيديو) • تبديل كاميرا (فيديو)
- **NetworkQualityIndicator** — مؤشر جودة الشبكة
- زر **إنهاء** (أحمر)

### 5.2 `SovereignActiveCallScreen` (الواجهة الموحدة)
- عرض حسب `CallType` (صوتي/فيديو/مؤتمر/بث/خطي)
- اسم الطرف + الحالة + المدة + الإشارة

### 5.3 `UnifiedCallOverlays`
- تختار الـ overlay الصحيح تلقائيًا:
  - مكالمة فردية → `YounesCallOverlay`
  - مؤتمر → `YounesConferenceOverlay`
  - بث → `LiveStreamViewerOverlay`

---

## 6) الإشارات (Call Signaling)

`CallSignalingClient` عبر WebSocket `/ws/calls` (Bearer token):
| الرسالة | الوظيفة |
|---|---|
| `OFFER` | عرض SDP (بدء مكالمة واردة) |
| `ANSWER` | قبول العرض |
| `ICE` | مرشّح ICE |
| `REJECT` | رفض |
| `HOLD` / `RESUME` | تعليق / استئناف |
| `DTMF` | أزرار لوحة |
| `RECORDING_*` | بدء/إيقاف التسجيل |

---

## 7) خدمات مساعدة للمكالمات

| الخدمة | الوظيفة |
|---|---|
| **TelecomBridge** | تكامل مع نظام الاتصال Android (شاشة النظام/المقبلة) |
| **CallRecordingManager** | تسجيل مشفر (AES) + فك تشغيل |
| **CallCaptionManager** | تسميات توضيحية حية (SpeechRecognizer) |
| **CallPttHelper** | زر Push-To-Talk (ضغط للحديث) |
| **CallReconnectManager** | إعادة اتصال تلقائي عند الانقطاع |
| **CallTelemetry** | قياسات وتحليلات المكالمة (RTT/خسارة/مدة) |
| **CallLogCipher** | تشفير سجل المكالمات (peerId/label) |
| **YemeniOperatorDetector** | كشف مشغّل اليمن (يمن موبايل/سبأفون/Y/يمن/ثابت) |

---

## 8) سجل المكالمات (`CallHistoryItem`)

| الحقل | القيمة |
|---|---|
| `type` | VOICE / VIDEO / GROUP / LIVE / SPACE / DINSTAR |
| `direction` | INCOMING / OUTGOING |
| `route` | RED (يونس) / DINSTAR (هاتف يمني) |
| `status` | COMPLETED / MISSED / REJECTED / ACTIVE / ENDED / FAILED |
| `durationMs` | المدة |
| `answeredAt` / `endedAt` | الأوقات |

- التخزين: Room (`call_logs`) مع **تشفير** peerId/label
- العرض: `UnifiedCallsScreen` مع فلاتر (الكل/فائتة/صوت/فيديو/جماعية/بث/مساحات/DINSTAR)

---

## 9) مركز المكالمات (`UnifiedCallsScreen`)

- **3 أزرار سريعة**: مؤتمر جماعي • بث مباشر • مساحة صوتية
- **فلاتر** حسب النوع
- **إعادة الاتصال** بنقرة (CallHistoryRow)
- **الهاتف اليمني (DINSTAR)**: لوحة أرقام + مفضلة + سجل + كشف مشغّل

---

## 10) نقاط القوة
1. ✅ **E2EE** لكل المكالمات (WebRTC + مشفر)
2. ✅ **Call Waiting** (مكالمة ثانية واردة)
3. ✅ **Adaptive Bitrate + Simulcast** (جودة تلقائية)
4. ✅ **TelecomBridge** (شاشة نظام)
5. ✅ **5 أنواع مكالمات** (صوت/فيديو/مؤتمر/بث/خطي)
6. ✅ **تسجيل مشفر + تسميات حية + PTT + إعادة اتصال**

## 11) عيوب/نواقص ملاحظة
- **`LiveStreamService`/`ConferenceService`**: البث/المؤتمر لا يملكان "مكالمة واردة" إشعارية كاملة (بل إشعار جارٍ فقط) — لا يوجد رنين/قبول للانضمام
- **دورة حياة**: `start()` ترجع `START_NOT_STICKY` — قد يُقتل الـ Service في الخلفية
- **`CallReconnectManager`**: يعيد الاتصال لكن لا يعيد إنشاء PeerConnection (يحتاج تحقق)
- **`CallCaptionManager`**: يعتمد على SpeechRecognizer (قد يفشل بدون اتصال)

---

## الخلاصة
نظام المكالمات في «يونس» **متقدم جدًا**: 5 أنواع، E2EE، Call Waiting، جودة تكيفية، تسجيل مشفر، TelecomBridge، وPTT. أقوى ما يميّزه هو **تكامل الهاتف اليمني (DINSTAR)** + **TelecomBridge** + **Simulcast**. أبرز ما يحتاج تطويرًا: **إشعارات واردة للمؤتمرات/البث** ودورة حياة أكثر قوة للخدمات في الخلفية.
