# 🩺 تقرير العيوب والأخطاء والنواقص الشامل
**المشروع:** RED Ultimate / YOUNES Sovereign · **التاريخ:** 2026-09-24 · **الفرع:** `arena/01a0d28e-pro-v1` @ `3c8a0bc`
**المنهجية:** مسح آلي شامل (16 محور فحص) + تحقق يدوي لكل رقم — لا بند في هذا التقرير إلا ومثبت بأمر/عدد فعلي.

---

## 0) ما أُصلح سابقاً (حتى لا يُحسب عيباً قائماً) ✅

| الإصلاح | التزام |
|---|---|
| جلي 114 ملف نفايات (سجلات بناء، نفايات DINSTAR، صور مكررة، App.tsx الميت) | `b7e60a9` |
| إصلاح `.gitignore` المكسور (`*.ap_` الدامج) + تحصين | `b7e60a9` |
| شارة غير المقروء الحية (كانت `0` ثابتة) عبر Room Flow | `b7e60a9` |
| وجهة مكالمة واحدة (كانت وجهتان تتراكبان) | `ac77dac` |
| مراقب وشريط حالة السيرفر الهادئ + تباين نص الاتصال ≥7:1 | `ac77dac` |
| شفافية نص القصص 0.4→0.6 + حذف docker-compose الخطِرين | `ac77dac` |
| الاستعادة من 3 إعادات تهيئة للبيئة + تثبيت refspec الفرع | `3c8a0bc` |

---

## 🔴 العيوب الحرجة P0 — تمنع اعتبار المشروع «مكتمل النضج»

### P0-1) انقسام خط golden — خطر مسارين متباعدين
- `main` = `v5.1/v6.0-golden` @ `733502d` — **التزام يتيم بلا أب** (تاريخ مُعاد بناؤه)، أحدث تاريخياً (13:10 مقابل 10:08)، لكنه **فاقد كل قيمة جلستنا**: فيه 14 `build_errors*` + 17 `sipcfg*` + `temp-dc.yml` + `App.tsx` + الشارة الميتة `unreadChats = 0` + صفر إصلاحاتنا.
- فرعنا = `v4.0-golden` + 3 التزامات قيمة (الجلي + 6 إصلاحات + التوثيق).
- **الخطر:** أي جهل بهذا الانقسام = فقدان إصلاحات أو استبدالها برجعة. الدمج يتطلب `cherry-pick` يدوياً (`RedDao.kt` متطابق فيلتصق إصلاحه؛ `ModernRedDashboard` تغيّر +326/−97 في v6 فيحتاج دمجاً يدوياً).
- **العلاج:** إعادة بناء الإصلاحات الثلاثة فوق `733502d` ثم PR موحّد.

### P0-2) ~9,760 ملفاً ميتاً داخل Git خارج graph البناء (~90MB)
| الشجرة | الملفات | الحالة |
|---|---|---|
| `app/` (fork Signal القديم) | **6,985** | خارج البناء — موثق بـREADME_DEAD_TREE «لا تحذف» |
| `lib/` + `core/` + `feature/` + `demo/` | **2,779** | مراجع Signal قديمة خارج البناء |
| `benchmark/` + `microbenchmark/` + `baseline-profile/` | 29 | معزولة |

**الأثر:** كل clone/fetch/فحص-شفرة/بحث يعالج ملفات لن تُبنى أبداً. **العلاج:** `git tag archive/...` ثم حذف من الفرع — السجل يحفظ كل شيء.

### P0-3) لا FCM ولا google-services.json — عقبة توزيع Play Store
نظام الإشعارات **UnifiedPush + ntfy** بمشتفر سيادي (`SovereignPushCipher`) — معماريًا أنيق ومستقل، لكن غياب مسار FCM الاختياري يعني: أي مستخدم يثبّت من متجر Google قد يفقد الإشعارات في الخلفية على أجهزة تقتل الخدمات الخلفية بشراسة (وخاصة الأجهزة الصينية). الملفات الفعلية: `push/` بثلاثة ملفات، `google-services.json: 0`.
**العلاج:** مزوّد إشعارات ثانٍ (FCM/أو وثيقة توزيع APK مباشر ومتاجر بديلة) — والكود جاهز لهذا (نمط `PushWakePresenter` قابل للتوسيع).

### P0-4) هشاشة بيئة التطوير (متكررة فعلاً 3 مرات)
إعادة تهيئة بيئة العمل تُسقط الفرع لأن `fetch` الافتراضي يجلب `main` فقط (`+refs/heads/main:...`). عولجت بالتحصين (refspec + أمر استعادة موثق)، لكنها تبقى نقطة فشل تشغيلية حتى تُثبَّت في إعدادات المستودع نفسه على GitHub (مثل إضافة الفرع إلى مسارات الحماية/القوالب).

---

## 🟠 أخطاء جسيمة P1

### P1-1) فوضى CI: 16+ ملف تعريف بناء متداخل
| الموقع | الملفات |
|---|---|
| `.github/workflows/` (جذر) | 5 (القانونية: `red-ultimate-ci.yml` فقط) |
| `RED_Ultimate_V1-main/.github/workflows/` | **7 قديمة** (ci-cd, docker-image, repair-lfs...) |
| `workflow-ready/` + `ci-proposed/` (حُذف) | 4 + 1 |
| GitLab CI | 2 (`+ نسخة داخل RED_Ultimate`) |

**الأثر:** أي تعديل CI في ملف خاطئ = انفجار صامت. **العلاج:** تقاعد 11 ملفاً والاحتفاظ بالقناة الرسمية + GitLab المرآة.

### P1-2) أربعة خوادم mock متوازية للوحة الإدارة
`dev-mock-server.mjs` + `dev-mock-api.mjs` + `mock-backend.cjs` + `server.cjs` + `dev-server/` (3 ملفات) — كلٌّ بعقود مختلفة قليلاً؛ تصلح لأغراض مختلفة لكنها بلا توثيق لأي يخدم أيّاً، وثلاثتها تغطي نفس الغرض.

### P1-3) كود واجهات ميت داخلياً
- `RedDashboard.kt` (قديم): **3,846 سطراً** لا يستدعيه أحد (MainActivity تستخدم `ModernRedDashboard`) — بقي «كمرجع» بلا أرشفة.
- طبقة `ui/screens/` (10 ملفات) تتقاطع مع شاشات `ui/` المسطحة — سياكت `README_SPLIT.md` تفرض ≤600 سطر/شاشة لكن `RedDashboard.kt` خالفته بـ6.4x.

### P1-4) تغطية اختبارات مبتورة في نصف المنتج
| المكوّن | الوضع |
|---|---|
| تطبيق أندرويد | جيد: 63 وحدة + 5 تكامل |
| خادم خلفي | جيد: 73 اختباراً |
| **لوحة الإدارة** | **صفر ملفات اختبار** رغم تهيئة vitest في `package.json` (`"test": "vitest run"` سينجح فارغاً = وهم حماية) |
| **media-sfu** | **اختبار واحد** (`node --check` صياغة فقط) — قلب الوسائط الحي بلا تغطية سلوكية |

### P1-5) إعدادات خادم تُخفي الأخطاء
- `flyway.validate-on-migrate: false` — انحراف المخطط لا يُكتشف عند الترحيل.
- `spring.main.allow-bean-definition-overriding: true` — قناع تعارضات Beans.
- `WebSocketConfig`: أصول مسموحة بـwildcards `http://192.168.*:*` — منطقي لـLAN، خطر إن وصل للإنترنت بلا تضييق عبر `ALLOWED_ORIGINS` الإنتاجي.

### P1-6) ازدواجية واجهات API (جيلان حيّان معاً)
مسارات `calls` عبر **9 ملفات متحكمات** (`/api/calls` و`/api/v1/calls` وبنية bridge) — الجيلان يعملان معاً بلا خطة تقاعد موثقة للقديم؛ مصدر خلط للعملاء الجدد وتضخيم سطح الصيانة.

---

## 🟡 عيوب متوسطة P2

| # | العيب | الدليل الفعلي |
|---|---|---|
| P2-1 | **ضجيج الجذر**: 77 ملفاً سائباً (24 md لاتيني + 21 md عربي + 16 ps1 + 4 png...) | `git ls-files` |
| P2-2 | تكرار مزدوج بين الجذرين | `run.sh`, `RUN.bat`, `README.md`, `.gitlab-ci.yml`, `.env.example` موجودة مرتين بقدر مختلف من التحديث |
| P2-3 | انحياز Windows: **61 سكربت ps1** مقابل **8 sh** — قابلية النقل محدودة | `scripts/` |
| P2-4 | 3 مكتبات رسوم بيانية باللوحة | `echarts`(3 ملفات مستخدمة) + `recharts`(**ملف واحد فقط**) — الحذف يقلص الحزمة |
| P2-5 | فساد ترميز (mojibake) بتعليقات `backend-server/Dockerfile` | سطور `Ã¢â‚¬â€` واضحة |
| P2-6 | خلط CRLF/LF | `application.yml` بـCRLF بينما بقية YAML بـLF؛ `.gitattributes` غير شامل |
| P2-7 | TODO حقيقية عالقة في الكود الحي | **LiveKitBridge = stub كامل بـ6 TODOs بلا SDK** (وغير مستخدم من أي ملف!) + بحث القنوات السحابي (P1-G) + `GroupDocument` lookup بالprefix فقط |
| P2-8 | غموض إصدارات | `versionCode = 2` / `1.0.0-alpha02` مع تعليق «خطة مجمدة versionCode = 3 لا تطبق الآن» — بلا أتمتة إصدار |
| P2-9 | تسميات متضاربة | الجذر `pro_new` vs الداخلي `RED-Ultimate` vs الوجهة `YOUNES/RED` — 3 أسماء لبناء واحد |
| P2-10 | `red-app/artwork` بلا نظام: **24MB** بفوضى (`ICON1.png`, `ICON_PREVIEW.png`, `Copilot_*.png`, `IMGs/` مكررات) | بالمستودع كاملاً |
| P2-11 | مجلدات شبه ميتة داخل المنتج | `janus`(3) `kamailio`(3) `prebuilt-backend`(1) `local-maven`(1) `integration_patches`(1) — بلا دور موثق |
| P2-12 | **لا إنجليزية إطلاقاً**: 162 نصاً عربياً في `values/` الافتراضية وبلا `values-en` | يمنع أي توسع غير عربي |
| P2-13 | ملفات IDE/أدوات متتبعة | 14 ملفاً `.idea/.kilo/.zcode/.artifacts/.vscode` + `.gitignore_local` غامض بالجذر |
| P2-14 | docs/dinstar-tools: 95 ملفاً (3.3MB) مخرجات أدوات حية داخل Git | يفترض external storage |

---

## 🔵 ملاحظات منخفضة P3

1. `SMTP_PASSWORD = ""` ثابت فارغ في `NotificationConfig.kt` — موضع إعدادات ميتة.
2. افتراضي `192.168.11.1` متموضع في عدة مواضع خادم (موثق، لكن smell تكوين).
3. Hikari pool ثابت `20/5` بلا ضبط بيئي.
4. بناءات Docker تعتمد Docker Hub أفقياً (موثق بالتعليقات مع بدائل — يفضل mirror مركزي).
5. `values-night` و`v31` موجودان لكن لا `values-en` (يأتي مع P2-12).
6. `ADMIN cookie secure` يعتمد على ضبط يدوي صحيح للبيئات (موثق — قابل للخطأ البشري).

---

## 📋 النواقص — ما يجب إضافته/تفعيله (بالأولوية)

| # | المفقود | الأثر | الجهد |
|---|---|---|---|
| ن1 | **تسوية انقسام golden** (P0-1) | يحمي كل العمل | يوم |
| ن2 | أرشفة الأشجار الميتة (P0-2) | سرعة ×5 لكل العمليات | ساعة |
| ن3 | مسار إشعارات ثانٍ/وثيقة توزيع (P0-3) | انتشار متاجر | 2-4 أيام |
| ن4 | اختبارات لوحة (vitest فعلي) + SFU سلوكية | حماية التطوير | 2-3 أيام |
| ن5 | تفعيل بحث FTS في واجهة التطبيق | الخادم جاهز (V61) والواجهة غائبة | يومان |
| ن6 | مؤشر جودة المكالمة المرئي (RedQualityManager موصول بلا UI) | لمسة تنافسية | يوم |
| ن7 | معرض/تصدير الثيمات فعلياً ب الواجهة (CustomThemeCreator جاهز شبه معطل) | ميزة مميزة | يوم |
| ن8 | ربط RBAC (V28_1) بصفحات اللوحة فعلياً | أمان إداري حقيقي | يومان |
| ن9 | `ONBOARDING.md` واحد يشرح أول 30 دقيقة لمطور جديد | تسريع الفريق | ساعتان |
| ن10 | `values-en` + هيكل i18n | توسع لغوي | يوم |
| ن11 | تقاعد 11 workflow قديم + توحيد mock | وضوح CI/تطوير | نصف يوم |
| ن12 | نقل التقارير الـ45 إلى `docs/archive/` + إلغاء تكرار الجذرين | جذر نظيف | ساعتان |
| ن13 | أتمتة `versionCode`/ملاحظات إصدار عربية | نضج إطلاق | ساعتان |
| ن14 | Git LFS للوسائط الكبيرة (artwork/dinstar-tools) | خفة المستودع | ساعتان |
| ن15 | تفعيل `validate-on-migrate` بعد تدقيق V64 + إزالة overriding | كشف مبكر للانحراف | نصف يوم |

---

## 📊 الخلاصة الرقمية

```
عيوب حرجة P0 : 4      جسيمة P1 : 6      متوسطة P2 : 14      منخفضة P3 : 6
نواقص قابلة للتنفيذ : 15 (بإجمالي ~10 أيام عمل فعلي)
ملفات ميتة داخل Git : ~9,760 (~90MB)      اختبارات مفقودة كلياً : اللوحة + SFU
أعمال CI متضاربة : 16+ تعريف             لغات واجهة : عربي فقط (0 إنجليزي)
```

> **الحكم العام:** الأساس التقني (تشفير، خادم، قواعد بيانات، دوكر) ناضج وقوي فعلاً؛ لكن **النضج الهندسي** (انضباط المستودع، CI واحد، اختبارات شاملة، تسوية الخطوط الذهبية) هو الفجوة الحقيقية — وكل بنوده معالجة بمسار واضح أعلاه.

**أُعدّ هذا التقرير بفحص آلي موثق الأوامر — 2026-09-24**
