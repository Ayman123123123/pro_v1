# 📱 تقرير الفحص الشامل لتطبيق أندرويد «يونس» (RED Sovereign)

> **التاريخ:** 2026-08-11
> **النطاق:** تطبيق الأندرويد القانوني `RED_Ultimate_V1-main/RED_Ultimate/red-app/` (وحدة Gradle `:app`)
> **الأسلوب:** فحص ثابت عميق (Static Analysis) — لا يوجد Java/Android SDK أو شبكة Maven في بيئة العمل، لذا لا يمكن تنفيذ بناء APK هنا.

---

## 1) فهم بنية المشروع

### 1.1 المخطط العام
```
pro_v1/
├─ RED_Ultimate_V1-main/
│  └─ RED_Ultimate/                 ← الجذر الفعلي لبناء Gradle
│     ├─ settings.gradle.kts        ← include(":app") → red-app + :shared-proto
│     ├─ gradle/libs.versions.toml  ← كتالوج التبعيات المركزي
│     ├─ red-app/                   ← التطبيق القانوني (:app) — يدخل البناء
│     ├─ app/                       ← Fork Signal القديم — خارج البناء (مصدر استخراج فقط)
│     └─ shared-proto/              ← بروتوكول protobuf مشترك (الأندرويد + الخادم)
```

### 1.2 وحدة `red-app` (التطبيق القانوني)
- **الحزمة:** `com.red.sovereign`
- **applicationId:** `com.red.sovereign` ، **minSdk 26** ، **targetSdk 35** ، **compileSdk 37**
- **المسار:** `src/main/java/com/red/sovereign/` — **148 ملف Kotlin**
- الميزات: حساب/mصادقة، تشفير libsignal (PQXDH/Double Ratchet)، WebSocket، محادثات/مجموعات/قصص/خلاصة اجتماعية، مكالمات WebRTC + مؤتمرات + بث مباشر، PSTN/DINSTAR، تخزين Room + SQLCipher، إشعارات، اكتشاف خادم محلي.

### 1.3 لماذا `red-app` وليس `app/`؟
`settings.gradle.kts` يوجّه `:app` إلى `red-app`. مجلد `app/` القديم (Fork Signal) **خارج مخطط البناء** — يوجد فقط كمصدر استخراج. لذلك إصلاحاتي تركّزت على `red-app`.

---

## 2) الفحوصات المنفَّذة

| الفحص | الأداة | النتيجة |
|---|---|---|
| مطابقة تبعيات build.gradle.kts مع كتالوج libs | مقارنة يدوية + سكربت | ✅ كل الأسماء معرّفة |
| مراجع `libs.*` في red-app | grep | ✅ سليمة (كل alias موجود في الكتالوج) |
| مكوّنات الـ manifest (خدمات/مستقبلات/نشاط) | سكربت | ✅ كل الـ10 مكوّنات موجودة كملفات |
| أذونات خدمات الخلفية (FGS types) | grep manifest | ✅ كل الأنواع لها الأذونات المطلوبة |
| الثيم `Theme.Younes` + `@xml/file_paths` | grep | ✅ موجودان |
| توقيعات `YounesTheme`/`SovereignBackground` | قراءة | ✅ مطابقة للاستخدام |
| خطوط Google Fonts (cairo/tajawal) + الشهادات | grep | ✅ موجودة ومطابقة للتوثيق |
| موارد drawable/color/string المفقودة (مع استبعاد android.R.) | سكربت | ✅ لا ناقص حقيقي |
| موارد مكررة (AAPT2 duplicate) | سكربت | ❌ **وجدت تضارب ic_launcher.svg/png** — أُصلح |
| توازن الأقواس | فحصان | ✅ سليم (كانت الأداة الثانية تعطي إيجابيات خاطئة) |
| المراجع الناقصة عبر الحزم | مؤشر تعريفات كامل | ❌ **وجدت VoiceBubble ناقص** — أُصلح |
| استيراد `Intent` في MainActivity | grep | ❌ **ناقص** — أُصلح |
| علامات TODO/FIXME/STOPSHIP | grep | ✅ لا يوجد (نظيف) |

---

## 3) الإصلاحات المطبَّقة (Build-breaking)

### ✅ الإصلاح 1 — استيراد `Intent` ناقص في `MainActivity.kt`
**الملف:** `red-app/src/main/java/com/red/sovereign/MainActivity.kt`
**المشكلة:** الكود يستخدم `Intent(...)` في سطرين (بدء/إيقاف `SovereignNotificationRouter`) دون `import android.content.Intent` → **خطأ ترجمة**.
**الحل:** أُضيف السطر `import android.content.Intent`.

### ✅ الإصلاح 2 — اسم دالة غير مطابق `VoiceBubble`
**الملف:** `red-app/src/main/java/com/red/sovereign/media/voice/VoiceBubble.kt`
**المشكلة:** `RedDashboard.kt` يستورد ويستدعي `com.red.sovereign.media.voice.VoiceBubble`، لكن الملف كان يعرّف `VoiceMessageBubble` → **مرجع غير محلول**.
**الحل:** أُعيدت تسمية الدالة إلى `VoiceBubble` (وهي الاستدعاء الوحيد؛ `VoiceMessageBubble` لم يُستخدم باسمه في أي مكان آخر).

### ✅ الإصلاح 3 — تضارب موارد الأيقونة (SVG + PNG)
**الملف:** `red-app/src/main/res/mipmap-mdpi/ic_launcher.svg`
**المشكلة:** وجود `ic_launcher.svg` (قالب Android Studio قديم) و`ic_launcher.png` بنفس الاسم في نفس المجلد → **خطأ AAPT2 "Duplicate resources"** يكسر ربط الموارد.
**الحل:** حُذف ملف `.svg` لأن لدينا PNGs حقيقية الآن لكل الكثافات.

---

## 4) تكامل أيقونة التطبيق (المنفَّذ مسبقًا في هذا الفرع)

- الأيقونة المعتمدة: **`younes_icon_ultimate_clean.png`** (حرف «ي» ذهبي على خلفية زمردية/كحلية — نسخة نظيفة من `07_ultimate`).
- مولّدت PNGs للمقاييس mdpi→xxxhdpi لكل من `ic_launcher` و`ic_launcher_round`.
- أُضيفت `ic_launcher_full.png` (foreground متكيّف) + خلفية متدرجة في `ic_launcher_background.xml`.
- رُبطت في `ic_launcher.xml` / `ic_launcher_round.xml` في `red-app` و`app/`.

---

## 5) ملاحظات وتحذيرات للبناء في بيئتك

> **البيئة هنا لا تسمح ببناء APK** (لا Java/Android SDK + شبكة Maven غير متاحة — فقط github.com يعمل). للتحقق النهائي يجب تشغيله على جهازك:

```bash
cd RED_Ultimate_V1-main/RED_Ultimate
./gradlew :app:assembleDebug          # APK تصحيح
./gradlew :app:lintDebug              # فحص الموارد والتوافق
```

- تبعية `libsignal-android` كبيرة — قد تحتاج وقتًا في أول تنزيل.
- شبكة Maven تتطلب اتصالاً (google() + mavenCentral + mirrors في `settings.gradle.kts`).
- خاصية `RED_SERVER_URL` الافتراضية `http://192.168.1.50` — غيّرها عبر `-PRED_SERVER_URL=` عند الحاجة.
- خاصية `RED_TARGET_ABI` الافتراضية `arm64-v8a`.

---

## 6) توصيات للمميزات الناقصة / التحسينات المستقبلية

بعد الفحص الشامل، التطبيق متماسك البنية لكن يُوصى بـ:

1. **إضافة `strings.xml`** — الكود يستخدم نصًا عربيًا مضمّنًا في كل الشاشات؛ نقله إلى موارد strings يحسّن قابلية الترجمة والمراجعة.
2. **تشغيل `ktlint`** — هناك أنماط تنسيق قد تُفشل `:ktlintCheck` في CI (يمكن التحقق عبر `./gradlew ktlintFormat`).
3. **التحقق من خاصية versionCode=1** — لرفع APK يجب زيادته مع كل إصدار.
4. **توقيع الإصدار release** — التعليق في `build.gradle.kts` ينبّه أن توقيع الإنتاج يجب أن يكون بمفتاح خاص منفصل وليس `red-debug-only`.
5. **اختبار الأذونات** — خدمات الخلفية `camera|microphone|phoneCall|remoteMessaging` تحتاج إلى منح الأذونات وقت التشغيل (ما زالت أذونات `POST_NOTIFICATIONS` تُطلب تلقائيًا في MainActivity).

---

## 7) الخلاصة

- **3 مشاكل حقيقية تكسر البناء** عُثر عليها وأُصلحت في `red-app`.
- **أيقونة التطبيق** المعتمدة مكتملة لكل المقاسات + متكيّفة (Adaptive).
- الكود ثابت البنية من ناحية الموارد والمراجع؛ **التحقق النهائي بالبناء الفعلي** مطلوب على جهاز به Android SDK لأن بيئة العمل لا توفر Java/Maven.
