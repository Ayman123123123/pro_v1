# خارطة الطريق — بعد التدقيق الشامل

> التاريخ: 2026-08-19 · الفرع: `arena/01a00f9e-pro-v1`
> الأساس: فحص فعلي للمستودع، لا تقدير. كل رقم هنا مُستخرَج بأمر.

---

## 1) أين المشروع الآن — بالأرقام

| المقياس | القيمة |
|---|---|
| ملفات متتبَّعة | 10,886 |
| اختبارات دنستار في العميل | 16 (كانت صفراً) |
| `red-app` (المنتج) | 158 ملف · 29,781 سطر |
| `backend-server` | 164 ملف · 18,365 سطر |
| لوحة الإدارة | 37 ملف `.tsx` · 25 صفحة |
| **اختبارات المنتج** | **482** (254 خادم + 228 عميل) |
| أشجار أندرويد | 2 (`red-app` منتج، `app` مرجع) |

## 2) ما أُنجز في هذه الجولة

| العمل | الحصيلة |
|---|---|
| توحيد أشجار الأندرويد الأربع | `android/` و`app-android/` حُذفا بعد استخراج المفيد |
| مراجعة تقارير الجذر بالقراءة الفعلية | 71 → 30 ملفاً عملياً؛ 42 مؤرشفاً بفهرس مبرَّر |
| تدقيق 365 ملفاً متغيراً سطراً سطراً | لا شيء مهم ضاع |
| خريطة المشغّلين + النطاق `722` | مصدر حقيقة واحد، ثلاث طبقات متسقة |
| استعادة 3 فجوات + إصلاح علّة قراءة | انظر §3 |
| **اختبارات دنستار** | من **صفر** إلى **16** |

## 3) الفجوات الخمس التي وُجدت واستُعيدت

| # | الفجوة | لولا الإصلاح |
|---|---|---|
| 1 | `DinstarDeviceStatus` | لا رؤية للحرارة — الاختناق الحراري يسبق سقوط المنافذ |
| 2 | النطاق `722` | **تعذُّر الاتصال بكل مشتركي سبأفون 4G في عدن** |
| 3 | إعادة اتصال WebSocket | لوحة جامدة تبدو سليمة عند انقطاع الشبكة |
| 4 | قراءة الحمولة من `data` | `port = 0` كاذب يبدو منفذاً حقيقياً |
| 5 | 4 أحداث WS مهملة | ردود USSD وأعطال البوابة تضيع صامتة |

**ما رُفض عمداً** — وهو جزء من التدقيق لا نقص فيه: `selectOptimalPort`
(مصدر حقيقة ثانٍ)، Circuit Breaker (يحمي استطلاعاً لا وجود له)،
استعلامات التصفية (مكرّرة في الواجهة)، `pstn_logs` (مدمج في `call_logs`).

---

## 4) ما بعد — مرتَّباً بالأثر لا بالسهولة

### الأولوية القصوى — عملك المحلي غير المرفوع

`C:\Users\hpc01\Pictures\pro_new` فرع `backup/strongest-2026-08-14`:
**~111 ملفاً معدَّلاً و~70 جديداً** لم تصل إلى المستودع، منها
`PstnManagement.tsx`، `DinstarDashboardUI.kt`، `DinstarLiveMonitor.kt`،
`DinstarWebSocketHandler.kt`، واختبارات الخادم.

> ⚠️ **تنبيه بنيوي**: تلك النسخة تحوي
> `android/features/pstn/PstnSipEngine.kt`، ومجلد `android/` **محذوف
> الآن**. الدمج الأعمى سيُعيد إحياء شجرة أزلناها عمداً — يجب توجيه
> الملف إلى `red-app/features/pstn/` بدل استعادة المسار.

هذا العمل معرَّض للضياع ما دام على جهاز واحد. رفعه أولوية على أي تطوير.

### الثانية — تشغيل الاختبارات فعلياً

البيئة هنا **بلا JDK ولا Gradle**، فتحققي كان بفحص التوازن والرموز
والتوقيعات. هذا يمسك أخطاءً نحوية (وقد أمسك واحداً: صيغة ثلاثية لا
توجد في Kotlin) لكنه **لا يُغني عن مترجم**.

```bash
cd RED_Ultimate_V1-main/RED_Ultimate
./gradlew :app:testDebugUnitTest        # 228 اختبار عميل
./gradlew :backend-server:test          # 254 اختبار خادم
```

الأولى بالمراقبة: `DinstarOperatorMappingTest` و`DinstarBackoffTest`
و`YemeniOperatorDetectorTest` — كلها جديدة أو معدَّلة في هذه الجولة.

### الثالثة — الدرس المنهجي من `722`

النطاق `722` **لم يُكتشف بفحص الكود**، بل بالبحث في الويب. الكود كان
متسقاً تماماً مع نفسه — ومخطئاً مع الواقع. هذا يعني:

> الاتساق الداخلي لا يُثبت الصحة. أي جدول يعكس واقعاً خارجياً
> (بادئات، رموز محافظات، طرازات أجهزة) يحتاج تحققاً دورياً من مصدر
> خارجي، لا مراجعة داخلية.

**المرشحون للتحقق التالي**: رموز محافظات الهاتف الثابت السبعة،
وقدرات طرازات DINSTAR في `DinstarModelProfileTest`.

### الرابعة — الـ100 تقرير في المجلدات الفرعية

75 في `RED_Ultimate/` و25 في `RED_Ultimate_V1-main/`. بعضها مرجعي حيّ
(`API_REFERENCE.md`, `DEPENDENCY_POLICY.md`) وبعضها لقطات كالتي
أرشفناها. المنهج نفسه ينطبق: قراءة فعلية، وتحقق من الادعاءات مقابل
الواقع، لا حكم بالحجم أو التاريخ.

### الخامسة — تفكيك `RedDashboard.kt`

3,603 سطر في ملف واحد، وخطة التفكيك موثَّقة في
`ui/screens/README_SPLIT.md` مع هياكل جاهزة. هذا **دَيْن تقني معروف
ومخطَّط**، لا عيب مكتشَف — يُنفَّذ حين تسمح الأولويات.

---

## 5) مبدأ يُحفظ من هذه الجولة

ثلاث فجوات من الخمس كانت **أعطالاً صامتة**: لوحة تبدو سليمة، منفذ `0`
يبدو حقيقياً، أحداث تُبثّ ولا تُستقبل. لا شيء منها يرفع خطأً ولا يظهر
في سجل.

> أخطر ما في نظام تشغيلي ليس ما يُعلن فشله، بل ما يفشل بهدوء وهو
> يعرض واجهة سليمة.

لذا وُجّهت الاستعادة إلى **إظهار الفشل**: `DINSTAR_EXCEPTION` صار خطأً
ظاهراً لا سطر سجل، و`WsConnectionState` صارت معروضة، و`port = -1`
يميّز الغياب عن المنفذ الأول.

---

## 6) جولة الدمج والمؤتمرات — تحديث لاحق

### ما أُغلق

**الدمج مع `main` (14 تعارضاً على دفعتين)** — صار الفرع مطابقاً
لـ`main` وPR #46 من `CONFLICTING` إلى `MERGEABLE`. القاعدة التي حكمت
الحسم: *اقرأ نيّة الطرفين، فقد يكون كلٌّ منهما يصلح عطباً مختلفاً*.
فلم يكن الحسم «جانبي» ولا «جانبهم» بل خليطاً بحسب الحجّة:

- أُخذت من `main` إصلاحات حقيقية كنا نفتقدها: `PostVisibility.FRIENDS`
  المستعملة فعلاً في ترشيح الفيد، وحقل `waveform` في القصص،
  و`PostVisibilityPicker` الذي جعل تثبيتنا لـ`visibility` تحايلاً
  زائداً يُلغي اختيار المستخدم، و`hangup` المصادَق الذي يمنع العميل
  من اختيار المنفذ.
- ورُفضت من `main` أطروحات أضعف: تجزئة كلمة مرور الغرفة العارية مقابل
  PBKDF2 (ونسختها لا تُترجم أصلاً لأنها تستدعي دالة محذوفة)، ولوحة
  ألوان ترسب في أربعة قياسات تباين، وكتل واجهة تُنتج تعريفات مكرّرة
  بعد التقسيم.

**البند ⑧ المؤتمرات** — كان نظام المنصّة معلناً في الواجهة وغير
مُنفَذ في الخادم: `PRODUCE` تمرّ بلا فحص دور، فأي مستمع ينشر صوته
وصورته و`APPROVE_SPEAKER` زينة. وأُضيف حفظ الأيدي المرفوعة والكتم،
بعد أن كانا يُبَثّان ولا يُحفَظان.

**البند ④ المحادثات** — الأزرار موصولة، لكن رفض الإذن كان ينتهي بلا
`else`: لا رسالة ولا سبب.

### ما تأكّد من مبدأ القسم الخامس

الأعطال الثلاثة في هذه الجولة صامتة كسابقاتها: ثغرة نشر لا ترفع خطأً،
وحالة تُبَثّ ولا تُحفَظ، وزرّ لا يفعل شيئاً. ويُضاف إليها نمط جديد:

> **الجدول الذي يعكس واقعاً خارجياً يفسد بالتحرير الحسن النيّة.**
> أعادت `main` جدول بادئات المشغّلين مقلوباً (73 سبأفون، 71 واي)
> بينما الصواب 71 سبأفون و73 يو. لم يكن خطأً مطبعياً بل جدولاً ثانياً
> أُحيي بعد أن أُلغي عمداً. وهذا يؤكّد ترشيح القسم الرابع: ما يعكس
> واقعاً خارجياً يحتاج **مصدراً واحداً** و**تحققاً خارجياً دورياً**،
> ولا يكفي أن يكون متسقاً مع نفسه.

### الدَّين الباقي

`ActiveCallScreen.kt` محجوب · تباين `Border/الخلفية` 1.70 دون 3:1 ·
تطبيق `ci-proposed/ci-workflow.yml` يدوياً (الدفع إلى `.github/`
محجوب بالصلاحيات) · `./gradlew` لم يُشغَّل (لا JDK في البيئة) ·
تفكيك `RedDashboard.kt`.
