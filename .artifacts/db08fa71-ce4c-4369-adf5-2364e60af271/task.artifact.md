# قائمة مهام الاستعادة العميقة (Deep Restoration)

- `[/]` استعادة لوحة التحكم (Admin Dashboard)
    - `[ ]` استعادة كافة الملفات في `src/pages/tabs/`
    - `[ ]` استعادة الملفات المفقودة في `src/pages/` (مثل `MasterLayout`, `UserApproval`)
    - `[ ]` تحديث `App.tsx` و `api.ts` لضمان تفعيل كافة الميزات المستعادة.
- `[ ]` استعادة تطبيق الأندرويد (Android App)
    - `[ ]` مزامنة موديول `android/` بالكامل.
    - `[ ]` مزامنة موديول `red-app/` لضمان وجود كافة الميزات (Luxury Bubble, Live Broadcast, etc.)
    - `[ ]` استعادة موديولات `core/`, `lib/`, `feature/` المفقودة.
- `[ ]` استعادة الباكند (Backend Server)
    - `[ ]` التأكد من وجود كافة الـ Controllers والخدمات في `src/main/kotlin/`.
- `[ ]` التحقق من سلامة البناء (Build Verification)
    - `[ ]` تشغيل بناء لوحة التحكم.
    - `[ ]` تشغيل بناء الأندرويد.
