# 📡 أقوى التقنيات والمكتبات للمكالمات + أخطاء منطق المكالمات الحالية

> **التاريخ:** 2026-08-11
> **البحث:** WebRTC 2026 + SFU + codecs + noise suppression (LiveKit, GetStream, libwebrtc, AV1/VP9/H.264)
> **الأساس:** فحص كود المكالمات في `red-app` (24 ملفًا)

---

## 1) أخطاء ومنطق خاطئ في المكالمات الحالية (مكتشفة بالفحص)

### 🔴 خلل 1: `START_NOT_STICKY` يقتل خدمة المكالمة في الخلفية
**الموقع:** `YounesCallService.onStartCommand` ترجع `START_NOT_STICKY`
**النتيجة:** عند ضغط النظام على الذاكرة، يقتل الخدمة (المكالمة تنقطع). **يجب** `START_STICKY` لإعادة إنشاء الخدمة تلقائيًا.

### 🔴 خلل 2: المؤتمرات/البث لا يملكون "مكالمة واردة" إشعارية
**الموقع:** `ConferenceService` / `LiveStreamService`
**النتيجة:** لا رنين ولا قبول/رفض للانضمام لمؤتمر/بث — فقط إشعار جارٍ. يحتاج منطق دعوة واردة.

### 🔴 خلل 3: إشعار "قبول" للمكالمة يفتح التطبيق بدل قبول المكالمة
**المُصلح سابقًا** (في `YounesCallService.incomingNotification`) — الآن الزر يستدعي `ACTION_ACCEPT`. ✅

### 🟠 خلل 4: `CallReconnectManager` يعيد WebSocket لكن لا يعيد PeerConnection
**الموقع:** `CallReconnectManager.start()` يعيد الاتصال بالإشارة فقط؛ لا يُعيد إنشاء `PeerConnection` والتفاوض → الفيديو/الصوت لا يستأنفان بعد انقطاع الشبكة.

### 🟠 خلل 5: معرّف المحادثة الفردية مقتطع 32 حرفًا
**الموقع:** `conversationId()` تأخذ `.take(32)` من SHA-256 (64 حرفًا) — **تصادمات محتملة** (نظريًا) بين محادثتين. و`onEnvelope` يحسم مجموعة vs فردية بطول `>32` — هشّ (UUID=36 >32 لكن hash=32) — يعمل لكن غير مثالي.

### 🟠 خلل 6: لا دعم لكوديكس AV1/VP9 في مفاوضات SDP
**الموقع:** `WebRtcEngine` يستخدم DefaultVideoEncoderFactory (VP8/H.264). لا يُفضّل VP9 SVC أو AV1 للجودة/البنطاق رغم دعم الأجهزة الحديثة.

### 🟡 خلل 7: لا تصعيد ترتيب كوديكس ولا `preferredPayloadTypes`
لا يُضبط ترتيب أفضليات الكوديكس ولا يدعم VP9-SVC للمجموعات الكبيرة.

---

## 2) أفضل التقنيات والمكتبات الحديثة (من بحث الويب 2026)

### 2.1 محرّكات WebRTC
| التقنية | لماذا | تناسبك |
|---|---|---|
| **libwebrtc** (Google, `io.github.webrtc-sdk`) | ✅ مستخدم الآن — المعيار الرسمي، سيطرة كاملة | الحالي |
| **GetStream webrtc-android** | مكتبة WebRTC محدّثة باستمرار + KTX + Compose | **ترقية مباشرة** لنفس البنية |
| **LiveKit Android** | SFU مفتوح المصدر + عميل Compose جاهز، تدريج جودة تلقائي | الأفضل للمجموعات الكبيرة |

> **توصية:** استبدل `io.github.webrtc-sdk:android` بـ **GetStream** (`io.getstream:stream-webrtc-android`) للحصول على أحدث تحديثات libwebrtc + ميزات حديثة دون تغيير بنية الكود كثيرًا.

### 2.2 كوديكس الفيديو (Codecs 2026)
| الكوديك | متى تستخدمه | لوضعك |
|---|---|---|
| **VP8** | التوافق العام | المتوافق |
| **H.264** | أقصى توافق مع الأجهزة | الموصى به للمكالمات الفردية |
| **VP9 + SVC** | **المجموعات/البث** (طبقات جودة) — يوفر بنطاق 40-60% | **الأهم للمجموعات** |
| **AV1** | ضغط أفضل 30-50% لكن يحتاج أجهزة حديثة | تجريبي للمستقبل |

> **التوصية:** أضف VP9-SVC كأولوية للمؤتمرات/البث (بفضل `enableIntelVp8Encoder`/إعدادات SVC في libwebrtc).

### 2.3 تحسين الصوت (Noise/Echo/AGC)
- **AEC + NS + AGC** مدمجة في WebRTC — **مفعّلة بالفعل** في `WebRtcEngine` ✅
- **RNNoise** — كشف ضجيج مفتوح المصدر للخلفية
- **Picovoice Koala** — كشف ضجيج بتعلم عميق على الجهاز (خصوصية تامة)
- **Opus** — الكوديك الصوتي القياسي (~32kbps) مدمج

### 2.4 معمارية التوزيع (SFU)
| الخيار | الوصف |
|---|---|
| **SFU (Selective Forwarding Unit)** | يعيد توجيه الحزم دون خلط — أفضل للمجموعات (LiveKit/Jitsi) |
| **Simulcast** | ✅ مستخدم الآن (3 طبقات HD/SD/LD) — أساس جيد للمجموعات |
| **Pion/Go SFU** | خفيف مفتوح المصدر للخادم |

> **التوصية:** أبقِ Simulcast + أضف دعم VP9-SVC في طبقات الجودة.

### 2.5 مكتبات إضافية حديثة
| المكتبة | الوظيفة | الفائدة |
|---|---|---|
| **androidx.media3** | ✅ مستخدم (ExoPlayer) | تشغيل |
| **androidx.core-telecom** | ✅ مستخدم (TelecomBridge) | مكالمات النظام |
| **Haze (dev.chrisbanes.haze)** | تأثيرات ضبابية | جمال الواجهة |
| **Lottie** | ✅ مستخدم | أنيميشن المكالمة |

---

## 3) خطة تفعيل المكالمات بأحدث التقنيات

### المرحلة 1 — إصلاح المنطق (عاجل)
1. **`START_STICKY`** بدل `START_NOT_STICKY` في كل خدمات المكالمات (يمنع القتل في الخلفية)
2. **إعادة إنشاء PeerConnection** في `CallReconnectManager` بعد إعادة الاتصال
3. **إشعار "دعوة واردة"** للمؤتمرات/البث (رنين + قبول/رفض)

### المرحلة 2 — ترقية المحرّك
4. الترقية إلى **GetStream webrtc-android** (أحدث libwebrtc)
5. **VP9-SVC** كأولوية للمؤتمرات/البث
6. **ترتيب كوديكس** ذكي (H.264 للفردي، VP9-SVC للمجموعات)

### المرحلة 3 — الجودة والتجربة
7. **RNNoise/Picovoice** ككشف ضجيج إضافي (اختياري)
8. **واجهة مكالمة جماعية** بشبكة فيديو أنيقة (grid) بدل overlay واحد
9. **Picture-in-Picture** للفيديو (مضبوط جزئيًا)
10. **جودة تكيفية أذكى** (downgrade قبل انقطاع الصوت)

---

## 4) الخلاصة

**قوي الآن:** E2EE + Simulcast + AEC/NS/AGC + TelecomBridge + Call Waiting + 5 أنواع مكالمات.

**أضعف نقطة (يجب إصلاحها أولًا):** `START_NOT_STICKY` + عدم إعادة PeerConnection بعد الانقطاع + لا إشعار واردة للمؤتمرات.

**أفضل ترقية تقنية:** GetStream webrtc-android + VP9-SVC للمجموعات + واجهة شبكة فيديو جماعية.

> **الأولوية القصوى:** إصلاح دورة الحياة (`START_STICKY`) وإعادة الاتصال — بدونها أي مكالمة قد تنقطع في الخلفية.
