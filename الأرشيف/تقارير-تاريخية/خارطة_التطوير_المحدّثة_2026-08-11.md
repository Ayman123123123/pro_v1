# 🗺️ خارطة تطوير «يونس / RED Sovereign» — المحدّثة بعد فحص الكود الفعلي

> **التاريخ:** 2026-08-11
> **الأساس:** فحص حقيقي للكود (red-app + backend-server + shared-proto + DB schema) — لا تخمين
> **الهدف:** مقترحات دقيقة قابلة للتنفيذ، مرتبة بالأولوية، مع تحديد مكان التنفيذ في الكود

---

## 0) ما أكدّته من فحص الكود (الحقيقة بلا وهم)

### ✅ مكتوب وموجود فعلاً في الكود
| الميزة | الموقع | الحالة |
|---|---|---|
| E2EE + PQXDH + Double Ratchet | `libsignal-service`, `crypto/` | ✅ حقيقي |
| مكالمات: VoIP + Conference + LiveStream | `calls/` (6 ملفات) | ✅ مكتوب |
| Stories 24h | `stories/` + `StoryEntity` | ✅ مكتوب |
| Social Feed + Communities + Polls | `social/`, `features/communities/`, `media/PollsScreen` | ✅ مكتوب |
| بحث محلي FTS5 | `features/chat/RedGlobalSearch.kt` | ✅ مكتوب |
| رسائل ذاتية التدمير | `core/delivery/BurnManager.kt` | ✅ مكتوب |
| PSTN / DINSTAR | `features/dinstar/`, `pstn-asterisk/` | ✅ مكتوب (ميزة فريدة) |
| SQLCipher + FLAG_SECURE + Cert Pinning | `core/database`, `security/`, `CertificatePinner` | ✅ مكتوب |
| حقل `avatarUrl` + `lastSeen` | `ContactEntity`, `GroupEntity`, `UserAccount` | ⚠️ الحقل موجود بالـ schema |
| `drafts` table | `DraftEntity` | ✅ مكتوب |

### ❌ مؤكد النقص بعد الفحص (هذه فجوات حقيقية)
| الفجوة | الدليل من الفحص |
|---|---|
| **Reactions على الرسائل غير موجودة** | لا `reaction` في `red_protocol.proto`، ولا في `Entities.kt` — النتائج التي تظهر كلها للمكالمات والـ feed |
| **شاشة البروفايل شبه فارغة** | مجلد `features/profile/` فيه فقط `BackupScreen.kt` — رغم أن `avatarUrl` معرّف في الـ schema! |
| **Biometric / AppLock غير موجود** | بحث `biometric` في red-app = صفر نتائج |
| **Stickers / GIF غير موجود** | لا ملفات في red-app ولا بروتوكول |
| **Message Editing غير موجود في proto** | `red_protocol.proto` فيه ChatMessage/Ack/Sync/Typing/Delete فقط — لا `EditRED` |
| **Channels / Broadcast كـ messaging** | غير موجود (النتائج كلها للمكالمات) |
| **`RedDashboard.kt` = 3040 سطر** | أكبر حتى من التقدير القديم (2700) — عبء بنيوي حقيقي |
| **Presence/Typing في proto ناقص** | `TypingRED` موجود لكن لا `PresenceRED` منفصل |

---

## 1) الأولوية A — أساسيات ناقصة (أثر فوري على المستخدم الجديد)

### A1. شاشة البروفايل الكاملة + رفع الصورة 🔥🔥
> **لماذا أولاً؟** أول ما يراه المستخدم، والبنية جاهزة (`avatarUrl` موجود في `ContactEntity` و`GroupEntity` و`UserAccount`) — فقط الـ UI ناقص.

| العنصر | مكان التنفيذ | الجهد |
|---|---|---|
| `ProfileScreen.kt` (صورة + اسم + username + بايو) | `features/profile/` (جديد) | متوسط |
| رفع صورة مشفّر عبر `MediaApi` | `features/profile/` + `media/MediaApi.kt` | متوسط |
| تحديث `avatarUrl` في `ContactEntity`/`UserAccount` | `core/database` + backend `UserAccount` | سهل |
| API backend لتحديث البروفايل | `auth/AuthController.kt` | سهل |
| عرض الصورة في قوائم الدردشة والأصدقاء | `ui/RedDashboard.kt`, `features/contacts/` | سهل |
| مشاركة QR للبروفايل | استعارة من `lib/qr` | سهل |

### A2. Reactions (تفاعلات إيموجي على الرسائل) 🔥🔥
> **لماذا؟** يمتلكها كل المنافسين (Signal/WhatsApp/Telegram)، وهي مفقودة تماماً من proto والـ DB.

| العنصر | مكان التنفيذ | الجهد |
|---|---|---|
| `message ReactionRED` في proto | `shared-proto/src/main/proto/red_protocol.proto` | سهل |
| `MessageReactionEntity` في Room | `core/database/Entities.kt` + migration | متوسط |
| تخزين وتوجيه الـ reaction على الخادم | `backend-server` (جدول + controller) | متوسط |
| UI: شريط إيموجي + عرض التفاعلات تحت الرسالة | `features/chat/` + `RedDashboard.kt` | متوسط |
| E2EE: الـ reaction يُشفّر ضمن الحوار الخاص | ضمن payload | متوسط |

### A3. قفل التطبيق بالبصمة (Biometric AppLock) 🔥
> **لماذا؟** ميزة خصوصية مرغوبة جداً، ولا توجد أي إشارة لها في الكود.

| العنصر | مكان التنفيذ | الجهد |
|---|---|---|
| `AppLockScreen.kt` + `BiometricPrompt` | `security/` (جديد) | متوسط |
| إعداد القفل في الإعدادات | `settings/SettingsScreen.kt` | سهل |
| تفعيل القفل عند `onResume` | `YounesApplication.kt` / النشاط الرئيسي | سهل |
| حفظ تفضيل القفل مشفّراً | `SecureStore` + `AndroidKeystore` | سهل |

---

## 2) الأولوية B — تجربة مستخدم يومية (تنافسية)

| # | الميزة | مكان التنفيذ | الجهد |
|---|---|---|---|
| B1 | **Edit / Unsend** رسالة | إضافة `EditRED` في proto + `editedAt` في `MessageEntity` | متوسط |
| B2 | **Read receipts تفصيلية** (من قرأ في المجموعة) | `MessageEntity.readers` + UI | مرتفع |
| B3 | **Slow Mode + @all/@admin** للمجموعات | `SovereignGroupSystem.kt` + backend `GroupService` | متوسط |
| B4 | **معرض وسائط المجموعة** | شبكة من `MessageEntity` (type=IMAGE/VIDEO) | متوسط |
| B5 | **Stickers + GIF** | مكتبة محلية + optional GIPHY (مع سيادة: محلي أولاً) | متوسط |
| B6 | **Presence حقيقي + آخر ظهور قابل للإخفاء** | `PresenceRED` في proto + `UserStatusService` (موجود!) | متوسط |
| B7 | **تثبيت رسالة (Pinned message)** | حقل `pinnedMessageId` في `ConversationEntity` | سهل |
| B8 | **Note to Self + جدولة الرسائل** | محادثة نظامية + scheduler | متوسط |

---

## 3) الأولوية C — تميّز «يونس» (لا يملكها المنافسون)

| # | الميزة | القيمة التنافسية |
|---|---|---|
| C1 | **تطوير PSTN/DINSTAR**: سجل مكالمات واردة، محادثة واردة، رنين على الـ SIM | ميزة نادرة في السوق العربي/اليمني |
| C2 | **النسخ الاحتياطي المشفر الكامل** (تصدير/استيراد DB مشفّر) | `BackupScreen.kt` موجود كـ UI — أكمل الـ logic | 
| C3 | **ترقية ما بعد الكم (SQPR ratchet)** لمستوى 3 كمّي | تفوّق على واتساب وتلجرام، يضاهي Signal |
| C4 | **الوضع اليمني/العربي**: تصنيفات محلية (مدن/قبائل/أسواق) في الخلاصة | هوية سيادية فريدة |
| C5 | **اكتشاف الخادم المحلي المحسّن**: تجربة اتصال أنضد بالـ LAN | سيادة محلية حقيقية |
| C6 | **قفل محادثة فردية بالبصمة** | خصوصية متقدمة فوق AppLock |

---

## 4) الأولوية D — بنيوي وصيانة (ضروري للاستدامة)

| # | المهمة | الوصف | الجهد |
|---|---|---|---|
| D1 | **تقسيم `RedDashboard.kt` (3040 سطر)** | فصل الـ 5 شاشات الأساسية إلى ملفات مستقلة (محادثات/مكالمات/مجتمع/دليل/إعدادات) — هذا أكبر عبء بنيوي حالياً | مرتفع |
| D2 | **توحيد التخزين على Room** | إزالة `MessageStore.kt` القديم إن أصبح مكرراً | متوسط |
| D3 | **نقل النصوص المضمّنة إلى `strings.xml`** | توطين كامل (عربي RTL + إنجليزي) | متوسط |
| D4 | **`npm ci` + `tsc` في CI** لوحة الإدارة | منع انكسار البناء | سهل |
| D5 | **تثبيت توقيع release** | مفتاح منفصل + رفع versionCode | سهل |
| D6 | **ملء الـ stubs المتبقية** | `MediaService` thumbnails حقيقية + malware scan + backup drill (من ROADMAP) | متوسط |

---

## 5) خطة تنفيذ مقترحة (3 أشهر)

### 📅 شهر 1 — أساسيات مكتملة (A)
1. **شاشة البروفايل + رفع الصورة** (A1) ← البنية جاهزة، فقط تنفيذ
2. **Reactions على الرسائل** (A2) ← proto + DB + UI
3. **قفل التطبيق بالبصمة** (A3) ← خصوصية مرئية فوراً
4. **Edit/Unsend** (B1) ← يكمل تجربة الرسائل

### 📅 شهر 2 — تجربة تنافسية (B)
5. معرض وسائط المجموعات (B4)
6. Slow Mode + @all/@admin (B3)
7. Presence + آخر ظهور (B6)
8. Stickers/GIF محلي (B5)
9. Pinned message + Note to Self (B7, B8)

### 📅 شهر 3 — تميّز + بنية (C + D)
10. **تقسيم RedDashboard.kt** (D1) ← صحة الكود طويلة المدى
11. تطوير DINSTAR (سجل وارد) (C1)
12. نسخ احتياطي مشفر كامل (C2)
13. ترقية ما بعد الكم SQPR (C3)
14. توطين كامل + توقيع release (D3, D5)

---

## 6) التوصية: من أين نبدأ الآن؟

> **أقوى بداية = شاشة البروفايل + رفع الصورة (A1).**

السبب التقني: البنية التحتية **جاهزة فعلاً** — الحقول `avatarUrl` و`displayName` و`username` و`lastSeen` موجودة في `ContactEntity` و`UserAccount`، و`MediaApi` موجود للرفع المشفّر. الشاشة الوحيدة الناقصة هي الـ UI في `features/profile/`. هذا يعني **عائد سريع مقابل جهد معقول**، وهو أول ما يراه المستخدم الجديد فيُحسّن الثقة والهوية.

بعد ذلك مباشرة: **Reactions (A2)** لأنها الأكثر طلباً ومفقودة تماماً من proto والـ DB.

---

*كل ميزة تُنفّذ في Commit منفصل مع اختبار `check-all.sh` أخضر، ووفق بوابة الميزة (Threat model + API/Proto + اختبارات + RTL + evidence) المذكورة في ميثاق المنتج.*
