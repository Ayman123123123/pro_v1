# تقرير التطوير الشامل — 2026-08-19

المرجع: البنود الثمانية عشر لطلب «تطوير شامل واحترافي». هذا التقرير
يسجّل **ما نُفِّذ وتُحقِّق منه فعليًّا**، ويفصل بوضوح بين ما جرى إثباته
بتشغيل أداة وبين ما جرى إثباته بالتحليل الساكن.

---

## قيد التحقق: ما الذي يمكن إثباته في هذه البيئة؟

بيئة العمل **لا تحتوي على JVM** (`java` غير موجود، ولا صلاحية تثبيت،
ولا وصول شبكي إلى مستودعات الحزم). لذلك:

| الطبقة | إمكان الترجمة | وسيلة التحقق المستعملة |
|---|---|---|
| `admin_dashboard` (TS/React) | ✅ ممكنة | `tsc --noEmit` + `vite build` + 4 حرّاس — **تشغيل فعلي** |
| `red-app` و`backend-server` (Kotlin) | ❌ متعذّرة | تحليل ساكن صارم + محاكاة تنفيذية للمنطق |

**ما يعنيه هذا بأمانة:** تغييرات Kotlin في هذا التقرير **لم تُترجَم**.
تُحقِّق منها بجرد الرموز وتوازن الأقواس وشمول `when` ومطابقة أسماء
الحقول بالملفات الفعلية، ومنطقُها الحسابي حوكي في Python قبل كتابة
التوقّعات. وهذا أقوى من المراجعة بالعين، لكنه **ليس بديلًا عن
`./gradlew test`**، وينبغي تشغيله على جهاز فيه JDK قبل الإصدار.

---

## ما أُنجز في هذه الجولة

### البند 10 — المنشورات: «الأصدقاء» و«عام» بدل «المتابَعة» ✅

الاستبدال **دلاليّ لا لفظيّ**، وهذا جوهر التنفيذ:

- «المتابَعة» علاقة **أحادية**: أتابعك دون أن تتابعني.
- «الأصدقاء» علاقة **متبادلة**: `FeedService.friendIds` يحسب تقاطُع
  مجموعتين — من أتابعهم ∩ من يتابعونني.

| الطبقة | التغيير |
|---|---|
| الخادم | `FeedScope` ⇐ `ALL / FRIENDS / PUBLIC` |
| الخادم | `friendIds()` بتقاطُع مجموعتين |
| التطبيق | تبويبات «لك / الأصدقاء / عام» من `FEED_TABS` واحدة |

**توافُق رجعي مقصود:** أُبقيت `FOLLOWING` و`YEMEN` مهجورتين
(`@Deprecated`) لأن النسخ المثبَّتة على أجهزة المستخدمين ما زالت
ترسلهما؛ حذفهما كان سيجعل Spring يردّ **400** على كل طلب منها.
تُطابَقان إلى البديل عبر `canonical()`.

**عيب اكتُشف أثناء العمل وأُصلح:** `FeedViewModel.createPoll` كان
يمرّر `scope` (مُرشِّح الفيد) مكان `visibility` (مدى ظهور المنشور)،
وهما تعدادان مختلفان في الخادم. مع التبويبات الجديدة كان سيُرسل
`FRIENDS` قيمةَ `visibility` غير موجودة ⇒ **400 عند نشر أي استطلاع**.

**الحارس:** `FeedFriendsScopeTest` — 7 اختبارات تثبّت التبادل، وتمنع
عودة الأحادية، وتغطّي مطابقة القيم المهجورة. منطق التقاطُع حوكي
تنفيذيًّا على 4 حالات قبل كتابة التوقّعات.

---

### البندان 2 و3 و16 — توحيد الهوية بين التطبيق ولوحة الإدارة ✅

كانت اللوحة تحمل هوية مختلفة عن التطبيق. وحُّدت، وكشف القياس **عيبين
مقيسين لا مسألتَي ذوق**:

| العنصر | قبل | بعد |
|---|---|---|
| النص الباهت على السطح | **2.91:1 راسب** | 5.32:1 AA |
| نص أبيض على زر أخضر | **2.16:1 راسب** | 7.09:1 AAA |
| المشغّل «غير معروف» (التطبيق) | **4.15:1 راسب** | 8.34:1 AAA |

- **13 زوجًا في اللوحة، صفر راسب.**
- لم يُكتفَ بمتغيّرات `:root`: طوردت القيم المثبَّتة في **26 ملف tsx**
  وفي قواعد CSS تتجاوز المتغيّرات (خلفية لوح المحتوى، نقش SVG،
  ألوان الخطأ) — **صفر بقايا**.
- **الخطوط:** `Cairo + Tajawal` ⇒ `IBM Plex Sans Arabic` (نفس عائلة
  التطبيق). كان الملف يشحن **15 وزنًا** أكثرها غير مستعمل ⇒ **8
  استيرادات / 268 كB**، محلّية كما تفرض سياسة `font-src 'self'`.

**تنبيه مقصود:** ألوان المشغّلين الأربعة (سبأفون، يو، يمن موبايل،
واي) **علامات تجارية فلم تُمسّ**، وكلها تتجاوز 4.5:1 أصلًا. المعدَّل
هو «غير معروف» وحده لأنه غياب تعرُّف لا علامة.

---

### البند 14 — إخراج لوحة إدارة الأسطول من تطبيق المستخدم ✅

`DinstarViewModel` (28 كB) + `DinstarWebSocketBridge` (13.6 كB) كانا
**عنقودًا ميتًا مغلقًا**: صفر مراجع للأول، ومستهلك الثاني الوحيد هو
الأول.

**والأهم أنه لم يكن ميتًا فحسب:** كان يستدعي **11 مسارًا** تحت
`/api/admin/dinstar/**` (تشغيل/إطفاء المنافذ، إعادة الضبط، USSD،
طوابير SMS) — وكلها تتطلب دور ADMIN فتردّ **403** لكل مستخدم عادي.

**النتيجة المقيسة:** حارس `check:roles` كان يفشل بـ11 مخالفة ⇒ **يمرّ
نظيفًا الآن** (تشغيل فعلي).

**ما مُنع عمدًا:** لم يُحذف `DinstarModels.kt` رغم إغراء ذلك، لأن
`YemenOperator` فيه مستعمَل في **19 موضعًا حيًّا** (كاشف المشغّل وشارة
الواجهة). حذفه كان سيكسر كاشف المشغّل اليمني.

---

### البند 13 — الترتيب: تصفير الشاشات اليتيمة ✅

| قبل | بعد |
|---|---|
| 2 يتيمة من 40 | **0 من 40** |

- **`DeviceSettingsScreen`** (544س): شاشة إعدادات ثانية موازية للحيّة،
  **قائمة ساكنة** تتنقّل بنصّ العنوان المعروض (`onNavigate(entry.title)`)
  والمعالِج نفسه `{}` افتراضيًّا ⇒ وصلها كان سيعرض تسعة أقسام لا يفعل
  أيٌّ من بنودها شيئًا. البديل الحيّ `SettingsScreen` عبر
  `YounesSettingsSheet`: **12 قسمًا** موصولة بـViewModels حقيقية، وقسم
  الأجهزة يحمّل ويلغي الاعتماد ويعرض انتهاء الشهادة فعلًا.

- **`IncomingPstnCallScreen`** (205س): شاشة **بلا مصدر بيانات**.
  تُوتبعت السلسلة كاملة: `DinstarEventListener` يعالج أحداث المكالمة
  **الصادرة** فقط، ولا إشارة `PSTN_INCOMING` في الطرفين. والتوجيه غير
  ممكن أصلًا: **لا حقل هاتف في `UserAccount`** ولا جدول يربط منفذ
  البوابة بمستخدم.

**استُخرج قبل الأرشفة:** `formatPhoneNumber` ⇐
`features/pstn/PhoneNumberFormat.kt` (منطق سليم قائم بذاته)، ويحرسها
`PhoneNumberFormatTest` — 7 اختبارات تشمل الحدود 4/6/7/9 وخاصية عدم
فقد أي رقم. سلوكها حوكي تنفيذيًّا قبل كتابة التوقّعات.

---

## بنود جرى التحقق من اكتمالها (فلم تُعدَّل بلا داعٍ)

| البند | الحالة الفعلية |
|---|---|
| **7 — البث بدون هدايا** | **صفر أثر** لأي `gift/هدايا/coins/diamond` في التطبيق والخادم واللوحة. والبث فيه مشاهدون وتفاعلات ودردشة حيّة و**simulcast** للجودة التكيّفية. |
| **9 — المكالمات الجماعية منفصلة عن المجموعات** | `ConferenceService` **لا يعرف `groupId` إطلاقًا**. ومركز المكالمات يفصل: فردية / مؤتمر / بث / مساحة، ولكلٍّ مدخل مستقل. |
| **5 — مكالمات داخل المجموعات** | موصولة عبر `ConferenceService.join` بأعضاء المجموعة — تتعايش مع البند 9 دون تعارض. |
| **12 — الإعدادات متكاملة** | 12 قسمًا حيًّا موصولًا بـ`SettingsViewModel` و`DeviceSettingsViewModel`. |

---

## دَين سابق أُغلق بالتحقق

| الدَّين | النتيجة |
|---|---|
| `shouldAutoDownload` مزدوج | **ليس تكرارًا**: الثانية اسم مستعار رفيع يفوّض إلى `RedQualityManager`. سليم. |
| `PstnCallService:27-29` جدول بادئات رابع خاطئ | **زال**: حلّ محلّه ثابت `MOBILE_NSN_LENGTH`. الجدولان الباقيان (الخادم/التطبيق) **متطابقان**: 70/71/73/77/78 + 722. |
| `DataStore` شرطٌ لـ`DeviceSettingsScreen` | **شرط لاغٍ**: الشاشة لا تعتمد على DataStore أصلًا، وقد أُرشفت لسبب آخر. |

---

## أرقام التحقق النهائية

```
لوحة الإدارة (تشغيل فعلي)
  tsc --noEmit ................. خرج 0
  vite build ................... ✅ (خطوط 268 كB / 8 ملفات)
  check:api .................... ✅
  check:guards ................. ✅ 43 ملفًا
  check:roles .................. ✅ (كان يفشل بـ11)
  check:contrast ............... ✅ 13 زوجًا، صفر راسب

Kotlin (تحليل ساكن)
  توازن الأقواس ................ 414 ملفًا، صفر خلل
  شاشات يتيمة .................. 0 من 40
  مراجع مكسورة للمؤرشف ......... 0
  الاختبارات ................... 537 @Test (red-app 253 / backend 284)
```

**الحرّاس مُختبَرة بالطفرة**: `check-contrast.mjs` يفشل فعليًّا عند
إعادة اللون القديم، وعند النص الأبيض، وعند حذف متغيّر — تُحقِّق منها
بثلاث طفرات ثم استُعيد الملف.

---

## ما يلزم من المستخدم

1. **تشغيل الاختبارات على جهاز فيه JDK** — لا يمكن هنا:
   ```
   ./gradlew :app:testDebugUnitTest
   cd backend-server && ../gradlew test
   ```
2. **مراجعة قرار «الأصدقاء = متابعة متبادلة»** — هذا تفسيري للطلب.
   إن كان المقصود «كل من أتابعهم» فالتعديل سطر واحد في `friendIds`.

## ما تبقّى مفتوحًا

- تفكيك `ChatHubScreen` (~1,491س) — آخر كتلة كبيرة في `RedDashboard.kt`.
- مكالمات البوابة الواردة — **محجوبة ببنية بيانات ناقصة**: تحتاج ربط
  (منفذ/رقم ← `redId`) في الخادم أولًا. الشرط موثّق في
  `_archive/README.md`.
- `ActiveCallScreen.kt` — محجوب، يحتاج لصقًا من جهازك.

---

# الجولة الثانية — اكتشاف حاسم

## عطبٌ كان يمنع ترجمة التطبيق كلّه

في `RedDashboard.kt:569`:

```kotlin
var messageText by remembr { mutableStateOf("") }   // ← مرجع غير قابل للحلّ
var messageText by remember { mutableStateOf("") }  // ← والمتغيّر معلَن مرتين
```

خطآن في سطرين متتاليين، **كلٌّ منهما وحده يُفشل ترجمة وحدة `:app`
كاملةً**. السطر الأول نسخة معطوبة، فحُذف وبقي الصحيح.

**مصدره `commit` الأساس `69277ec` (دمج PR #40)، لا هذه الجلسة.** أي
أن تطبيق الأندرويد **لم يكن يُترجَم على هذا الفرع أصلًا**.

### لماذا لم يُكتشف قبل الآن؟

لأن فحص `build` في CI كان **قالبًا افتراضيًّا**:

```yaml
- name: Run a one-line script
  run: echo Hello, world!
```

فكان يظهر **أخضر** على الدوام دون أن يُترجم شيئًا. وهذا هو الدرس
الأهمّ في هذه الجولة: **فحصٌ أخضر كاذب أسوأ من غياب الفحص**، لأنه
يمنح ثقة بلا أساس ويُخفي عطبًا قاتلًا لأشهر.

## ما بُني لمنع التكرار

**`scripts/check-kotlin-structure.py`** — فاحص بنيوي يعمل بلا JDK:
توازن الأقواس، وتكرار تعريف `by remember`، والأخطاء الإملائية في
دوال Compose، والاستيراد المكرّر، ومراجع الرموز المؤرشفة.

التجريد **بآلة حالات لا بتعبيرات نمطية** — قرار مبنيّ على فشل مقاس:
التتابع بالـregex أعطى **35 إنذارًا كاذبًا** مهما رُتّب، لأن النصوص
والتعليقات متداخلة تعريفًا، ولأن أقواسًا داخل تعليقات عربية
(`{redId}` في توثيق مسار) كانت تُحسب أقواسًا حقيقية.

**مُختبَر بالطفرة:** يلتقط العطب الأصلي، والقوس الناقص، والاستيراد
المكرّر — ثم يعود نظيفًا على **413 ملفًا**.

**`ci-proposed/`** — أربع مهام CI تُنفَّذ فعلًا. الدفع إلى
`.github/workflows/` **رُفض لغياب صلاحية `workflows`** عن تطبيق
GitHub في هذه الجلسة، فحُفظت مع دليل تطبيق يدويّ.

## تفكيك ChatHubScreen — قياس قبل التقسيم

`ChatHubScreen`: **1,492 ⇒ 1,465 سطرًا** بفصل `SafetyNumberDialogs.kt`.

الفصل كان محدودًا **عن قصد**. قِست لكل كتلة مرشَّحة كمّية الحالة
المشتركة التي يلزم تمريرها:

| الكتلة | الأسطر | متغيّرات مشتركة | منها متغيّرة |
|---|---|---|---|
| حوار رمز الأمان | 35 | **4** | 2 |
| بطاقة جهة اتصال | 69 | 5 | 3 |
| قائمة رسالة | 87 | 15 | 9 |
| المحادثات + المجموعات | 790 | **31** | 18 |

وكل الدوال المفصولة سابقًا في هذا الملف **دون ستّة معاملات**. فصلٌ
بـ31 معاملًا يخالف المعيار ويُنتج شيفرة **أسوأ** من الحالة الراهنة.
لذلك فُصلت الكتلة المؤهَّلة وحدها، وتُركت البقيّة مع توثيق السبب —
لا عجزًا بل لأن الفصل القسري خسارة صافية.

---

# الجولة الثالثة — أعطال صامتة

الجامع بين كل ما في هذه الجولة: **ميزات تبدو مكتملة في الشيفرة وهي
معطَّلة كليًّا وقت التشغيل**، ولا يكشفها مترجم ولا مراجعة بصرية.

## ١ — البحث المحلي لم يكن يعمل إطلاقًا

عيبان، كلاهما مقيس بتنفيذ SQL فعلي لا بالقراءة:

**الجدول لم يكن يُنشأ.** صيغة المُجزِّئ كانت تضع اقتباسًا مزدوجًا
داخل المفرد، فيردّها FTS5 بـ`parse error in tokenize directive`.
وابتلاعُ الخطأ بـ`catch (_: Exception) {}` أخفى ذلك، فكان كل بحث
يرجع فارغًا بلا أثر في السجلّ.

**والأهمّ للعربية:** `unicode61` يعدّ الحركات (فئة `Mn`) **فاصلًا بين
الكلمات**:

```
"السَّلامُ عليكم"  ⇒  ['الس', 'لام', 'عليكم']
```

فكلمة واحدة تصير رمزين مبتورين لا يطابقهما بحثٌ عن «السلام». وخيار
`remove_diacritics` لا يعالجها بأيٍّ من قيمه — جُرّبت الثلاث.

الحلّ `normalizeArabic` على طرفَي العملية. ومكسبٌ إضافي: صار
«ابراهيم» يطابق «إبراهيم» و«مكتبه» تطابق «مكتبة» — أشيع خطأين
إملائيين في الكتابة اليومية.

**ولماذا لم يُكتشف؟** الاختبار كان يستنسخ المنطق داخله بدل استيراده،
فبقي أخضر والبحث معطَّل. أُعيدت كتابته ليستورد الشيفرة الحقيقية.

## ٢ — `LIKE` على عمود BLOB

`encryptedPlaintext` من نوع `ByteArray` ⇒ `BLOB`، و`LIKE` على BLOB
**لا يطابق شيئًا أبدًا**: لا يخطئ بل يرجع صفر صفوف صامتًا. أُصلح
بـ`CAST`. ومعه ثغرة: `%` و`_` محرفا بدل، فكان بحث المستخدم عن «%»
يطابق **كل** رسائل المحادثة.

## ٣ — عقود JSON: العطب نفسه ثلاث مرات

| المرسَل من العميل | المقروء في الخادم | الأثر |
|---|---|---|
| `scope` | `visibility` | 400 عند إنشاء استطلاع |
| `visibleTo` | `visibility` | **إنشاء أي قصة يفشل** |
| `mediaKeys` | `media` | 400 عند إرفاق وسيط |

لا مترجم يشتكي: كلُّ طرف يترجم وحده بنجاح.

وفي القصص تحديدًا كان الاحتمال الأسوأ قائمًا: لو عُطّل
`FAIL_ON_UNKNOWN_PROPERTIES` يومًا، لصار الحقلان يُهملان صامتَين
فتُنشأ القصة بخصوصية `CONTACTS`. عندها يختار المستخدم «أشخاصًا
محدَّدين» فتُنشر قصّته على **كل** جهات اتصاله دون أن يعلم. وهذا
تسريب خصوصية، لا خلل عرض.

إصلاح كلٍّ على حدة يترك النمط قائمًا، فبُني
`scripts/check-api-contracts.py`: يوازن 42 صنفًا مشتركًا وينذر عند
حقل يرسله العميل ويجهله الخادم. مُختبَر بالطفرة ×3، منها طفرة تتحقّق
من **عدم** الإنذار الكاذب عند `@SerialName` صحيح.

## ٤ — لونان راسبان في WCAG

لونٌ واحد كان يؤدّي دورين متناقضين: حاويةَ زرّ (يُقاس نصُّه عليها،
فتسوء كلّما فتح اللون) وعلامةً على سطح داكن (تُقاس هي عليه، فتسوء
كلّما غمق). فرسب كلٌّ في دوره الخطأ — 4.23:1 و2.08:1. الفصل إلى
`*Container` و`*Accent` يُنجح الأربعة.

## الأرقام

| المقياس | القيمة |
|---|---|
| ملفات Kotlin بلا مشكلة بنيوية | 415 |
| عقود JSON مفحوصة | 42 |
| `@Test` | 548 |
| فحوص اللوحة | 6/6 ✅ |
