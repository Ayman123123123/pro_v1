# تقرير فحص شامل — تطبيق أندرويد يونس / RED Sovereign

التاريخ: 2026-08-13  
النطاق: الوحدة القانونية `RED_Ultimate_V1-main/RED_Ultimate/red-app` (147 ملف Kotlin).  
مجلد `app/` القديم (شوكة Signal) **خارج رسم البناء** حسب `settings.gradle.kts` — ليس المنتج.

لم أُبنَ APK في هذه الجلسة (لا Android SDK كامل مفترض هنا). كل بند أدناه مُستخرج من قراءة الملفات مباشرة.

---

## 1) ما هو التطبيق فعلياً؟

منتج Compose مستقل (`applicationId = com.red.sovereign`) وليس Signal معاد التسمية.

| عنصر | الواقع في الكود |
|---|---|
| الدخول | بدون رقم/SMS — تسجيل اسم + كلمة مرور + موافقة إدارة |
| النقل | WebSocket محلي `/ws/master` + REST على `RED_SERVER_URL` |
| التشفير | libsignal (جلسات فردية) + توزيع مفاتيح مجموعات |
| التخزين | Room + SQLCipher |
| المكالمات | WebRTC + SFU + مؤتمر + بث + مسار PSTN/DINSTAR |
| الدفع | لا Firebase في `dependencies` — لا إشعار سحابي عند قتل العملية |

الخادم الافتراضي في Gradle: `http://192.168.1.50:8088`. إن لم يُمرَّر `-PRED_SERVER_URL` فكل جهاز يضرب هذا الـ IP.

---

## 2) عيوب حرجة (تكسر أماناً أو استخداماً)

### 2.1 قفل التطبيق لا يعمل كما يُعلن

`MainActivity` يرث `ComponentActivity` لا `FragmentActivity`:

```36:36:RED_Ultimate_V1-main/RED_Ultimate/red-app/src/main/java/com/red/sovereign/MainActivity.kt
class MainActivity : ComponentActivity() {
```

`AppLockScreen` يعمل `context as? FragmentActivity` ثم يتوقف إن فشل الـ cast.  
النتيجة: زر «فتح بالبصمة» يبقى معطلاً أو يعرض «تعذّر بدء المصادقة».

### 2.2 تعارض BiometricPrompt

يُضبط معاً:

- `DEVICE_CREDENTIAL`
- `setNegativeButtonText("خروج")`

واجهة Android الرسمية ترمي استثناء: لا يُسمح بزر سلبي مع بيانات اعتماد الجهاز.  
`runCatching` يبلع الخطأ ويُبقي القفل مكسوراً بصمت.

### 2.3 إعادة القفل في كل `onResume`

```151:155:RED_Ultimate_V1-main/RED_Ultimate/red-app/src/main/java/com/red/sovereign/MainActivity.kt
    override fun onResume() {
        super.onResume()
        if (authViewModel.state is AuthState.Authenticated && SettingsRuntime.current.appLockEnabled) {
            appLocked = true
        }
```

لا مهلة، لا تمييز بين أول فتح وعودة من إشعار/PiP/حوار البصمة.  
حتى لو نجح الـ Prompt، دورة حياة الحوار قد تعيد `onResume` فتعيد القفل فوراً.

### 2.4 كلمة المرور تبقى في الذاكرة حتى الموافقة

`pendingCredentials: Pair<String, String>?` يخزّن اسم المستخدم **وكلمة المرور نصاً واضحاً** بعد التسجيل وأثناء `PENDING`، ويُعاد استخدامها في `checkApproval()` عبر `login()`.  
أي تفريغ ذاكرة / نسخة شاشة مطوّر / عملية dump تكشف السر. لا يوجد مسح للمصفوفة بعد الاستخدام.

### 2.5 بناء الإصدار (release) بلا توقيع

في `red-app/build.gradle.kts`:

- `debug` يوقَّع بمفتاح `red-debug.p12` وكلمة `red-debug-only` (معلنة في المستودع).
- `release` يفعّل R8/`minify` **بدون** `signingConfig`.

`assembleRelease` ينتج حزمة غير موقّعة أو يفشل حسب إصدار AGP. لا سياسة `applicationId` منفصلة للإنتاج كما يعلّق الملف نفسه.

### 2.6 تثبيت الشهادات افتراضياً فارغ

`RED_TLS_PINS` الافتراضي `""`.  
`CertificatePinner.verify`: إن لم توجد دبابيس للجذر → `return true`.  
في الإنتاج بدون تمرير pins، الحماية الفعلية هي فقط سلسلة النظام. تعليقات `network_security_config` عن pin-set ما زالت نموذجاً (`AAAA…`).

### 2.7 فقدان بيانات صامت عند فتح قاعدة SQLCipher

`RedDatabase.buildDatabase`: أي فشل فتح (مفتاح خاطئ بعد استعادة، تلف، ترقية) يحذف القاعدة بعد نسخة `.bak` ثم `fallbackToDestructiveMigration()`.  
المستخدم يفقد المحادثات دون شاشة قرار.

### 2.8 لا إشعارات عند إغلاق التطبيق من النظام

`VoipPushRegistrar` يحاول Firebase بالانعكاس. **لا اعتماد Firebase في Gradle.**  
الاستيقاظ الوحيد: خدمة أمامية `RedConnectionService`.  
أندرويد 12+ يقتل الخدمات؛ المكالمات الواردة والرسائل تختفي إذا أُغلقت العملية.

### 2.9 مستقبلات مُصدَّرة بلا حماية كافية

`PhoneStateReceiver` و`CallBootReceiver`: `exported="true"`.  
`PHONE_STATE` يمكن أن يُرسله تطبيق خبيث على أجهزة قديمة فيُسكت الرنين أو يضع المكالمة في انتظار.  
`PACKAGE_REPLACED` بدون `data android:scheme="package"` يستقبل استبدالات حزم أخرى.

---

## 3) عيوب أمنية ومتوسطة

| # | المشكلة | الدليل |
|---|---|---|
| 3.1 | لا صلاحية `USE_BIOMETRIC` / `USE_FINGERPRINT` في البيان | `AndroidManifest.xml` |
| 3.2 | لا `READ_MEDIA_*` ولا `READ_CONTACTS` | اختيار وسائط/جهات اتصال النظام غير مصرّح |
| 3.3 | `FileProvider` يسمح فقط بـ `cache/camera/` | مشاركة صور/صوت/فيديو من مسارات أخرى تفشل |
| 3.4 | قناة `red_calls_incoming` `VISIBILITY_PUBLIC` + `setBypassDnd(true)` | يظهر نشاط المكالمة على شاشة القفل ويتجاوز «عدم الإزعاج» |
| 3.5 | `FLAG_SECURE` دائماً | جيد للخصوصية؛ يمنع لقطات المنشورات العامة والتصدير |
| 3.6 | `cleartextTrafficPermitted=true` لكل النطاقات في debug | مقبول للتطوير؛ خطر إن وُزّع APK debug |
| 3.7 | بصمة `BIOMETRIC_WEAK` | أضعف من STRONG / مفتاح مرتبط بـ Keystore للمصادقة |
| 3.8 | نسخة احتياطية: الملف `.enc` مربوط بـ Keystore الجهاز | لا يمكن الاستعادة على جهاز آخر؛ الـ metadata JSON غير مشفّر |
| 3.9 | الاستعادة تغلق Room لكن `INSTANCE` يبقى | فتح لاحق قد يستخدم نسخة مغلقة أو قديمة |
| 3.10 | حقل `encryptedPlaintext` يخزّن النص بعد الفك | تسمية مضللة؛ الحماية الوحيدة غلاف SQLCipher |
| 3.11 | رقم السلامة مخصص SHA-256 وليس بروتوكول Signal الرسمي | غير متوافق مع تحقق Signal القياسي |
| 3.12 | تمييز مجموعة/فرد بـ `conversationId.length > 32` | هش؛ UUID مقابل hash قصير قد ينكسر فيفشل فك التشفير |
| 3.13 | `updateUsername` لا يكتب الاسم في `TokenStore` | التعليق فارغ: `if (get("username") != null) { }` |
| 3.14 | جلسة `clearSession` لا تمسح `device_id` / `fcm_token` / `pstn_enabled` | بقايا هوية جهاز بعد تسجيل الخروج |
| 3.15 | إرسال الحمولة عبر `startForegroundService` + extras | حد حجم Intent؛ ملفات كبيرة لا تناسب 256KB أصلاً على المسار هذا |

---

## 4) نواقص منتج وواجهة

### 4.1 ملف واجهة عملاق

`RedDashboard.kt` = **3262 سطراً** — الدردشة، المجموعات، المكالمات، البث، المنشورات، PSTN، البحث… في ملف واحد.

ملفات التقسيم الموعودة فارغة عمداً:

- `ui/screens/ChatsScreen.kt` → `ChatsScreenPlaceholder()`
- ونفس الشيء لـ Home / Groups / Calls / More

لا تُستدعى من أي مكان. الواجهة الحقيقية كلها داخل الـ God File.

### 4.2 نصوص غير قابلة للترجمة

أكثر من **200** `Text("…")` عربي ثابت في `RedDashboard.kt` مقابل **50** سطراً فقط في `strings.xml`.  
لا `values-en`. لا إمكانية تعدد لغات حقيقي.

### 4.3 اعتماديات معلنة وغير مستخدمة

في Gradle: WorkManager، Paging 3.  
في الكود: **صفر** `Pager`/`PagingData`/`WorkManager` (تعليق مستقبلي في `CallTelemetry` فقط). يزيد الحجم دون فائدة.

### 4.4 إكمال ميزات جزئي

موجود في الكود لكنه غير مكتمل كمنتج:

- اكتشاف LAN + مهلات قصيرة (4/6 ثوانٍ) — جيد للشبكة المحلية، فاشل عبر VPN/WAN.
- حالات، مجتمعات، استطلاعات، أحداث، ملصقات: طبقات API موجودة؛ الجودة تعتمد على الخادم ولم تُختَبر هنا كـ UI منفصل عن اللوحة العملاقة.
- تسجيل المكالمات، ترجمة حية، PTT، مشغّل يمني: وحدات موجودة؛ جزء كبير يحتاج أجهزة حقيقية.
- شاشات المكانس (placeholder) توحي بتقسيم لم يحدث.

### 4.5 إصدار المنتج

`versionCode = 1`, `versionName = 1.0.0-alpha01`.  
لا قنوات تحديث داخل التطبيق رغم وجود مجلدات `apkupdate` في شوكة Signal القديمة (غير مربوطة).

---

## 5) جودة هندسية

| بند | الحكم |
|---|---|
| حجم الوحدة القانونية | معقول (147 kt) مقارنة بشوكة Signal الضخمة |
| تجديد التوكن | `Mutex` + فحص مزدوج في `AuthorizedApiClient` — تصميم سليم |
| إعادة اتصال WS | backoff في `RedConnectionService` — موجود |
| اختبارات وحدة | عشرات الاختبارات تحت `red-app/src/test` (مكالمات، تشفير خفيف، FTS) |
| اختبارات UI / أجهزة | لا androidTest داخل `red-app` |
| `exportSchema = false` | لا تاريخ مخطط Room للمراجعة |
| أخطاء تُبتلع | عشرات `catch (_: Exception) {}` في وسائط/نسخ/اكتشاف |
| مفتاح خرائط في وحدة `app/` القديمة | `YOUR_MAPS_API_KEY_HERE` — لا يخص المنتج الحالي |

---

## 6) ما يعمل بشكل جاد (حتى لا يختلط مع العيوب)

- هوية بدون SIM كما في README.
- SQLCipher + Keystore لعبارة المرور.
- جلسات Signal فردية + توزيع مفاتيح مجموعات في الخدمة.
- Refresh token محمي من سباق 401.
- `allowBackup=false`.
- إعداد شبكة release يمنع cleartext.
- خدمة مكالمات + Telecom `ConnectionService`.
- قنوات إشعار مفصولة.

هذه طبقات حقيقية وليست واجهات وهمية فقط — لكنها غير كافية للإطلاق العام بسبب البنود الحرجة أعلاه.

---

## 7) أولويات إصلاح مقترحة (بدون تنفيذ في هذا الفحص)

1. جعل `MainActivity` من `FragmentActivity` وإصلاح `BiometricPrompt` (إمّا DEVICE_CREDENTIAL بلا زر سلبي، أو BIOMETRIC_STRONG مع زر إلغاء).
2. قفل بعد خلفية حقيقية (مثلاً 15–60 ثانية) لا في كل `onResume`.
3. عدم الاحتفاظ بكلمة المرور؛ إعادة طلبها أو استخدام توكن جهاز معلّق.
4. توقيع release بمفتاح غير موجود في Git + pins إلزامية في CI.
5. عدم حذف قاعدة البيانات إلا بقرار مستخدم.
6. Firebase أو آلية إيقاظ أخرى إن أردتم رسائل/مكالمات بعد القتل.
7. `exported=false` أو صلاحيات نظام للمستقبلات.
8. تفكيك `RedDashboard.kt` ونقل النصوص إلى `strings.xml`.
9. صلاحيات الوسائط + مسارات FileProvider كاملة.
10. ربط WorkManager للمزامنة بدلاً من الاعتمادية الميتة.

---

## 8) خلاصة صريحة

التطبيق **ليس** قشرة على Signal؛ هو عميل سيادي محلي فيه تشفير ومكالمات وخدمة أمامية حقيقية.  
هو أيضاً **ألفا**: قفل التطبيق معطوب معمارياً، لا دفع سحابي، عنوان خادم ثابت، واجهة ملف واحد، إصدار غير موقّع، وحذف قاعدة بيانات عند أول عثرة.

لا يصلح للإطلاق العام أو لمتجر قبل إصلاح البنود 2.1–2.9 على الأقل، ثم اختبار جهازين حقيقيين على شبكة LAN مع خادم Docker.
