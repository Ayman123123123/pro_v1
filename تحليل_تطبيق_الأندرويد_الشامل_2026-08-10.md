# 📱 التحليل العميق الشامل لتطبيق الأندرويد «يونس / RED Sovereign»
**التاريخ:** 2026-08-10 · **المسار:** `RED_Ultimate_V1-main/RED_Ultimate/red-app/`
**المنهجية:** قراءة فعلية للملفات (147 ملف Kotlin + 40 XML + Manifest + Gradle) — لا اعتماد على أي تقرير سابق.

---

## 1️⃣ بطاقة الهوية والبناء

| البند | القيمة | المصدر المؤكد |
|---|---|---|
| applicationId / namespace | `com.red.sovereign` | `build.gradle.kts:12-16` |
| compileSdk | 37 (من catalog) | `libs.versions.compileSdk` |
| minSdk / targetSdk | 26 / 35 | `build.gradle.kts` |
| versionName / versionCode | `1.0.0-alpha01` / 1 | نفسه |
| لغة البناء | Kotlin 2.2.20 + Compose + **kapt** (وليس KSP) | plugins |
| Java / JVM target | 21 (مع coreLibraryDesugaring) | compileOptions |
| عنوان الخادم | خاصية Gradle `RED_SERVER_URL` (افتراضي `http://192.168.1.50`) تُحقن في `BuildConfig` | `buildConfigField` |
| ABI | معماريّة واحدة قابلة للضبط `RED_TARGET_ABI` (افتراضي arm64-v8a) مع تحقق `require()` | top of file |
| التوقيع | `redLocalDebug` بمفتاح PKCS12 علني (`signing/red-debug.p12` — **محفوظ في Git**) لضمان تحديثات Alpha بين أجهزة Docker/Windows؛ الإنتاج يتطلب مفتاحًا خاصًا منفصلًا (موثّق في التعليق) | signingConfigs |
| debug vs release | debug: cleartext مسموح؛ release: minify + ProGuard + cleartext ممنوع | buildTypes |

**حجم الكود:** 147 ملف `.kt` (126 رئيسي + 21 اختبار) = **20,736 سطرًا** + 40 ملف XML (ثيمات، 30 Drawable، أيقونات 8K، Lottie typing_dots).

**التبعيات الحرجة المؤكدة:** Compose BOM، Navigation، CameraX، **libsignal-android**، **SQLCipher** (`signal.android.database.sqlcipher`)، Room + kapt، WebRTC، media3 (ExoPlayer)، Coil(+video)، Lottie، Paging 3، WorkManager، emoji2-picker، Google Fonts (Cairo/Tajawal)، ZXing، OkHttp، core-telecom، `:shared-proto`.

---

## 2️⃣ بنية التدفق (Application → Activity → شاشتين رئيسيتين)

```
MainActivity (ComponentActivity + Compose فقط — لا Fragments)
 ├─ DebugSecurityManager.initialize + توصيات أمنية مسجلة
 ├─ FLAG_SECURE على النافذة (منع لقطات الشاشة والمصغّرات) ✅
 ├─ SettingsRuntime (حجم الخط/التباين عبر CompositionLocal Density)
 └─ حسب AuthState:
      • غير موثّق → AuthFlow (ترحيب/تسجيل/دخول/استعادة)
      • موثّق → RedDashboard + تشغيل 4 خدمات:
        RedConnectionService (WebSocket دائم) · YounesCallService.listen
        YounesConnectionService.register (Telecom) · SovereignNotificationRouter
 └─ onUserLeaveHint: دخول PiP تلقائيًا إذا كانت مكالمة فيديو نشطة ✅
```

**الخدمات المصرّح بها في Manifest (مؤكد):** `YounesCallService` (mic+camera+phoneCall)، `ConferenceService`، `LiveStreamService`، `YounesConnectionService` (BIND_TELECOM_CONNECTION_SERVICE)، `RedConnectionService` (remoteMessaging)، `SovereignNotificationRouter`. **المستقبلات:** `CallBootReceiver` (BOOT_COMPLETED + استبدال الحزمة)، `PhoneStateReceiver`. **FileProvider** لمشاركة صور الكاميرا. `allowBackup=false` ✅.

---

## 3️⃣ نظام المصادقة — بلا رقم هاتف، بموافقة إدارة

`AuthViewModel` (193 سطرًا) + `AuthApi` + `TokenStore` + `DeviceKeyManager`:

- **التسجيل:** اسم مستخدم + كلمة مرور + اسم ظاهر → **توليد مفاتيح التشفير محليًا أولًا** (`keys.enrollment()`) ثم `POST /api/auth/register` → حالة `Pending` مع **رموز استعادة**.
- **الدخول:** `/api/auth/login` → حالات الحساب الخمس: `APPROVED / PENDING / REJECTED / SUSPENDED / BANNED` — لكلٍّ شاشة.
- **الاستعادة:** RedId + رمز استعادة + كلمة مرور جديدة (`/api/auth/recover`).
- **التوكنات:** refresh token في SecureStore؛ `restore()` عند الإقلاع؛ عند UNAUTHORIZED في WebSocket يحدّث التوكن تلقائيًا.
- **اكتشاف الخادم المحلي (LAN):** عند فشل الشبكة فقط (وليس خطأ خادم) يجرب `LocalServerDiscovery` ثم يعيد المحاولة — مع رسائل خطأ **معرّبة بالكامل** (12 رسالة مترجمة حرفيًا في `localize()`).
- **PSTN:** `dialPstn()` → `/api/pstn/calls` مع عدّاد يومي (`usedToday/dailyLimit`).

## 4️⃣ التشفير — libsignal **PQXDH** حقيقي بما بعد الكمّ 🛡️

`SignalSessionManager` + `DeviceKeyManager` + `PersistentSignalProtocolStore`:

- **هوية الجهاز محلية 100%:** IdentityKeyPair + SignedPreKey (EC) + **Kyber-1024 KEM PreKey** (توقيع الهوية لكليهما) — أي بروتوكول **PQXDH** المقاوم للحوسبة الكمّية، نفس مستوى Signal الحديث. لا شيء وهمي.
- **التشفير لكل أجهزة الطرف الآخر:** جلب دليل الأجهزة `/api/identity/directory/{redId}` → استهلاك PreKey → بناء جلسة (SessionBuilder) لكل جهاز → SessionCipher → مغلّف لكل جهاز. حد أقصى 256KB للرسالة.
- **الفك:** يدعم PREKEY_TYPE و WHISPER_TYPE.
- **أرقام الأمان:** SHA-256 على مفاتيح الهوية المرتبة → 60 رقمًا بمجموعات خماسية (مثل Signal) + شاشة Safety + ماسح QR للتحقق.
- **مجدد PreKeys:** `replenishPreKeys()` عند إقلاع الخدمة وعند كل اتصال، مع تنبيه عند انخفاض المخزون EC/Kyber.
- **المجموعات:** `GroupCryptoManager` — توزيع مفاتيح عبر رسائل `GROUP_KEY_DISTRIBUTION` مشفرة فرديًا، تدوير المفاتيح `rotate()`.
- **تخزين الجلسات:** `PersistentSignalProtocolStore` مشفر (`ProtocolRecordCipher`).

## 5️⃣ قاعدة البيانات المحلية — Room + **SQLCipher**

`RedDatabase` (v1) بعبارة مرور عشوائية UUID تُولّد وتُخزّن في SecureStore — **لا تغادر الجهاز**:

8 كيانات: `messages` (مشفر) · `local_history` (نصوص مفكوكة محليًا) · `conversations` (تثبيت/أرشفة/كتم) · `contacts` · `groups` · `call_logs` (نوع/اتجاه/مسار RED أو DINSTAR) · `stories` · `drafts` (مسودات). DAO كامل بـ Flow + بحث + حذف انتقائي + `FtsSearchManager` للبحث النصي الكامل.

## 6️⃣ الاتصال الدائم — `RedConnectionService` (349 سطرًا، الأهم)

خدمة أمامية بديلة عن Firebase push:

- **WebSocket** إلى `ServerEndpoint` (`/ws`) بحالة ConnectionState أربعية + إعادة اتصال **exponential backoff** (1→60 ثانية).
- **طوابير إرسال:** `pendingSends` و `pendingGroupSends` تُصرَف عند الاتصال — رسالة لا تضيع.
- **خط الأنابيب الكامل:** تشفير Signal → إرسال → حفظ في local_history → نشر في `DecryptedMessageBus` → إشعار بمعاينة اختيارية (حسب `notificationPreview`) → ACK “DELIVERED/READ”.
- **أنواع أخرى:** ACK تحديث الحالة ✓✓، DELETE (حذف محلي فقط — مقصود)، TYPING عبر `TypingEventBus`.
- **أمان الإدخال:** التحقق من الأنواع المسموحة فقط (TEXT/RICH_TEXT/FILE/VOICE/IMAGE/VIDEO/AUDIO) وحدود الأحجام.

## 7️⃣ الواجهة — `RedDashboard` (2,211 سطرًا، قلب التطبيق)

**5 أقسام بشريط سفلي:** الرئيسية (Feed + Stories) · الدردشات · المجموعات · المكالمات · المزيد — **الدردشات هي الافتراضي** (قرار UX موثّق).

- تبديل تلقائي لتبويب المكالمات عند ورود/بدء مكالمة ✅
- FAB سياقي لكل قسم (دردشة جديدة/مجموعة/مُتصل/إنشاء محتوى)
- **شاشات فرعية كاملة:** الأجهزة، الخصوصية، الاستكشاف، إنشاء مجموعة، النسخ الاحتياطي، معلومات المجموعة، بحث شامل، المجتمعات، جهات الاتصال (+ ماسح QR للـ RED ID)
- **مُتصل RED ID حقيقي:** `YNS-XXXX-XXXX` (نمط مُتحقق) مع طلب أذونات صوت/كاميرا — إصلاح موثّق بدل تحويل DINSTAR
- **فقاعات الرسائل الغنية:** نص، RICH_TEXT، صوت (موجة VoiceWaveform)، صور، فيديو، صوتيات، ملفات — مع تنزيل تلقائي ذكي (WiFi/بيانات/حد الحجم) وحالات الإرسال/الفك/التصدير
- **سجل مكالمات موحّد:** صوت/فيديو/DINSTAR/مجموعة/بث — (مكتمل)

## 8️⃣ المكالمات — 3 أنظمة + جسر Telecom

| النظام | الملفات | الوظيفة |
|---|---|---|
| 1-1 WebRTC | `YounesCallService` (490) + `WebRtcEngine` (369) + `CallSignalingClient` | عرض/رد، كتم/كاميرا/تبديل، **تكيّف جودة تلقائي** (RTT/فقد → BitrateProfile)، إحصاءات شبكة، PTT، ترجمة مباشرة، **تسجيل مشفر** (`CallRecordingManager` + `CallLogCipher`) |
| مؤتمرات | `ConferenceService` + `ConferenceSignalingClient` + `ConferenceOverlay` | غرف SFU جماعية |
| بث مباشر | `LiveStreamService` + `LiveStreamViewerOverlay` | بث/مشاهدة |
| نظام Android | `YounesConnectionService` + `TelecomBridge` + `CallBootReceiver` + `PhoneStateReceiver` | **تكامل ConnectionService** — المكالمة تظهر كمكالمة نظام، إقلاع بعد Boot |

## 9️⃣ هاتف DINSTAR GSM/PSTN (الجسر اليمني) 📞

- **النماذج:** `DinstarPort` (8 منافذ SIM) — حالة تسجيل/مكالمة/إشارة %/GPRS/رقم وIMSI وICCID **مُقنّعة**، مع `isAvailable` (مسجل + خامل + إشارة ≥20%).
- **المشغلون اليمنيون:** سبأفون/MTN/يمن موبايل/هيتل/يو يمن — كشف تلقائي بالبادئات (77x/71x/73x/70x) + ألوان لكل مشغل (مع اختبار وحدات `YemeniOperatorDetectorTest`).
- **الجسر:** `DinstarWebSocketBridge` (أحداث: تغير منفذ/CDR/رسائل SMS واردة) + `DinstarViewModel` (تحديث الحالة، إرسال SMS) + واجهة `DinstarPhoneScreen` مع لوحة أرقام عربية.
- **التنبيهات:** `PhoneStateReceiver` يقنّع الأرقام في الإشعارات (+967••••XXX) — خصوصية حتى في شاشة القفل.

## 🔟 الوسائط والاجتماعي

- **رسائل صوتية احترافية:** `VoiceMessageViewModel` (448) — تسجيل بجودتين (bitrate/sampleRate)، إيقاف مؤقت/قفل/إلغاء بالسحب، **قص الصمت** تلقائيًا، معاينة قبل الإرسال، موجة صوتية، **تشفير + رفع + منح وصول** في خط أنابيب واحد.
- **مرفقات مشفرة:** `EncryptedAttachment` + تحميل عبر `/api/media` ومنح عبر `/api/media/grants` + `MinioUploader` + كاش مشفر `EncryptedMediaCache`.
- **اجتماعي:** Feed منشورات (تصويت/تفاعلات/إخفاء/إبلاغ/كتم/Thread) · Stories صوتية وفيديو بانتهاء صلاحية · **استطلاعات** حقيقية (`/api/admin/content/polls` — تصويت/إغلاق/نشطة) · **فعاليات** (RSVP/check-in/إلغاء) · مجتمعات (انضمام/مغادرة).

## 1️⃣1️⃣ الأمان المؤكد في الكود

✅ Certificate pinning **قائم على البيانات** (لا pins مزيفة؛ بدون pins يسقط إلى TLS النظام) · ✅ SecurityHeadersInterceptor · ✅ تعطيل redirects في العميل الافتراضي · ✅ عميل WebSocket منفصل · ✅ FLAG_SECURE · ✅ عدم النسخ الاحتياطي · ✅ **RemoteAppWipe** — مسح ذاتي كامل (توكنات، قواعد بيانات، Keystore aliases `red.*`) بدون factory reset · ✅ DebugSecurityManager (تحققات + توصيات حسب شدة) · ✅ SANITIZE للمدخلات · ✅ مفاتيح أصل الإنسان في SecureStore/AndroidKeyStore.

## 1️⃣2️⃣ الاختبارات (21 ملفًا — تغطية نوعية)

BitRate/NetworkStats/Telemetry للمكالمات · تسلسل إشارات المكالمة · تشفير سجل المكالمات · كاش مشفر · استطلاعات API · VoiceManifest · SafetyQrPayload · CertificatePinner · FtsSearch · RichMessage · UUIDv7 · كشف المشغل اليمني · خريطة سجل المكالمات.

---

## ⚠️ الخلاصة النقدية (حقائق من الكود، لا من تقارير)

| # | ملاحظة | الخطورة |
|---|---|---|
| 1 | **مفتاح توقيع debug محفوظ في Git** (`signing/red-debug.p12`) — مقصود وموثّق للـ Alpha، لكن يجب عدم استخدامه إطلاقًا للإنتاج | متوسطة (مُدار بتعليق) |
| 2 | عناوين IP محلية ثابتة: `DinstarWebSocketBridge` الافتراضي `http://192.168.1.50:8080` وبيانات `DevicesScreen` التجريبية و `gatewayIp=192.168.11.1` — يُفترض أن تأتي من ServerEndpoint/الإعدادات | منخفضة-متوسطة |
| 3 | `RemoteAppWipe` يحذف قاعدتي `red_messages.db` و`red_signal_protocol.db` بينما اسم قاعدة Room الفعلي **`red_sovereign.db`** → المسح الذاتي **لن يحذف القاعدة المشفرة الحقيقية** | **عالية — خلل وظيفي حقيقي** |
| 4 | فيديو `viemo` لا شيء — الإشعارات تعتمد WebSocket دائم بدل FCM (قرار خصوصية مقصود لكنه يستهلك بطارية) | تصميمي |
| 5 | إجراء «دعوة عبر RED ID» في ContactsScreen فارغ `{}` | تجميلي |
| 6 | 5 استيرادات `com.red.sovereign.proto.RedProtos` — protobuf يتولد أثناء البناء (ليست أخطاء) | لا شيء |

**الحكم:** تطبيق أندرويد حقيقي متكامل البنية — تشفير بعد-كمّي فعلي، قاعدة مشفرة، خدمة اتصال مرنة، مكالمات WebRTC بثلاثة أنظمة، جسر GSM يمني، واجهة عربية كاملة RTL بثيم سيادي (زمردي/ذهبي/كوبالت). الفجوات الموثقة سابقًا تبقى: لا مسار hangup/PSTN وارد حي، Spaces معطلة، وأزرار معطلة بصريًا عن قصد (README: «الميزات غير المكتملة معطلة بصريًا»).
