# تقرير الجلسة — 2026-09-15 — تدقيق ما بعد الدمج + إصلاحات حقيقية

> الفرع: `arena/01a0a242-pro-v1` (مباشرة على نقطة دمج PR #56 = `6c91a4e`).
> هذه الجلسة: **فهم أين وصلنا** (من رابط المحادثة السابقة + الوثائق) ← **تدقيق مستقل بالكود
> نفسه** لكل وعود الجلسة السابقة ← **إصلاح العيوب الحقيقية المتبقية** التي اكتشفها التدقيق.

---

## 1) أين وصلنا في المحادثة السابقة (تم جلب الرابط والتحقق)

الرابط `opncd.ai/share/Vfe4qVZE` فُتح بنجاح. خلاصته:

- جلسة 2026-09-14 (17:13) أنتجت **التدقيق الشامل** (10 وكلاء متوازيين): جذور عطل الرسائل
  الخمسة RC1..RC5، تشريح كل نوع مكالمات (1:1 / مجموعات GRP / أصدقاء FRND / مؤتمرات CONF /
  بث LIVE)، بحث المنافسين، وخطة P0..P5.
- الجلسة **انتهت بأخطاء مزوّد متكررة** (`invalid_request_error`) ومقاطعات — لكن العمل اكتمل
  لاحقاً في جلسة Arena وأُنجز فعلاً: **المراحل 0..11 كلها منفذة ومدمجة في `main` عبر PR #56**
  (2026-09-14 23:24 UTC) — موثقة في `الخطة_الأسطورية_2026-09-14.md` + `ACCEPTANCE_DOD_2026-09-15.md`.
- `RoomSeparationPolicy.kt` المذكور في الرابط كـ"غير ملتزم" **غير موجود في الشجرة** — فكرته
  (فصل GRP_/FRND_/LIVE_/CONF_) عاشت في التنفيذ النهائي عبر معالجات مسارات منفصلة
  (CallWebSocketHandler / GroupCallService / ConferenceController / LiveStreamWebSocketHandler).

---

## 2) التحقق المستقل — كل وعود الجلسة السابقة في الكود المدموج (✅ = تحقق بالسطر)

| الوعد | الدليل المباشر |
|---|---|
| إصلاح عدم ظهور الرسائل (تجمّد القوائم) | `RedDashboard.kt:1662,2225` — `derivedStateOf` للخاص والمجموعات |
| توحيد `conversationId` (RC1) | `core/YounesId.kt` مصدر حقيقة واحد (5 أرقام = نمط الخادم) |
| إعادة إرسال `SENDING` العالقة (RC4) | `core/outbox/OutboxRetryWorker.kt` + مجدولة في `YounesApplication` كل 15د |
| عنصر نائب للرسائل غير القابلة للفك | `RedConnectionService.saveDecryptPlaceholder` + `publishPlaceholderResolution` |
| إظهار فشل الإرسال بالعربي | `reportSendFailure` + `MessageSendErrorBus` + `sendFailureArabic` |
| رنين من تطبيق مقتول — بلا Firebase | `RedPushService` (UnifiedPush connector) + `UnifiedPushSender` (خادم) + `POST /api/devices/push-token` + حاوية `ntfy` في docker-compose + سلسلة `RINGING_PUSH_SENT` |
| صفر Firebase/FCM | `grep firebase` في red-app + backend = **صفر** |
| `app/` محذوف + `red-app` الوحيد | لا `app/`؛ `:app → red-app` في settings؛ CI يبني `:app` |
| إلغاء PSTN/Dinstar | هجرة `V52__Drop_Cancelled_Pstn_Dinstar_Scope.sql` (35 جدولاً)؛ بقايا dinstar في الكود الحي = تعليقان توثيقيان فقط |
| رحيق CALL_SIGNAL الميت | **كان ما زالت حية — أُصلحت اليوم (§3-ع1)** |
| RINGING/BUSY/HOLD/RESUME عبر `/ws/calls` | `CallWebSocketHandler` — كل الأنواع + صندوق بريد + `RINGING_PUSH_SENT` |
| بث SFU-first + احتياطي mesh | `LiveStreamWebSocketHandler` (`VIEWER_NEEDS_MESH`) + حذف الهدايا (صفر ملفات Gift) |
| صلاحيات SFU | `media-sfu/server.js:467` — `sfuCanProduce` يرفض النشر بلا صلاحية + `preferredLayers/requestKeyFrame/restartIce` |
| أدوار المؤتمرات/المساحات | `ConferenceWebSocketHandler` — HOST/CO_HOST/SPEAKER/LISTENER + منصة + كتم الكل + قفل 423 |
| CI واحد بلا تكرار | `.github/workflows/red-ultimate-ci.yml` فقط + `concurrency cancel-in-progress` + فلتر paths (القديمة مؤرشفة خارج مجلد workflows فلا تُشغَّل) |
| العقود والمزامنة (P9) | `PHASE9_DATA_CONTRACTS_2026-09-15.md` + فهرس فريد `(conversationId, sequenceNumber)` + حذف `RedisSequenceGenerator` |

**خلاصة التدقيق:** كل المراحل موصولة فعلاً. لا رسائل كاذبة.

---

## 3) عيوب حقيقية اكتشفها تدقيق اليوم وأُصلحت في هذه الجلسة

### ع1 — مسار `CALL_SIGNAL` الميت (خادم + Proto) ✅ أُصلح
كانت `RedMasterHandler` تستقبل `CALL_SIGNAL` وتطبع Log فقط — لا عميل يرسلها، والمسار الحي
للمكالمات هو `/ws/calls`. الخطة كانت قد أوصت بحذفها ولم تُنفَّذ.
- حُذفت الحالة من `when` + دالة `receiveCallSignal` (RedMasterHandler.kt).
- حُذف حقل `call_signal = 9` ورسالة `CallSignalRED` من `red_protocol.proto` (src + bin نسختان متطابقتان).
- **آمن سلكياً:** عميل قديم يرسل الحقل → يُعامل كحقل مجهول ويُتجاهل (`else -> Unit`). الرقم 9 لن يُعاد استخدامه.

### ع2 — مزامنة سجل المكالمات كانت ميتة بصمت ✅ أُصلحت
`CallLogSyncScheduler.schedulePeriodicSync` كان **لا يستدعيها أحد** (كانت تُستدعى فقط من
`CallSystemIntegration.completeInitialization` غير المستدعاة أصلاً) — فمزامنة سجل المكالمات
المشفرة E2EE مع الخادم لم تكن تعمل دورياً أبداً.
- أُضيفت الجدولة في `AppStartupCoordinator.onAuthenticated` (كل دخول) و`CallBootReceiver` (بعد كل reboot/تحديث — WorkManager لا ينجو من الإقلاع).

### ع3 — "تحويل المكالمة" وهمي 100% ✅ أُرشف
`CallTransferManager` يقرر النجاح بـ `Math.random() > 0.1` — بلا أي إشارة نقل حقيقية.
ميزة تكذب على المستخدم. أُرشف مع شاشته إلى `الأرشيف/dead-code-2026-09-15/` + تنظيف
`CallManagerIntegration` (الدالتان `initiateTransfer/cancelTransfer` + سطر `clearState`).
التحويل الحقيقي (عند الرغبة لاحقاً): إشارة `TRANSFER` عبر `/ws/calls` + endpoint خادم.

### ع4 — "الخلفية الافتراضية" وهمية + شاشة مكالمة ميتة ✅ أُرشفت
`VirtualBackgroundManager` (ضباب Compose على المعاينة فقط — لا يلامس إطارات WebRTC المرسلة،
وحتى `setEffect` لم يكن موصولاً بأي زر) + `UnifiedModernCallScreen` (**صفر مستدعين** —
الواجهة الحية `CallScreens → UnifiedCallOverlays → YounesCallOverlay`). أُرشفت الثلاثة
مع اختبارهما من `ManagersTest`. الخلفية الحقيقية تتطلب معالجة إطارات على مسار الإرسال.

### ع5 — `CallSystemIntegration` منسّق وهمي ✅ أُرشف
"موحّد التهيئة" غير المستدعى من أي مكان؛ كل ما فيه نافع يعيش لدى مالكه الحقيقي
(القنوات في `YounesApplication`، `WebRtcBootstrap.ensure` عند المحركات، الدفع في
`AppStartupCoordinator`/`CallBootReceiver`). أُرشف + حُدّث التعليق في `CallNotificationManager`.

> كلها موثقة في `الأرشيف/dead-code-2026-09-15/README.md` مع كيفية إحياء أي منها بشكل حقيقي.

### ع6 — توثيق ✅
`README.md` الجذري كان يقول إن `quality-gate.yml` هو الـ CI القانوني — حُدّث إلى `red-ultimate-ci.yml`.

---

## 4) مسار الرسائل اليوم — لماذا كان لا يظهر ولا للطرفين، وماذا الآن

```
إرسال: UI (عرض متفائل ⏳) → RedConnectionService.drainSends → SignalSessionManager.encrypt
  → fan-out لكل جهاز معتمد (معرف فريد لكل نسخة) → /ws/master → الخادم يخزن (تسلسل ذري)
  → ACK SENT (⏳→✓) → تسليم حي أو pendingFor + دفع UnifiedPush عند غياب سوكت المستلم
استقبال: /ws/master → فك (زوجي/مجموعات SenderKey) → Room مشفر SQLCipher → DecryptedMessageBus
  → RedDashboard (derivedStateOf — إعادة حساب عند كل إضافة) → فشل الفك = عنصر نائب بلا ACK
  → يعيد السيرفر التسليم تلقائياً → فشل الإرسال = ⚠ + Toast عربي → Outbox Retry كل 15د + عند الاتصال
```
الجذور الخمسة القديمة (RC1 انشطار المعرّف، RC2 دالة مفقودة، RC3 فلتر الجهاز، RC4 عالقة SENDING،
RC5 كشف المجموعة) **معالجة كلها في الكود الحالي**. المتبقي الوحيد: تجربة على جهازين حقيقيين.

---

## 5) المكالمات — خريطة المسارات الحية بعد إصلاحات اليوم

| النوع | المسار | الحالة |
|---|---|---|
| 1:1 صوت/فيديو منفصلان | `/ws/calls` (JSON) + P2P WebRTC + Opus/FEC/DTX + H264/VP9 تكيّف + `CallRingPolicy` 45s + Telecom + `RedBundledTones` | كامل — ينتظر اختبار جهاز/شبكة مخنقة |
| مجموعات المحادثات `GRP_` | سوكت المكالمات + SFU أولاً/Mesh احتياط + `GROUP_CALL_INVITE/KICK/MUTE` + بطاقة "انضم" في الشات | كامل — ينتظر أجهزة متعددة |
| أصدقاء (زووم/ايمو) `FRND_` | سوكت منفصل + أدوار + مشاركة شاشة SFU + قفل E2E (423) + تسجيل بموافقة | كامل (اللوبي/Breakout مؤجلة بقرار) |
| مؤتمرات/مساحات `CONF_` | `ConferenceWebSocketHandler` (HOST/CO_HOST/SPEAKER/LISTENER + منصة + كتم الكل) + تذاكر واعية بالدور في SFU | كامل — ينتظر حمل |
| بث مباشر `LIVE_` | SFU-first (نشر مرة واحدة) + `VIEWER_NEEDS_MESH` + Simulcast + تفاعلات بلا هدايا | كامل — LL-HLS Egress مؤجل (خدمة منفصلة) |
| الرنين من مقتول | سوكت حي → عند الفشل UnifiedPush → ntfy ذاتي → `RedPushService` → رنين فوري CallStyle/FSI | كامل — ينتظر اختبار قتل فعلي |

**بحث اليوم (تحديث المنافسين):** UnifiedPush+ntfy ذاتي الاستضافة مؤكد كالبديل المعياري لـFCM —
WebSocket دائم ~0–1% بطارية/يوم وتسليم فوري (مراجعات 2025–2026)، وهو نفسه ما بُني في المرحلة 2.
مراجع المنافسين الأخرى (واتساب/تيليجرام/زنجي/تيك توك/يوتيوب/X Spaces) موثقة بروابطها في
`الخطة_الأسطورية_2026-09-14.md` §3.

---

## 6) ما تبقى فعلياً (بلا ترتيب تنفيذ كود إضافي)

1. **اختبارات الجهاز** (بوابة القبول §7 في الخطة): جهازان للرسائل، قتل/رنين على Pixel/Samsung/Xiaomi، Throttling 3G، حمل (8 فيديو / 1000 مشاهد / 500 مستمع).
2. متابعات مؤجلة بقرار المالك: LL-HLS Egress للبث الضخم، موزّع RED مضمّن، اللوبي/Breakout، تحويل المكالمات الحقيقي، خلفية افتراضية حقيقية (معالجة إطارات).
3. البناء المحلي عند المالك (البيئة هنا بلا Android SDK/JDK — كل تحقق هذه الجلسة سكوني `grep`/قراءة).

## 7) ملفات هذه الجلسة

- معدّلة: `RedMasterHandler.kt`، `red_protocol.proto` (×2)، `AppStartupCoordinator.kt`، `CallBootReceiver.kt`، `CallManagerIntegration.kt`، `CallNotificationManager.kt`، `ManagersTest.kt`، `README.md` (جذري).
- مؤرشفة (`git mv`، السجل محفوظ): 5 ملفات إلى `الأرشيف/dead-code-2026-09-15/` + README توثيقي جديد.
