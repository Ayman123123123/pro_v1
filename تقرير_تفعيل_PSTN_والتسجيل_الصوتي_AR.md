# تقرير — تفعيل PSTN وإصلاح التسجيل الصوتي

> التاريخ: 2026-08-14 — فحص حرفي للوحة الإدارة وتطبيق الأندرويد والخادم.

---

## 1) تفعيل ميزة PSTN (الهاتف اليمني عبر DINSTAR)

### التشخيص
- **الخادم** يملك نقطة النهاية سليمًا: `PUT /api/admin/users/pstn` (`PstnAuthorizationController`) — تستقبل `userId` و`enabled` و`dailyLimit` وتخزّن `pstnEnabled` و`pstnDailyLimit` في حساب المستخدم، مع تسجيل تدقيق.
- **لوحة الإدارة** كانت تعرّف دالة `updatePstnAccess()` في `api.ts` **لكن لا أحد يستدعيها**، ولا يوجد زر في واجهة إدارة المستخدمين — فقط عمود `PSTN` يعرض «مفعل/معطل» بلا أي وسيلة تغيير.
- **التطبيق** يعرض شاشة «الهاتف اليمني» لكنها تبقى «غير مفعل — يفعله المسؤول من اللوحة» لعدم وجود وسيلة للتفعيل.

### الإصلاح (تم)
في `UserManagement.tsx`:
1. استيراد `updatePstnAccess` + أيقونة `PhoneOutlined` + `InputNumber`.
2. **عمود PSTN** أصبح قابلًا للنقر (Tag «مفعل/معطل») يعرض الحد اليومي.
3. **زر تفعيل/تعطيل PSTN** في عمود الإجراءات (بجانب الملف التشغيلي) — يعمل للمستخدمين المعتمدين غير المسؤولين.
4. **نافذة منبثقة** لإدخال الحد اليومي (0–1000) ثم تفعيل/تعطيل، مع رسائل نجاح/خطأ عربية، وتحديث الملف التشغيلي تلقائيًا.

في `api.ts`:
- أُضيف `pstnDailyLimit?: number` لواجهة `UserRecord` (كان مفقودًا رغم أن الخادم يعيده).

> ملاحظة تشغيلية: التفعيل من اللوحة **لا يكفي** وحده — يجب أن يكون `DINSTAR_ENABLED=true` في ملف `.env` وأن تكون بوابة DINSTAR (UC2000-VE-8G) متصلة وسليمة، وإلا ستظهر الميزة مفعلة للمستخدم لكن المكالمة تفشل.

---

## 2) إصلاح التسجيل الصوتي («لا يعمل ويقول خطأ»)

### التشخيص
- إذن `RECORD_AUDIO` **مصرّح في المانيفست** ومسار طلب الإذن سليم.
- المسار: ضغط زر الميكروفون → فحص الإذن → `MediaRecorder` (AAC/M4A) → رفع مشفّر إلى MinIO.
- **الخلل الجوهري**: عند الفشل كانت الواجهة تعرض رسالة خام بالإنجليزية (مثل `MICROPHONE_PERMISSION_REQUIRED` أو رسالة استثناء `MediaRecorder`) عبر `ErrorPanel` — فتبدو «خطأ» بلا تفسير ولا طريقة حل.

### الإصلاح (تم)
1. **ترجمة أخطاء التسجيل** (`VoiceRecorderPanel.kt`): أُضيفت دالة `localizeVoiceError()` تحوّل كل أكواد الفشل إلى رسائل عربية واضحة مع توجيه عملي:
   - إذن الميكروفون مفقود → «فعّل إذن الميكروفون من الإعدادات».
   - فشل بدء التسجيل (الميكروفون مشغول) → «أغلق أي مكالمة/تسجيل آخر».
   - تسجيل قصير جدًا → «سجّل ثانية واحدة على الأقل».
   - فشل التشفير/الرفع/الاتصال → رسائل مخصصة.
2. **رمز خطأ ثابت** (`VoiceMessageViewModel.kt`): أصبح فشل `MediaRecorder` يعيد `VOICE_RECORDER_START_FAILED: <السبب>` بدل رسالة الاستثناء الخام، لتلتقطها الترجمة بدقة.

---

## 3) ملخص كل التعديلات في الجلسة (تراكمي)

| الملف | التغيير |
|---|---|
| `admin_dashboard/src/pages/UserManagement.tsx` | زر تفعيل/تعطيل PSTN + نافذة الحد اليومي + عمود PSTN قابل للنقر |
| `admin_dashboard/src/api.ts` | إضافة `pstnDailyLimit` لـ `UserRecord` |
| `red-app/.../voice/VoiceRecorderPanel.kt` | ترجمة أخطاء التسجيل الصوتي إلى العربية |
| `red-app/.../media/VoiceMessageViewModel.kt` | رمز خطأ ثابت لبدء التسجيل |
| `red-app/.../RedDashboard.kt` | إصلاح مرجع `conversation` + مطابقة فقاعات واتساب + ربط أعضاء المجموعة بمحادثة خاصة + تمرير وصف المجموعة |
| `red-app/.../SovereignGroupSystem.kt` | تمرير وصف المجموعة + أعضاء قابلون للنقر للمراسلة |
| `Auth*` (خادم + تطبيق) | إصلاح `INVALID_REQUEST` + ترجمة الأخطاء + إدخال عنوان الخادم يدويًا |

> ⚠️ لا توجد Java/Node/Android SDK في هذه البيئة، فلم أستطع تشغيل البناء — لكن تحققت يدويًا من توازن الأقواس والأقواس المعقوفة في كل ملف معدّل.
