# تقرير استرجاع المفقودات من كل الفروع — 2026-08-19

فحص شامل لـ **27 فرعاً** على GitHub، ملفاً ملفاً، لاستخراج كل ما حُذف أو
استُبعد بالخطأ عبر كل مراحل التطوير.

---

## المنهجية

1. جرد كل فرع: `git ls-tree -r` لكل الـ27 فرعاً
2. مقارنة بالفرع الحالي: `comm -13` لاستخراج المفقود
3. **لكل ملف مفقود**: فحص هل هو (أ) مفقود فعلاً (ب) معاد التسمية (ج) مكرر (د) مستبعد عمداً
4. التحقق من التوابع قبل الاستعادة: المفاتيح الخارجية، تعارض الـ beans، تعارض المسارات
5. الاستعادة مع التصحيح عند اللزوم

**النتيجة الخام:** 29 ملفاً فريداً مفقوداً → بعد التحليل: **9 استُعيدت، 20 مبررة**.

---

## 🔴 أخطر ما وُجد: 6 مسارات WebSocket معطّلة

### المشكلة
`websocket/WebSocketConfig.kt` كان يسجّل **مساراً واحداً فقط**:

```kotlin
registry.addHandler(dinstarWebSocketHandler, "/ws/dinstar")
    .setAllowedOrigins("*")   // بلا JWT، وCORS مفتوح للجميع
```

بينما النسخة الكاملة (7 مسارات + مصادقة JWT + قيود CORS) كانت في
`config/WebSocketConfig.kt` وفُقدت أثناء توحيد الحزم.

### الأثر المؤكد
كل المعالجات موجودة في الكود، والعملاء يستدعون المسارات فعلياً:

| المسار | المعالج | يستدعيه |
|---|---|---|
| `/ws/master` | RedMasterHandler ✅ | 4 ملفات |
| `/ws/calls` | CallWebSocketHandler ✅ | 3 ملفات |
| `/ws/conference` | ConferenceWebSocketHandler ✅ | 2 ملف |
| `/ws/livestream` | LiveStreamWebSocketHandler ✅ | 2 ملف |
| `/ws/typing` | TypingHandler ✅ | 1 ملف |
| `/ws/admin/logs` | AdminLogHandler ✅ | 8 ملفات |
| `/ws/dinstar` | DinstarWebSocketHandler ✅ | مسجّل |

**ستة من سبعة مسارات كانت تُرجع 404** — الرسائل الحية، المكالمات،
المؤتمرات، البث، مؤشر الكتابة، وسجلات الإدارة كلها معطّلة.

### الإصلاح
أُعيد دمج النسخة الكاملة في `websocket/WebSocketConfig.kt` مع:
- تسجيل المسارات السبعة كاملة
- `JwtHandshakeInterceptor` على **كل** مسار (شمل `/ws/dinstar` الذي كان بلا مصادقة)
- استبدال `setAllowedOrigins("*")` بـ `setAllowedOriginPatterns` المضبوط عبر
  `red.security.allowed-origins`
- إضافة `companion object { val ROUTES }` ليتحقق منها الاختبار العقدي

---

## 🔐 ترقية أمنية مفقودة: كلمات مرور الغرف

### قبل
`ConferenceController.hashPassword()` كان يستخدم **SHA-256 بلا ملح** —
عرضة لجداول قوس قزح والهجوم بالقوة الغاشمة.

### بعد
استُعيد `RoomPasswordHasher.kt` (من `arena/019ffece-pro-v1`):
- **PBKDF2-HMAC-SHA256**
- **210,000 تكرار**
- ملح عشوائي 16 بايت لكل كلمة
- مقارنة بزمن ثابت عبر `MessageDigest.isEqual`

### التوافق الرجعي
الغرف المنشأة بالنظام القديم تبقى تعمل:
```kotlin
return RoomPasswordHasher.verify(password, record.passwordHash) ||
    legacySha256(password) == record.passwordHash
```

---

## 🗄️ أربعة جداول قواعد بيانات ضاعت نهائياً

`V23__Sovereign_Ultimate_Capabilities_And_Performance_Indexes.sql` تعارض رقمه
مع `V23__Multi_Gateway_Fleet.sql`، فحُذف **بالكامل** بدل إعادة ترقيمه.

الجداول الضائعة (تحققتُ أن **لا أثر لها** في أي ترحيل حالي):

| الجدول | الوظيفة |
|---|---|
| `call_qoe_telemetry` | جودة المكالمات: MOS، فقد الحزم، الارتعاش، RTT |
| `group_invite_links` | روابط دعوات مشفّرة بحد استخدام وانتهاء صلاحية |
| `channel_subscriber_preferences` | تفضيلات إشعارات مشتركي القنوات |
| `device_session_fingerprints` | بصمات الجلسات وكشف الاختراق |

### أُعيدت كـ `V30__Sovereign_Ultimate_Capabilities_And_Performance_Indexes.sql`

**تصحيحات ضرورية** — النسخة الأصلية كانت **ستُفشل الترحيلات**:

| في الأصل | الحقيقة في مخططنا | الإجراء |
|---|---|---|
| `REFERENCES communities(id)` | لا يوجد `communities` | → `channels(id)` |
| `REFERENCES user_devices(id)` | لا يوجد `user_devices` | حُذف المفتاح الخارجي |
| `ON call_cdr_logs(...)` | الجدول هو `call_history` | → `call_history(caller_id, ...)` |
| `ON user_reports(...)` | قد لا يوجد | غُلّف بفحص `information_schema` |

كل الفهارس المشكوك فيها داخل كتلة `DO $$ ... END $$` مع فحص وجود
الجدول والعمود — لا تفشل الترحيلات مهما كان المخطط.

---

## ✅ ما استُعيد (9 ملفات)

| الملف | المصدر | الحجم |
|---|---|---|
| `websocket/WebSocketConfig.kt` | `019fe9e3` (مدموج) | 7 مسارات + JWT |
| `calls/RoomPasswordHasher.kt` | `019ffece` | 47 سطر |
| `V30__Sovereign_Ultimate...sql` | `019ff019` (مصحّح) | 4 جداول + 11 فهرس |
| `test/RedSecurityServiceTest.kt` | `019ffece` | 49 سطر |
| `test/websocket/WebSocketRouteContractTest.kt` | `019ffece` | 22 سطر |
| `RED_ULTIMATE_MASTER_UPGRADE_REPORT.md` | `019fe9e3` | 53 سطر |
| `red-app/.../mipmap-mdpi/ic_launcher.svg` | `019fe9e3` | أيقونة |
| `RED_Ultimate/run_red.bat` | `019fe9e3` | 26 سطر |
| تقريرا الجلسة 2026-08-15 (إصلاح + دمج PR39) | `01a0064f` | عربي |

---

## ⛔ ما رُفض عمداً — ولماذا

### `LiveStreamPublicController.kt` — كان سيُسقط Spring Boot
استعدتُه أولاً، ثم اكتشفت بالفحص أنه **نسخة طبق الأصل** من
`LiveStreamAdminController.kt` الموجود (الفرق: 3 أسطر تعليق فقط).

**ستة مسارات متطابقة حرفياً** بينهما:
```
/streams · /streams/{id}/viewers · /streams/{id}/viewers/join
/streams/{id}/viewers/leave · /admin/streams/{id}/start · /admin/streams/{id}/stop
```
`@RequestMapping("/api/live")` نفسه في كليهما → `AmbiguousMappingException`
عند الإقلاع. **فأزلته**، لكن نقلتُ التعليق التوثيقي القيّم (الذي يشرح سبب
إعادة التسمية أصلاً) إلى الملف الباقي.

### `attrs_red.xml` — كان سيكسر بناء الأندرويد
يعيد تعريف `colorOnSurface`، والسمة `Theme.Younes` ترث
`Theme.Material3.Dark.NoActionBar` التي **توفّره أصلاً**. إعادة التعريف
تسبب تعارض موارد. الـ5 ملفات التي تستخدم `?attr/colorOnSurface` تعمل
بالفعل عبر Material3.

### `AuthFlow.kt` (821 سطر) — أُرشف عمداً
موجود كـ `_archive/AuthFlow.legacy.kt.archived`، واستُبدل بـ
`ui/AuthScreens.kt` + `auth/AuthViewModel.kt`. إعادته تعني ازدواج شاشات المصادقة.

### ملفات SQL — إعادة تسمية لا فقدان
تحققت بمطابقة **بصمة SHA1 للمحتوى**:

| المفقود ظاهرياً | الموجود فعلاً | المطابقة |
|---|---|---|
| `V25__Younes_Id_Five_Digits.sql` | `V24__Younes_Id_Five_Digits.sql` | ✅ متطابق |
| `V25__User_Avatar_And_Bio.sql` | `V25_1__User_Avatar_And_Bio.sql` | ✅ متطابق |
| `V30__Dinstar_Enhancements.sql` | `V25__Dinstar_Enhancements.sql` | ✅ متطابق |
| `V31__Complete_Missing_Features...` | `V26__Complete_Missing_Features...` | ✅ متطابق |
| `V31__Dinstar_Enhanced.sql` | `V26_1__Dinstar_Enhanced.sql` | نسخة أحدث موجودة |

### أخرى
- `config/WebSocketConfig.kt` — دُمج محتواه في `websocket/` (تجنّب تعارض bean)
- `pstn/DinstarWebSocketHandler.kt` — موجود في `websocket/`
- `controllers/LiveStreamController.kt` — موجود في `calls/`
- `DinstarTab.tsx` — استُبدل بـ `DinstarControl.tsx`، وغير مستورد في أي مكان
- `.env` — **أسرار**، والمستودع عام
- `.idea/*.xml` — إعدادات IDE محلية
- `backend-server/gradle/verification-metadata.xml` — يُولَّد آلياً
- الفرع `arena/019fe8fa` (31,096 ملف) — الـ20,363 "المفقودة" كلها نسخ
  مكررة تحت `pro/` و `project/` من نفس المشروع

---

## التحقق النهائي

```
الملفات قبل : 11,085
الملفات بعد : 11,093   (+8 صافي، وصفر محذوف)

توازن أقواس Kotlin:
  WebSocketConfig.kt        { 4 = 4 }   ✅
  RoomPasswordHasher.kt     { 9 = 9 }   ✅
  ConferenceController.kt   { 49 = 49 } ✅

V30 SQL:
  كل REFERENCES تشير لجداول موجودة    ✅
  DO $$ / END $$ متوازن                ✅
  لا تعارض في رقم V30                  ✅

WebSocketRouteContractTest:
  ROUTES تطابق المسجَّل حرفياً          ✅
```

---

## ⚠️ ما زال غير محمي

تعديلاتك في `C:\Users\hpc01\Pictures\pro_new` — **111 ملفاً معدّلاً و~70 جديداً**
— لم تصل إلى GitHub بعد. هذا التقرير يغطي **الفروع المرفوعة فقط**.

شغّل `scripts/BACKUP_EVERYTHING.ps1` لرفعها، وسأدمجها بنفس هذا المستوى من التدقيق.
