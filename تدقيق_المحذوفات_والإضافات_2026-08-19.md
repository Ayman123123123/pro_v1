# تدقيق كل ما حُذف وأُضيف على الفرع — ملفاً ملفاً

> النطاق: كل تغيير بين نقطة انطلاق الفرع `69277ec6` و`HEAD`
> **362 ملفاً**: 210 حذف · 81 إضافة · 48 نقل · 23 تعديل
> المنهج: لا حكم بالاسم ولا بالحجم. لكل ملف محذوف سألتُ: **هل وظيفته
> موجودة الآن بصيغة أحدث؟** والإجابة من فحص الكود لا من الافتراض.

---

## 1) الخلاصة التنفيذية

| الفئة | العدد | الحكم |
|---|---|---|
| `android/` | 86 | ✅ الوظائف كلها مغطاة في `red-app` بصيغة أنضج |
| `app-android/` | 27 | ✅ نسخة أقدم من نفس الأفكار (حزمة `com.developedchat`) |
| `_conversation_archive/` | 86 | ✅ نسخ لقطات من فروع سابقة — 44 لها نظير بالاسم، و42 نسختها ملفات أحدث |
| جذر المستودع | 11 | ✅ **ليست حذفاً بل نقلاً** إلى `الأرشيف/` (git لم يرصد النقل لتعديل طفيف) |
| **فجوة حقيقية وُجدت** | **1** | ⚠️ **استُعيدت** — انظر §3 |

**لم يضع شيء.** وُجدت فجوة واحدة حقيقية وأُصلحت في هذه الجولة.

---

## 2) كيف تحققتُ — لا بالأسماء

المطابقة بالاسم أعطت نتيجة مقلقة: **78 من 105 ملف Kotlin محذوف «بلا نظير»**.
لكن هذا المعيار خاطئ، لأن `red-app` أعاد تنظيم البنية:

- `RedChatListScreen.kt` (android) ← وظيفته داخل `ui/RedDashboard.kt`
- `SovereignButtons.kt` ← أُدمجت في `ui/theme/SovereignThemeSystem.kt`
- `DinstarTab.kt` (862 سطر) ← `features/dinstar/` الثلاثي + `MoreScreen`

فانتقلتُ إلى **الفحص الوظيفي**: هل المفهوم نفسه موجود؟

| الوظيفة | التغطية في `red-app` |
|---|---|
| محادثة فردية | `RedDashboard.kt:829` `ChatHubScreen` + `RichTextMessage` + `VoiceMessage` + `ImageMessage` |
| سجل المكالمات | 3 ملفات (`CallHistoryModels`, `CallHistoryViewModel`, …) |
| مؤتمر | **13 ملفاً** مقابل 2 محذوفَين |
| بث مباشر | **9 ملفات** مقابل 3 |
| القصص | `stories/` (4 ملفات) + `StoryCleanupWorker` |
| إشعارات | 8 ملفات |
| WebSocket دنستار | `DinstarWebSocketBridge.kt` |
| أزرار/أيقونات | `SovereignThemeSystem.kt` + 35 ملفاً يستخدم الأيقونات |

`red-app` يملك **34 ملف مكالمات** مقابل 12 محذوفاً — الاتجاه توسّع لا انكماش.

### لماذا حُذف `app-android/` بلا تردد

حزمته `com.developedchat` — اسم منتج سابق. ملفاته أصغر بمراتب من نظائرها
(`ChatDetailScreen` 129 سطراً مقابل `ChatHubScreen` وتوابعه)، ولا تحوي تشفيراً
على الجهاز. هي **الجيل الأول** من نفس الفكرة.

---

## 3) الفجوة الوحيدة التي وُجدت — واستُعيدت

### `DinstarDeviceStatus` — قياسات عتاد البوابة

الفحص الوظيفي أعطى `❌ غائب` لمفهوم «حالة الجهاز». تتبّعتُ السلسلة:

| الحلقة | الحالة |
|---|---|
| الجهاز | `POST /api/get_status` — `DinstarConnectionFactory.kt:172` ✅ |
| الخادم — جلب | `DinstarApiService.getDeviceStatus()` ✅ |
| الخادم — تخزين | جدول `dinstar_device_status` (9 أعمدة) ✅ |
| الخادم — بث | `webSocketHandler.broadcastDeviceStatus()` ✅ |
| الخادم — نشر | `GET /api/admin/dinstar/device-status` ✅ |
| **التطبيق** | **❌ لا مستهلك** |

**السلسلة كانت مقطوعة عند الحلقة الأخيرة فقط.** الجهاز ينتج القياسات
والخادم يخزّنها ويبثّها — ولا شيء يقرأها. هذه ليست ميزة ملغاة بقرار، بل
طرف مفكوك خلّفه دمج `android/`.

**ما استعدتُه** (بمعايير `red-app` لا بنسخ الكود القديم):

- `DinstarModels.kt` — `data class DinstarDeviceStatus` بـ9 حقول + `isEmpty`
- `DinstarViewModel.kt` — `_deviceStatus` StateFlow + `refreshDeviceStatus()`

**قرارات في الاستعادة:**

1. **الحقول تبقى `String?`** — البوابة تُرجع `"45%"`, `"128MB"`, `"47C"`
   بوحدات مُلحقة تتفاوت بين الإصدارات. تحويلها إلى أرقام يفقد الوحدة
   ويكسر عند صيغة غير متوقَّعة.
2. **فحص `error` صراحةً** — الخادم يُرجع `{"error": "No gateway available"}`
   برمز 200 لا باستثناء، فالتحليل الساذج كان سيُنتج حالة فارغة صامتة.
3. **قبول `snake_case` و`camelCase`** — التخزين يستخدم `cpu_used`، والقبول
   المزدوج يحمي من تغيّر الصيغة.
4. **دالة منفصلة عن `refreshStatus()`** — تلك تجلب المنافذ دوريًا من
   `/fleet/ports`؛ استعلام العتاد يمرّ عبر الجهاز فلا يُستدعى بكثافة.

> **الحرارة** هي أنفس هذه القياسات: الاختناق الحراري يسبق سقوط المنافذ،
> فتوفّرها في التطبيق إنذار مبكر لا رفاهية عرض.

---

## 4) ما بدا فجوة ولم يكن

| البلاغ | الحقيقة |
|---|---|
| «تفاصيل الدردشة غائبة» | موجودة في `RedDashboard.kt:829`. `ui/screens/ChatsScreen.kt` هيكل فارغ (12 سطراً) لتفكيك مخطَّط موثَّق في `README_SPLIT.md` — لا ميزة ناقصة |
| «الأزرار المخصّصة غائبة» | `SovereignButtons.kt` أُدمج في `SovereignThemeSystem.kt`؛ 12 موضع تدرّج لوني |
| «`MediaBubble.kt` مفقود» | نظيره الحالي **أأمن**: القديم يأخذ `url` خاماً، والحالي يستخدم `AttachmentManifest` مع فك تشفير على الجهاز |
| «11 ملفاً حُذف من الجذر» | نُقلت إلى `الأرشيف/تقارير-تاريخية/` — git صنّفها حذفاً+إضافة لتعديل طفيف |
| «43 ملف `.tsx` ضاع من الأرشيف» | لوحة الإدارة الحالية **25 صفحة** تغطي كل وظيفة: `LiveMonitor`→`MasterOverview`، `UserApproval`→`Approvals`، `DinstarTab`→`DinstarControl`+`PortControl`+`SimInventory` |

---

## 5) الحذف داخل الملفات المعدَّلة

الـ23 ملفاً المعدَّلاً كلها إضافية. أكبر حذف كان **32 سطراً** من
`YemeniOperatorDetector.kt` — وهو **الخريطة الخاطئة**:

```
- clean.startsWith("73") -> OperatorInfo("سبأفون", …)   ← خطأ
- clean.startsWith("71") -> OperatorInfo("Y (واي)", …)   ← خطأ
- clean.startsWith("70") -> OperatorInfo("يو (YOU)", …)  ← خطأ
```

الصحيح المثبَّت من المصادر: **`71`=سبأفون · `73`=YOU · `70`=Y Telecom ·
`77`+`78`=يمن موبايل**. الخطأ لم يكن تجميلياً: نظام التوجيه يختار الشريحة
المطابقة لمشغّل الوجهة (Least Cost Routing)، فخطأ التصنيف = توجيه خارج
الشبكة = **كلفة أعلى على كل مكالمة**.

كذلك حُذفت خريطة الهاتف الثابت الناقصة (5 محافظات) واستُبدلت بالسبع الكاملة.

---

## 6) ما لم يُفحص بعد

- **103 ملف `.md`** في المجلدات الفرعية (`docs/` 28، `RED_Ultimate_V1-main/` 25،
  `RED_Ultimate/` 15) — لم تُراجع بمنهج القراءة الفعلية.
- **`app/`** (fork Signal، 6,963 ملفاً) باقٍ كمرجع استخراج خارج شجرة البناء،
  وفيه **31 ملفاً** بحزمة `com.red.sovereign` صفرَ الاعتماد على Signal.
- **العمل المحلي غير المرفوع** في `pro_new` فرع `backup/strongest-2026-08-14`
  (~111 معدَّلاً + ~70 جديداً). ⚠️ فيه `android/features/pstn/PstnSipEngine.kt`
  ومجلده محذوف الآن — يلزم توجيهه إلى `red-app` عند الدمج.
- **لا ترجمة فعلية**: البيئة بلا JDK/Gradle. التحقق كان بفحص التوازن
  والرموز والتوقيعات.

---

# الجولة الثانية — تحقق من الويب واستعادة إضافية

## 7) تحقق `71` سبأفون — أعيد من الصفر

المستخدم شكّك في الإسناد، فأعدتُ البحث بدل الاتكاء على ما سُجِّل سابقاً.
**النتيجة تؤكد ما في الكود**: `71` = سبأفون، من عشرة مصادر مستقلة —
ويكيبيديا الإنجليزية والعربية، Grokipedia، dialingcodes، chahaoba،
phonecardchoice، mycountrymobile، numberozo، وتطبيق «يمن بوك» نفسه
(«يمن موبايل 77-78، يو 73، سبأفون 71، واي 70»).

| البادئة | المشغّل | التقنية |
|---|---|---|
| 70 | Y Telecom (واي) | GSM 900, 4G |
| **71** | **سبأفون** | GSM, 4G |
| **722** | **سبأفون عدن 4G** ← جديد | VoLTE, 4G/5G |
| 73 | YOU / اليمنية العمانية (MTN سابقاً) | GSM, 4G |
| 77 · 78 | يمن موبايل | CDMA2000, 3G, 4G |
| 10 | يمن فورجي | LTE ثابت |

المصدر الوحيد المخالف (`callmama.com`) يناقض **نفسه** في الصفحة ذاتها —
يقول «073, 070, 075 Yemen Mobile» ثم «Sabafon 71» — ويخترع `075` و`079`
لا وجود لهما. مرفوض.

## 8) البادئة `722` — كانت مرفوضة ووُجدت بالتحقق

البحث كشف ما لم يكن في أي جدول لدينا: **سبأفون عدن أطلقت نطاقاً مستقلاً
`722` لشبكة الجيل الرابع (VoLTE)**، مؤكداً من ويكيبيديا العربية، ومن متجر
اتصالات في عدن يقول حرفياً «أرقام جديدة تبدأ بـ722، والشرائح القديمة
ستبقى 71»، ومن رقم حقيقي منشور `722012919`.

**الخطر الذي كان قائماً**: التصنيف يقرأ **رقمين فقط**، فـ`722012919` يُقرأ
`72` → «غير معروف» → يرفضه `PstnCallService` قبل الطلب. **كل مشترك سبأفون
4G في عدن كان يتعذّر الاتصال به.** وفي كاشف المشغّل كان يسقط إلى فرع
الهاتف الثابت فيُنسب لمحافظة خطأً.

**الإصلاح — ثلاث طبقات، مطابقة الأطول أولاً:**

| الملف | التغيير |
|---|---|
| `DinstarLoadBalancer.kt` | `OPERATOR_PREFIXES_3` تُفحص قبل جدول الرقمين |
| `DinstarModels.kt` | `SABAFON.prefixes = {"71", "722"}` + `fromNumber` يجرّب 3 ثم 2 |
| `YemeniOperatorDetector.kt` | يستدعي `fromNumber` بدل `fromPrefix(substring(0,2))` |

`718` (عدن القديم) يطابق `71` أصلاً فلا يحتاج إدخالاً. و`721`/`723` تبقى
مرفوضة لأنها غير مخصَّصة — `722` وحده المخصَّص من نطاق `72`.

**19 اختباراً** الآن، منها أربعة جديدة تغطي `722` بكل صيغه الدولية.

## 9) إعادة اتصال WebSocket — فجوة ثانية استُعيدت

`DinstarWebSocketBridge` في `red-app` أنضج في التحليل (يحمل `gatewayId`،
ويميّز `signalDbm = null` عن `0` التي تعني ‎-113 dBm) — **لكنه لا يعيد
الاتصال**. النسخة المحذوفة من `android/` كانت تملك تأخيراً تصاعدياً.

هذا أخطر مما يبدو: البوابة على شبكة محلية قد تنقطع، وبلا إعادة اتصال
يبقى المشغّل أمام **لوحة جامدة تبدو سليمة** — لا رسالة خطأ، فقط أحداث
توقفت. عطل صامت أسوأ من عطل معلن.

**ما استُعيد** — مع تحسينات على الأصل:

- تأخير تصاعدي `1s → 2s → 4s … 60s`، 10 محاولات كحد أقصى
- `WsConnectionState` (متصل/جارٍ/غير متصل/فشل) معروضة عبر `wsConnectionState`
- **`closedByClient`** — لم تكن في الأصل: تميّز الإغلاق المتعمَّد عن
  الانقطاع، وبدونها كان `disconnect()` يُطلق دورة إعادة اتصال عبثية
- **تصفير العدّاد عند النجاح** — الانقطاع التالي يبدأ من ثانية لا من آخر تأخير
- `readTimeout = 0` — المهلة الافتراضية تقطع WebSocket طويل الأمد بصمت
- `send()` لإرسال الأوامر عبر القناة الحية

## 10) حصيلة الاستعادة

| الفجوة | الأثر لو بقيت |
|---|---|
| `DinstarDeviceStatus` | لا رؤية لحرارة/ذاكرة البوابة — الاختناق الحراري يسبق سقوط المنافذ |
| البادئة `722` | تعذُّر الاتصال بكل مشتركي سبأفون 4G في عدن |
| إعادة اتصال WS | لوحة جامدة تبدو سليمة عند انقطاع الشبكة |

---

# الجولة الثالثة — فحص الـ56,809 سطراً المحذوفة

## 11) أين ذهبت الأسطر فعلاً

| النوع | أسطر محذوفة | ملفات | الحكم |
|---|---|---|---|
| `.txt` | **30,362** | 2 | `MANIFEST_ALL_FILES.txt` — قائمة مسارات، لا كود |
| `.kt` | **16,779** | 127 | محور الفحص ↓ |
| `.json` | 3,115 | 7 | أقفال حزم مكرّرة |
| `.md` | 2,930 | 73 | تقارير مؤرشفة (نُقلت لا حُذفت) |
| `.tsx`/`.jsx` | 3,153 | 96 | لقطات أرشيف — أكبرها 135 سطراً |

**أكثر من نصف الحذف (30 ألف سطر) ملف واحد يسرد أسماء ملفات.** حجمه
مضلِّل تماماً.

## 12) منهج فحص الـ127 ملف Kotlin

استخرجتُ **كل دالة عامة** محذوفة وبحثتُ عن اسمها في المشروع الحيّ
(العميل + الخادم). النتيجة: **212 دالة بلا أثر اسمي**. ثم فحصتُ كل
مجموعة بمضمونها لا باسمها:

| المجموعة | الحكم بعد الفحص |
|---|---|
| `SovereignDaos.kt` — 82 دالة | كيانات `red-app` أدمج: `CallLogEntity` فيه `route="DINSTAR"` و`type` يغطي PSTN، فلا حاجة لجدول `pstn_logs` منفصل |
| تصفية سجل المكالمات (7 استعلامات) | **موجودة بالفعل** — `RedDashboard.kt:2354` فيه 8 فلاتر (الكل/فائتة/صوت/فيديو/جماعية/بث/مساحات/DINSTAR) تعمل في الذاكرة |
| `selectOptimalPort` | **مرفوض بقرار معماري**: اختيار المنفذ في الخادم؛ تكراره يُنتج مصدرَي حقيقة |
| Circuit Breaker | **لا يُضاف**: كان يحمي حلقة استطلاع دوري، والبنية الحالية **دفع عبر WebSocket** بلا استطلاع. التأخير التصاعدي في الجسر يؤدي الدور |
| `UserIntelligenceTab.tsx` | يستدعي `/api/admin/users` وحدها — مغطاة في `Approvals.tsx` و`AuthorityTab.tsx` |

## 13) علّة حقيقية وُجدت أثناء الفحص — قراءة الحمولة

الخادم يغلّف كل حدث تحت `data`:

```kotlin
mapOf("type" to eventType, "timestamp" to …, "data" to data)
```

والعميل كان يقرأ `gatewayId` و`port` **من جذر JSON**. النتيجة صمت خبيث:
`gatewayId` فارغ دائماً، و`port` يساوي `0` — **و`0` منفذ صالح**، فيبدو
كأن المنفذ الأول هو المتغيّر في كل مرة.

الإصلاح: `val body = json.optJSONObject("data") ?: json` — مع تراجع إلى
الجذر احتياطاً، و`optInt("port", -1)` بدل `0` كي يُميَّز الغياب.

## 14) أربعة أحداث كان الخادم يبثّها ولا يستقبلها أحد

الخادم يبثّ **8 أنواع**، والعميل كان يعالج **4**:

| الحدث | ما كان يضيع |
|---|---|
| `DINSTAR_DEVICE_STATUS` | قياسات العتاد تصل دفعاً — كنا نستعلمها يدوياً فقط |
| `DINSTAR_USSD` | **رد USSD غير متزامن، لا يعود في استجابة الطلب أبداً** — فكان الأمر يُرسل بلا رد ظاهر |
| `DINSTAR_PORT_CONTROL` | تحكّم من عميل آخر — لوحتان تتضاربان بلا مزامنة |
| `DINSTAR_EXCEPTION` | **الأخطر**: فشل مكالمة، شريحة منزوعة، منفذ ساقط |

`DINSTAR_EXCEPTION` كان يُهمل بالكامل. المشغّل لا يعرف أن شريحةً سقطت
إلا حين تفشل المكالمات عليها — لذا رُبط بـ`DinstarCommandResult.Error`
ليظهر خطأً صريحاً لا سطر سجل.

## 15) حصيلة الجولات الثلاث

| ما استُعيد | لماذا كان مهماً |
|---|---|
| `DinstarDeviceStatus` | الحرارة تسبق سقوط المنافذ |
| البادئة `722` | تعذُّر الاتصال بكل مشتركي سبأفون 4G في عدن |
| إعادة اتصال WebSocket | لوحة جامدة تبدو سليمة |
| قراءة `data` | `port = 0` كاذب يبدو منفذاً حقيقياً |
| 4 أحداث WS | ردود USSD وأعطال البوابة تضيع صامتة |

**ما رُفض عمداً** — وهذا جزء من التدقيق لا نقص فيه: `selectOptimalPort`
(مصدر حقيقة ثانٍ)، Circuit Breaker (يحمي استطلاعاً لا وجود له)،
استعلامات التصفية (مكرّرة في الواجهة)، `pstn_logs` (مدمج في `call_logs`).

---

# الجولة الرابعة — مسح شامل لكل مصدر محتمل

سؤال المستخدم: هل توجد فروع أخرى، أو ملفات في المجلد الرئيسي، أو أي شيء
محلي؟ فُحص **كل** مصدر يمكن أن يخفي عملاً.

## 16) ما فُحص وما وُجد

| المصدر | النتيجة |
|---|---|
| فروع محلية | 2 فقط (`arena/01a00f9e-pro-v1`, `main`) |
| **فروع الخادم** | **28** — لا `local-work-*` ولا `backup/strongest-*` |
| `git stash` | فارغ |
| ملفات غير متتبَّعة | **صفر** |
| ملفات متجاهَلة | 1 (`aistudio-export/node_modules/`) |
| `git worktree` | واحد فقط |
| الوسوم (tags) | صفر محلياً وبعيداً |
| **كائنات معلّقة** | **1** — فُحص ↓ |
| أقراص مركّبة (`/mnt`, `/media`) | فارغة |

## 17) الكائن المعلّق `2c9af7556`

كوميت «استرجاع المفقودات من كل الفروع الـ27» — **نسخة ما قبل rebase**
من `bf5e035a1` الموجود في تاريخ الفرع. عند مقارنته بـ`HEAD` ظهر
**259 ملفاً «غائباً»**، وهو رقم مفزع للوهلة الأولى. التصنيف يحسم:

| الموقع | ملفات كود |
|---|---|
| `android/` | 84 (محذوف عمداً بعد استخراج المفيد) |
| `_conversation_archive/` | 50 (أرشيف محادثات، حُذف بتدقيق) |
| `app-android/` | 26 (محذوف عمداً) |
| **أي مكان آخر** | **صفر** |

والباقي 57 تقريراً `.md` نُقلت إلى `الأرشيف/`. و`aistudio-export`
متطابق حرفياً: **73 ملفاً = 73 ملفاً، صفر فروق**.

⇒ **الكائن المعلّق لا يحمل شيئاً مفقوداً.**

## 18) مسح الفروع الـ28 بحثاً عن كود غير مدموج

فُحص كل فرع مقابل `HEAD` مع استبعاد النسخ المكررة (`pro/`, `project/`,
`missing_files/`) ومجلدات حُذفت عمداً:

- بصمات أولية: **2,747**
- بعد إزالة التكرار: **2,677 ملفاً فريداً**
- **بمسار حقيقي داخل `RED_Ultimate_V1-main/`: 16 فقط**

من الستة عشر، **خمسة عشر مرفوضة سابقاً بمبرر موثَّق**: `AuthFlow.kt`
(مؤرشف)، `LiveStreamPublicController` (6 مسارات متعارضة تُسقط Spring)،
`DinstarTab.tsx`، `WebSocketConfig.kt` كملف جديد، وهجرات SQL متعارضة
(`V23`, `V24`, `V25`×2, `V26`, `V30`×2, `V31`×2).

## 19) الملف السادس عشر — استُعيد

**`AdminSocialController.kt`** (231 سطراً، من `feat/admin-social-management`)
— وحدة إدارة كاملة للمجموعات والمنشورات، **لم تُفحص من قبل** لأن الفرع
ليس من فروع `arena/`.

عشرة مسارات تحت `/api/admin/social`: إحصائيات المجموعات، قائمة بحث
مرقّمة، تفاصيل مع الأعضاء، حذف مجموعة مع عضوياتها، إزالة عضو،
إحصائيات المنشورات، قائمة بحث، **حذف ناعم واستعادة**.

**تحقّقات ما قبل الاستعادة** — كلها اجتازت:

| الفحص | النتيجة |
|---|---|
| MongoDB مُفعَّل؟ | ✅ `spring-boot-starter-data-mongodb` + URI في `application-*.yml` |
| `GroupDocument`/`GroupMember`/`PostDocument` موجودة؟ | ✅ في `groups/` و`social/` |
| كل حقل مستخدَم موجود؟ | ✅ بما فيها `avatarMediaKey`, `reactionCounts`, `deletedAt` |
| `PostKind.POLL` موجود؟ | ✅ |
| مسارات `/api/admin/social` قائمة؟ | ❌ غائبة تماماً — لا تعارض |
| محمي أمنياً؟ | ✅ `SecurityConfig:112` يفرض `hasRole("ADMIN")` على `/api/admin/**` |
| توازن الأقواس | ✅ |

الحذف الناعم (`deletedAt`) مع مسار استعادة قرار صائب: الإشراف على
المحتوى يحتاج تراجعاً، والحذف الصلب يجعل الخطأ نهائياً.

## 20) الخلاصة — لا يوجد عمل ضائع في أي مصدر يمكن الوصول إليه

بعد مسح الفروع والـstash والكائنات المعلّقة والملفات غير المتتبَّعة
والأقراص: **المستودع مكتمل**. الاستثناء الوحيد خارج نطاقي تماماً:

> **`C:\Users\hpc01\Pictures\pro_new` فرع `backup/strongest-2026-08-14`**
> — تأكّد بالفحص أنه **غير مرفوع إلى الخادم**. أنا في صندوق معزول بلا
> اتصال بقرصك، فلا سبيل لجلبه إلا أن ترفعه أنت.

---

# الجولة الخامسة — إعادة فحص كل مصدر من الصفر

أُعيد الفحص بعد `git fetch --all --prune` تحسّباً لرفع جديد، وبمنهج
أوسع: هذه المرة **بلا تقييد بامتدادات الكود**، لأن التقييد السابق كان
يحجب ملفات مثل `.env`.

## 21) الفروع — 28، لا جديد

عدد الفروع كما هو. **لا `local-work-*` ولا `backup/strongest-*`** حتى
هذه اللحظة. توزيع العمل غير المدموج:

| الفئة | العدد |
|---|---|
| فروع `arena/` | 23 |
| `main` | 1 |
| `feat/admin-social-management` | 1 |
| `fix/restore-websocket-registrations` | 1 |
| `revert-37-…` | 1 |

## 22) الملفات الغائبة عن HEAD — الصورة الكاملة

اتحاد أشجار الفروع الـ28 مقابل HEAD:

| المرحلة | العدد |
|---|---|
| كل الملفات الغائبة | 20,638 |
| بعد استبعاد نسخ `pro/`, `project/`, `missing_files/` والمجلدات المحذوفة عمداً | **74** |
| منها ملفات كود | 20 |
| منها تقارير `.md` مؤرشفة | 53 |
| منها **`.env`** | 1 ⚠️ |

الرقم 20,638 مخيف ظاهرياً، لكن 99.6% منه نسخ مكررة كاملة للمشروع
داخل `pro/` و`project/` — وهي بالضبط ما حُذف في جولة توحيد سابقة.

## 23) الملفات العشرون — ثلاثة لم تُفحص من قبل

سبعة عشر مرفوضة بمبررات موثّقة سابقاً. الثلاثة الجديدة:

### أ) `controllers/LiveStreamController.kt` (60 سطراً) — **مرفوض**

موجود اليوم باسم `controllers/LiveStreamAdminController.kt` بنفس
`@RequestMapping("/api/live")` ونفس المسارات الستة. الفرق الوحيد
**لصالح النسخة الحالية**:

```kotlin
// المرشح: أي شخص يمرّر أي معرّف مشاهد
fun join(@PathVariable streamId: String, @RequestParam viewerId: String)

// الحالي: الهوية من الرمز المصادَق عليه
fun join(@PathVariable streamId: String, authentication: Authentication)
```

المرشح يسمح بانتحال هوية أي مشاهد وتضخيم العدّاد. استعادته **تراجع
أمني**، ولو أُضيف لتعارض مع الحالي وأسقط بدء Spring.

### ب) `pstn/DinstarWebSocketHandler.kt` (198 سطراً) — **مرفوض**

النسخة الحالية `websocket/DinstarWebSocketHandler.kt` أكبر (243 سطراً)
وتغطي كل أحداثه. الدوال الثلاث الزائدة عنده — `broadcastAlert`,
`getActiveSessionCount`, `closeAllSessions` — فُحص عدد مستدعيها في
كامل المشروع: **صفر لكل واحدة**. كود ميت.

وتأكّد أن الحالي **مسجَّل فعلاً**: `websocket/WebSocketConfig.kt:102`
يربط `/ws/dinstar`، ضمن سبعة مسارات كلها بـ`setAllowedOriginPatterns`.

### ج) `backend-server/gradle/verification-metadata.xml` (2,977 سطراً) — **مرفوض، وإضافته تكسر البناء**

ملف تحقّق تجزئات التبعيات. `backend-server` بناء مستقل (له
`settings.gradle.kts` خاص وليس ضمن `include` الجذر)، فملف الجذر
(1,314 مكوّناً) لا يغطيه — بدا للوهلة الأولى نقصاً حقيقياً.

لكن مطابقة تبعياته العشر الحالية بالمرشح:

| التبعية | في الملف؟ |
|---|---|
| okhttp 4.12.0, okhttp-digest 3.1.1, jjwt×3 0.12.6, minio 8.6.0, asterisk-java 3.41.0, bcprov 1.79, mockito-kotlin 6.1.0 | ✅ |
| **`org.jsoup:jsoup:1.18.3`** | ❌ **مفقود** |

وضع تحقّق التبعيات مع مكوّن مفقود يعني **فشل البناء فوراً**
(`Dependency verification failed`). الملف لُقط قبل إضافة `jsoup`.
مرفوض ما لم يُعَد توليده بـ`--write-verification-metadata`.

## 24) ⚠️ الاكتشاف المهم — ثغرة تسريب أسرار

الفرع **`arena/019fe9e3-pro-v1` يحوي `.env` في الجذر**. القيم
تطويرية افتراضية (`red_…`, `admin`, `127.0.0.1`) فلا تسريب إنتاج،
لكن البنية كاملة: `JWT_SECRET`, `TURN_SECRET`, `AMI_PASSWORD`,
`DB_PASSWORD`, `MONGO_PASSWORD`, `MINIO_PASSWORD`, `REDIS_PASSWORD`,
`RED_ADMIN_PASSWORD`, `DINSTAR_PASSWORD`.

غيابه عن HEAD صحيح. **لكن `.gitignore` لم يكن يمنع عودته**:

| المسار | قبل | بعد |
|---|---|---|
| `.env` (الجذر) | ❌ يُلتقط | ✅ متجاهَل |
| `backend-server/.env` | ✅ (بـ`.gitignore` فرعي) | ✅ |
| **`red-app/keystore.properties`** | ❌ يُلتقط | ✅ متجاهَل |
| `*.jks` / `*.keystore` | ❌ | ✅ |
| `cookies.txt` | ❌ | ✅ |
| `build_errors*.txt`, `logcat.txt` | ❌ | ✅ |

`keystore.properties` هو الأخطر: كلمة سر مفتاح توقيع التطبيق. من
يملكها يوقّع تحديثاً مزيّفاً يثبّته المستخدمون على أنه أصلي.

أُضيفت القواعد مع `!.env.example` و`!.env.template` كي لا تُحجب
القوالب المشروعة. تحقّق: **صفر ملف متتبَّع تأثّر بالقواعد الجديدة**.

هذه سُدَّت **قبل** رفعك لعملك المحلي، وهو التوقيت المناسب تماماً.

## 25) كل ما هو محلي — لا شيء مخفي

| الفحص | النتيجة |
|---|---|
| ملفات غير متتبَّعة (`-uall`) | **0** |
| ملفات معدَّلة | **0** |
| متجاهَل | 1 (`aistudio-export/node_modules/`) |
| `git stash` | 0 |
| `git worktree` | 1 |
| الوسوم | 0 محلياً، 0 بعيداً |
| كائنات معلّقة | 1 (`2c9af7556` — حُلّل، لا يحوي جديداً) |
| `.git/info/exclude` | فارغ |
| مجلدات `build/` تحوي مصادر | **0** (فلا كود يخفيه `**/build/`) |
| مستودعات git أخرى في النظام | **0** |
| محتوى `/home/user` خارج المستودع | لا شيء سوى ملفات bash الافتراضية |

الجذر يحوي 56 مدخلاً، كلها متتبَّعة: 30 تقريراً `.md`، أربع صور
(`ICON1.png` وغيرها)، سكربتات (`RUN.bat`, `run.sh`, `start-preview.sh`,
`merge-local-copies.ps1`)، ومجلدات المشروع.

## 26) الخلاصة النهائية

**لا يوجد أي عمل ضائع يمكن الوصول إليه من هذه البيئة.** فُحص كل
مصدر ممكن — فروع، stash، كائنات معلّقة، ملفات غير متتبَّعة، مجلدات
البناء، القرص كله — والنتيجة صفر.

الاستثناء الوحيد يبقى خارج متناولي تماماً:

> **`C:\Users\hpc01\Pictures\pro_new`، فرع `backup/strongest-2026-08-14`**
> — لا وجود له على الخادم. أنا في صندوق معزول بلا أي اتصال بجهازك،
> فالسبيل الوحيد أن ترفعه أنت. و`.gitignore` صار الآن جاهزاً لاستقباله
> بأمان.
